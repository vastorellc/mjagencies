---
phase: 06-auto-upload-scheduling
plan: 05
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified: []
autonomous: false
requirements:
  - AUTOUP-01
  - AUTOUP-02
  - AUTOUP-03
  - AUTOUP-04
  - AUTOUP-05
  - AUTOUP-06
  - AUTOUP-07
  - AUTOUP-08
  - STORE-02
  - STORE-03
  - STORE-05

must_haves:
  truths:
    - "All 8 AUTOUP requirements are verifiable via automated checks or smoke test"
    - "STORE-02: Nginx serves /uploads/ at VPS_PUBLIC_URL over public HTTPS (env confirmed)"
    - "STORE-03: File deleted after successful upload (verified by grep in upload workers)"
    - "STORE-05: Instagram 100 MB gate present in both frontend and backend"
    - "tsc --noEmit passes in both frontend/ and backend/"
    - "All scheduling unit tests pass"
  artifacts:
    - path: "backend/src/lib/upload-youtube.ts"
      provides: "resumable YouTube upload — verified by grep for createReadStream"
    - path: "backend/src/lib/upload-instagram.ts"
      provides: "container-in-job Instagram upload — verified by grep for createContainer in function body"
    - path: "backend/src/lib/scheduling.ts"
      provides: "getPeakTimes — verified by vitest"
  key_links:
    - from: "frontend/src/pages/GeneratorPage.tsx"
      to: "frontend/src/components/ScheduleModal.tsx"
      via: "scheduleModal state triggers render"
      pattern: "scheduleModal"
---

<objective>
Verification checkpoint — all Phase 6 plans complete. Run automated checks, inspect critical code paths, and confirm success criteria from ROADMAP.md are met.

Purpose: Catch any TypeScript errors, missing wires, or spec violations before closing the phase.
Output: Phase 6 verification evidence; SUMMARY files complete; STATE.md updated.
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification — tsc + vitest + grep checks</name>

  <read_first>
    backend/src/lib/upload-youtube.ts
    backend/src/lib/upload-instagram.ts
    backend/src/lib/upload-facebook.ts
    backend/src/lib/upload-tiktok.ts
    backend/src/lib/upload-worker.ts
    backend/src/lib/scheduling.ts
    backend/src/routes/upload.ts
    frontend/src/pages/GeneratorPage.tsx
    frontend/src/components/ScheduleModal.tsx
    frontend/src/lib/api.ts
  </read_first>

  <files></files>

  <action>
Run each check. Fix any failures before proceeding to the human checkpoint.

**Check 1 — Backend TypeScript:**
```bash
cd C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend && npx tsc --noEmit
```
Expected: exits 0, no output.

**Check 2 — Frontend TypeScript:**
```bash
cd C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend && npx tsc --noEmit
```
Expected: exits 0, no output.

**Check 3 — Scheduling unit tests:**
```bash
cd C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend && npx vitest run src/test/scheduling.test.ts
```
Expected: all 8 tests pass.

**Check 4 — AUTOUP-01: YouTube uses resumable stream:**
```bash
grep -n "createReadStream" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-youtube.ts
```
Expected: at least one match (the media.body line).

**Check 5 — AUTOUP-02: Instagram container created inside uploadInstagram function (not module level):**
```bash
grep -n "createContainer\|me/media" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-instagram.ts
```
Expected: `createContainer(publicUrl` called inside the `uploadInstagram` async function body.

**Check 6 — AUTOUP-02: Instagram publishes ONLY after FINISHED:**
```bash
grep -n "FINISHED\|media_publish" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-instagram.ts
```
Expected: `media_publish` call is conditional on `finalStatus === 'FINISHED'`.

**Check 7 — AUTOUP-03: Facebook reads page_access_token:**
```bash
grep -n "page_access_token\|page_id\|pageToken\|pageId" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-facebook.ts
```
Expected: references to `page_id` and token from `platform_config.facebook`.

**Check 8 — AUTOUP-04: TikTok flag gate:**
```bash
grep -n "isTikTokApproved\|TIKTOK_APPROVED" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-worker.ts
```
Expected: `process.env.TIKTOK_APPROVED === 'true'` check in the tiktok worker.

**Check 9 — STORE-03: deleteFile called in all workers:**
```bash
grep -rn "deleteFile" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-youtube.ts C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-instagram.ts C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-facebook.ts
```
Expected: one match per file (after success).

**Check 10 — STORE-05: Instagram 100 MB gate in backend:**
```bash
grep -n "100 \* 1024 \* 1024\|instagram_file_too_large" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/routes/upload.ts
```
Expected: the gate and error code both present.

**Check 11 — STORE-05: Instagram 100 MB gate in frontend:**
```bash
grep -n "100 \* 1024 \* 1024" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend/src/pages/GeneratorPage.tsx
```
Expected: the frontend gate present.

**Check 12 — AUTOUP-07: pg-boss createQueue before work:**
```bash
grep -n "createQueue\|boss.work" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-worker.ts
```
Expected: createQueue calls appear before work calls in file order.

**Check 13 — AUTOUP-05/06: peak-times endpoint registered:**
```bash
grep -n "peak-times\|getPeakTimes" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/routes/upload.ts
```
Expected: GET handler and import both present.

**Check 14 — AUTOUP-08: Realtime onUpload wired (not empty handler):**
```bash
grep -n "handleUpload\|onUpload" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend/src/pages/GeneratorPage.tsx
```
Expected: `onUpload={() => { void handleUpload(platform) }}` (not the old empty `// Phase 6 wires this` comment).

**Check 15 — VPS_PUBLIC_URL in REQUIRED_ENV:**
```bash
grep -n "VPS_PUBLIC_URL" C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/index.ts
```
Expected: present in the REQUIRED_ENV array.

If any check fails, fix the issue before proceeding to the human checkpoint task.
  </action>

  <verify>
    <automated>cd C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend && npx tsc --noEmit && echo "BACKEND_OK" && cd C:/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend && npx tsc --noEmit && echo "FRONTEND_OK"</automated>
  </verify>

  <acceptance_criteria>
    - Backend `tsc --noEmit` exits 0 (BACKEND_OK printed)
    - Frontend `tsc --noEmit` exits 0 (FRONTEND_OK printed)
    - All 8 scheduling tests pass in vitest run
    - All 15 grep checks return expected matches (no unexpected empty results)
    - No check reveals a spec violation (e.g. multipart on YouTube, container outside worker, empty onUpload)
  </acceptance_criteria>

  <done>All automated checks pass; no TypeScript errors; all spec constraints verified by grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 6 complete. The platform auto-upload flow is fully wired:
- Backend: POST /api/upload/file (multer → VPS disk), POST /api/upload/schedule (pg-boss), GET /api/upload/peak-times
- Four platform workers: YouTube (resumable stream), Instagram (container-in-job + poll loop), Facebook (page token + 3-phase), TikTok (stub behind flag)
- Frontend: ScheduleModal with PKT peak times, handleUpload with 100 MB Instagram gate, handleScheduleConfirm calling uploadFile + scheduleUpload
- Supabase Realtime subscription from Phase 5 pushes upload status changes to the UI without polling
  </what-built>

  <how-to-verify>
Run the backend dev server and frontend dev server, then verify these flows:

**Automated checks (should all pass from Task 1):**
1. Backend tsc --noEmit exits 0
2. Frontend tsc --noEmit exits 0
3. Scheduling tests: all 8 pass

**Manual smoke test (requires connected OAuth accounts from Phase 2):**
1. Start backend: `cd backend && npm run dev`
2. Start frontend: `cd frontend && npm run dev`
3. Log in as a user who has YouTube/Instagram/Facebook connected in Settings
4. Generate copy for any video/description on the Generator page
5. Click Upload on the YouTube card → ScheduleModal opens → shows Fri/Sat/Sun PKT peak slots
6. Select "Upload now" → Confirm → upload_status changes from idle to uploading (button state updates immediately)
7. Wait for pg-boss job to run — upload_status changes from uploading to posted (Realtime push, no page refresh)
8. Try uploading a video over 100 MB to Instagram → clear error message shown, modal does NOT open
9. TikTok card upload button is greyed out as "Available once API approved" — not clickable

**ROADMAP success criteria check:**
- AUTOUP-01: YouTube upload uses resumable (grep confirms createReadStream in media.body)
- AUTOUP-02: Meta container created in worker at fire time (grep confirms createContainer inside uploadInstagram function)
- AUTOUP-03: Facebook uses page_access_token + page_id (grep confirms)
- AUTOUP-04: TikTok behind isTikTokApproved flag (grep confirms)
- AUTOUP-05: Peak times appear in modal for youtube/instagram/facebook
- AUTOUP-06: Peak time values correct (YouTube: Fri/Sat/Sun 6pm+8pm PKT etc.)
- AUTOUP-07: scheduledAt passed as startAfter to pg-boss (job survives restart)
- AUTOUP-08: Upload status changes pushed via Realtime — no polling needed
- STORE-02: VPS_PUBLIC_URL in env.example and validated on startup
- STORE-03: deleteFile() called after successful upload in each worker
- STORE-05: Instagram 100 MB gate in frontend AND backend
  </how-to-verify>

  <resume-signal>Type "approved" to close Phase 6, or describe any failures found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification phase | Read-only grep + tsc checks — no new code written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Information Disclosure | Phase 6 verification log | accept | Verification output is local dev output only; no sensitive data in grep patterns |
</threat_model>

<verification>
All verification is performed in Task 1 and the human checkpoint.
</verification>

<success_criteria>
- All 15 automated grep/tsc/vitest checks pass
- Human smoke test confirms upload state machine works end-to-end
- All 9 ROADMAP Phase 6 success criteria confirmed
- Phase 6 closed; STATE.md updated to Phase 7
</success_criteria>

<output>
After approval, create `.planning/phases/06-auto-upload-scheduling/06-05-SUMMARY.md` and update `.planning/STATE.md` to reflect Phase 6 complete.
</output>
