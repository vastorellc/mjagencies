---
phase: 04-virality-score-checklist
plan: 05
type: execute
wave: 2
depends_on: [04-01, 04-03]
files_modified:
  - frontend/src/components/PlatformCardGrid.tsx
  - frontend/src/components/PlatformCardGrid.test.tsx
autonomous: true
requirements: [SCORE-03, SCORE-04]
tags: [ui, platform-cards, tailwind]

must_haves:
  truths:
    - "Renders 5 mini-cards in a horizontal grid (one per platform: youtube, instagram, tiktok, facebook, x)"
    - "Each card shows: 1-letter circle (Y/I/T/F/X), platform-specific score number, expected view range from VIEW_RANGES table"
    - "View range comes from viewRangeFor(platform, perPlatformScore) — uses platform's OWN score (not overall, per SCORE-04)"
    - "Score number is colored per its band (D-23 palette)"
    - "Mobile-friendly: grid-cols-5 desktop, 2-row stacked on small screens"
  artifacts:
    - path: "frontend/src/components/PlatformCardGrid.tsx"
      provides: "PlatformCardGrid React component"
      exports: ["default PlatformCardGrid"]
    - path: "frontend/src/components/PlatformCardGrid.test.tsx"
      provides: "Render tests for all 5 platforms"
  key_links:
    - from: "frontend/src/components/PlatformCardGrid.tsx"
      to: "frontend/src/lib/viewRange.ts"
      via: "import { viewRangeFor }"
      pattern: "from '\\.\\./lib/viewRange'"
    - from: "frontend/src/components/PlatformCardGrid.tsx"
      to: "frontend/src/lib/score.ts"
      via: "import { bandForScore }"
      pattern: "from '\\.\\./lib/score'"
---

<objective>
Implement the per-platform mini-card grid (D-22 #2). Each card surfaces 3 facts: which platform, that platform's score, and the expected view range bucket from D-13. Display palette uses D-23 band colors on the score number itself.

Purpose: Per-platform variant visualization (SCORE-03) + view-range surfacing (SCORE-04). Consumed by GeneratorPage in 04-08.
Output: One component file + render tests.
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-virality-score-checklist/04-CONTEXT.md
@viral-copy-generator/frontend/src/lib/types.ts
@viral-copy-generator/frontend/src/lib/score.ts
@viral-copy-generator/frontend/src/lib/viewRange.ts
@viral-copy-generator/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlatformCardGrid.tsx + tests</name>
  <files>frontend/src/components/PlatformCardGrid.tsx, frontend/src/components/PlatformCardGrid.test.tsx</files>
  <read_first>
    - frontend/src/lib/score.ts (bandForScore)
    - frontend/src/lib/viewRange.ts (viewRangeFor)
    - frontend/src/lib/types.ts (Platform, PerPlatformScores, ColorBand)
    - .planning/phases/04-virality-score-checklist/04-CONTEXT.md D-22, D-23, specifics (1-letter circles)
    - viral-copy-generator/CLAUDE.md (Tailwind only)
  </read_first>
  <behavior>
    - Component signature: `(props: { perPlatform: PerPlatformScores }) => JSX.Element`
    - Renders exactly 5 cards in stable order: youtube, instagram, tiktok, facebook, x
    - Each card has data-testid="platform-card-{platform}"
    - 1-letter circle uses platform-specific bg color: YouTube red, Instagram pink, TikTok black, Facebook blue, X black (UI-03 mapping)
    - Score number colored per band (text-red-500 / text-amber-500 / text-green-500 / text-emerald-400)
    - View range string rendered below score
    - Layout: `grid grid-cols-5 gap-2` desktop; on mobile let it wrap (use grid-cols-2 sm:grid-cols-5 to be safe)
  </behavior>
  <action>
Create `frontend/src/components/PlatformCardGrid.tsx`:

```typescript
import { bandForScore } from '../lib/score'
import { viewRangeFor } from '../lib/viewRange'
import type { Platform, PerPlatformScores, ColorBand } from '../lib/types'

interface Props {
  perPlatform: PerPlatformScores
}

interface PlatformMeta {
  letter: string  // 1-letter circle (Y/I/T/F/X)
  label: string
  // Tailwind background for the circle (UI-03 mapping)
  circleBg: string
  circleText: string
}

const PLATFORM_META: Record<Platform, PlatformMeta> = {
  youtube:   { letter: 'Y', label: 'YouTube',   circleBg: 'bg-red-600',     circleText: 'text-white' },
  instagram: { letter: 'I', label: 'Instagram', circleBg: 'bg-pink-500',    circleText: 'text-white' },
  tiktok:    { letter: 'T', label: 'TikTok',    circleBg: 'bg-black',       circleText: 'text-white' },
  facebook:  { letter: 'F', label: 'Facebook',  circleBg: 'bg-blue-600',    circleText: 'text-white' },
  x:         { letter: 'X', label: 'X',         circleBg: 'bg-black',       circleText: 'text-white' },
}

// D-23 score-text palette per band
const BAND_TEXT: Record<ColorBand, string> = {
  'red':          'text-red-500',
  'amber':        'text-amber-500',
  'green':        'text-green-500',
  'bright-green': 'text-emerald-400',
}

const PLATFORM_ORDER: Platform[] = ['youtube', 'instagram', 'tiktok', 'facebook', 'x']

export default function PlatformCardGrid({ perPlatform }: Props) {
  return (
    <div
      data-testid="platform-card-grid"
      className="grid grid-cols-2 gap-2 sm:grid-cols-5"
    >
      {PLATFORM_ORDER.map((p) => {
        const score = perPlatform[p]
        const band = bandForScore(score)
        const meta = PLATFORM_META[p]
        const range = viewRangeFor(p, score)
        return (
          <div
            key={p}
            data-testid={`platform-card-${p}`}
            data-platform={p}
            data-band={band}
            className="flex flex-col items-center gap-1 rounded bg-zinc-900 px-2 py-3"
          >
            <div
              className={`flex h-8 w-8 items-center justify-center rounded-full font-bold ${meta.circleBg} ${meta.circleText}`}
              aria-label={meta.label}
            >
              {meta.letter}
            </div>
            <div
              data-testid={`platform-score-${p}`}
              className={`text-2xl font-bold ${BAND_TEXT[band]}`}
            >
              {score}
            </div>
            <div
              data-testid={`platform-range-${p}`}
              className="text-[10px] text-zinc-400"
            >
              {range}
            </div>
          </div>
        )
      })}
    </div>
  )
}
```

Create `frontend/src/components/PlatformCardGrid.test.tsx`:

```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import PlatformCardGrid from './PlatformCardGrid'
import type { PerPlatformScores } from '../lib/types'

const mockScores: PerPlatformScores = {
  youtube: 65,
  instagram: 78,
  tiktok: 85,
  facebook: 30,
  x: 50,
}

describe('PlatformCardGrid', () => {
  it('renders exactly 5 cards in YT/IG/TT/FB/X order', () => {
    render(<PlatformCardGrid perPlatform={mockScores} />)
    const grid = screen.getByTestId('platform-card-grid')
    const cards = grid.querySelectorAll('[data-testid^="platform-card-"]')
    expect(cards.length).toBe(5)
    const order = Array.from(cards).map((c) => c.getAttribute('data-platform'))
    expect(order).toEqual(['youtube', 'instagram', 'tiktok', 'facebook', 'x'])
  })

  it('shows 1-letter circles Y, I, T, F, X', () => {
    render(<PlatformCardGrid perPlatform={mockScores} />)
    expect(screen.getByLabelText('YouTube').textContent).toBe('Y')
    expect(screen.getByLabelText('Instagram').textContent).toBe('I')
    expect(screen.getByLabelText('TikTok').textContent).toBe('T')
    expect(screen.getByLabelText('Facebook').textContent).toBe('F')
    expect(screen.getByLabelText('X').textContent).toBe('X')
  })

  it('renders per-platform scores', () => {
    render(<PlatformCardGrid perPlatform={mockScores} />)
    expect(screen.getByTestId('platform-score-youtube').textContent).toBe('65')
    expect(screen.getByTestId('platform-score-instagram').textContent).toBe('78')
    expect(screen.getByTestId('platform-score-tiktok').textContent).toBe('85')
    expect(screen.getByTestId('platform-score-facebook').textContent).toBe('30')
    expect(screen.getByTestId('platform-score-x').textContent).toBe('50')
  })

  it('looks up view range using each platform OWN score (SCORE-04)', () => {
    render(<PlatformCardGrid perPlatform={mockScores} />)
    // tiktok score 85 -> bright-green -> '250k-5M+'
    expect(screen.getByTestId('platform-range-tiktok').textContent).toBe('250k-5M+')
    // facebook score 30 -> red -> '< 500'
    expect(screen.getByTestId('platform-range-facebook').textContent).toBe('< 500')
    // youtube score 65 -> green -> '10k-100k'
    expect(screen.getByTestId('platform-range-youtube').textContent).toBe('10k-100k')
    // x score 50 -> amber -> '500-5k'
    expect(screen.getByTestId('platform-range-x').textContent).toBe('500-5k')
  })

  it('applies band-specific score text color (D-23)', () => {
    render(<PlatformCardGrid perPlatform={mockScores} />)
    const ttScore = screen.getByTestId('platform-score-tiktok') // 85 -> bright-green
    expect(ttScore.className).toContain('text-emerald-400')
    const fbScore = screen.getByTestId('platform-score-facebook') // 30 -> red
    expect(fbScore.className).toContain('text-red-500')
    const ytScore = screen.getByTestId('platform-score-youtube') // 65 -> green
    expect(ytScore.className).toContain('text-green-500')
    const xScore = screen.getByTestId('platform-score-x') // 50 -> amber
    expect(xScore.className).toContain('text-amber-500')
  })

  it('uses platform-specific circle backgrounds (UI-03)', () => {
    render(<PlatformCardGrid perPlatform={mockScores} />)
    expect(screen.getByLabelText('YouTube').className).toContain('bg-red-600')
    expect(screen.getByLabelText('Instagram').className).toContain('bg-pink-500')
    expect(screen.getByLabelText('TikTok').className).toContain('bg-black')
    expect(screen.getByLabelText('Facebook').className).toContain('bg-blue-600')
    expect(screen.getByLabelText('X').className).toContain('bg-black')
  })

  it('grid uses sm:grid-cols-5 layout', () => {
    render(<PlatformCardGrid perPlatform={mockScores} />)
    const grid = screen.getByTestId('platform-card-grid')
    expect(grid.className).toContain('grid')
    expect(grid.className).toContain('sm:grid-cols-5')
  })
})
```
  </action>
  <verify>
    <automated>cd viral-copy-generator/frontend && npx vitest --run --project=unit src/components/PlatformCardGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 7 tests pass
    - `grep -n "PLATFORM_ORDER" frontend/src/components/PlatformCardGrid.tsx` matches and order is youtube, instagram, tiktok, facebook, x
    - `grep -n "viewRangeFor" frontend/src/components/PlatformCardGrid.tsx` matches (uses platform-own score)
    - `grep -n "letter: 'Y'" frontend/src/components/PlatformCardGrid.tsx` matches
    - tsc passes
  </acceptance_criteria>
  <done>PlatformCardGrid renders 5 cards with platform-specific circles, scores, and view ranges; tests green.</done>
</task>

</tasks>

<verification>
- vitest passes
- View range lookup uses each platform's own score (verified in test)
- Band text colors applied correctly
</verification>

<success_criteria>
1. SCORE-03: 5 platform variants rendered
2. SCORE-04: View range uses platform-specific score (not overall)
</success_criteria>

<output>
Create `.planning/phases/04-virality-score-checklist/04-05-SUMMARY.md` after completion.
</output>
