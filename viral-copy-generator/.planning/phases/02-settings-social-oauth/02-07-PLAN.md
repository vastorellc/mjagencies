---
phase: 02-settings-social-oauth
plan: 07
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - .planning/phases/02-settings-social-oauth/02-VERIFICATION.md
autonomous: false
requirements:
  - SETTINGS-01
  - SETTINGS-02
  - SETTINGS-03
  - SETTINGS-04
  - SETTINGS-05
  - SETTINGS-06
  - SETTINGS-07
  - SETTINGS-08
  - SETTINGS-09
  - SETTINGS-10
tags: [verification, e2e, manual-checkpoint, rls-cross-user, oauth-roundtrip]

must_haves:
  truths:
    - "Full Vitest suite (encryption + oauth-state + settings + oauth-google + oauth-meta + meta-refresh) is green"
    - "Backend boots cleanly with all 8 required env vars set; logs include both pg-boss schedule registrations"
    - "Curl smoke for /api/settings is 401 without auth and returns the masked shape with auth"
    - "Real Google OAuth round-trip succeeds end-to-end against the human's actual Google Cloud project"
    - "Real Meta Instagram OAuth round-trip succeeds OR is documented as blocked-on-app-review"
    - "Real Meta Facebook OAuth round-trip succeeds OR documented as blocked"
    - "RLS cross-user check: User A's settings are not visible to User B (DB-level)"
    - "Phase exits with VERIFICATION.md committed and all SETTINGS-NN requirements ticked off in the report"
  artifacts:
    - path: ".planning/phases/02-settings-social-oauth/02-VERIFICATION.md"
      provides: "Test matrix mapping each SETTINGS-NN to its automated and manual evidence"
      contains: "SETTINGS-01"
  key_links:
    - from: "Manual OAuth smoke"
      to: "settings.platform_config in Supabase"
      via: "After click-Connect → check DB row contains ciphertext (not plaintext)"
      pattern: "youtube|instagram|facebook"
---

<objective>
Final phase verification. The first task runs the full automated suite and produces a curl-driven smoke report against a live local server. The second task is a blocking human checkpoint for the real OAuth round-trip across all three providers (these cannot be unit-mocked — they require the user's actual Google Cloud and Meta dashboards). The output is `02-VERIFICATION.md` listing each requirement and its evidence, suitable for `/gsd-verify-work` to ratify the phase.

This plan does NOT modify any source code. If any verification fails, the executor returns control to the user with a structured failure description and a recommendation to invoke `/gsd-plan-phase 2 --gaps`.
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-settings-social-oauth/02-RESEARCH.md
@.planning/phases/02-settings-social-oauth/02-01-SUMMARY.md
@.planning/phases/02-settings-social-oauth/02-02-SUMMARY.md
@.planning/phases/02-settings-social-oauth/02-03-SUMMARY.md
@.planning/phases/02-settings-social-oauth/02-04-SUMMARY.md
@.planning/phases/02-settings-social-oauth/02-05-SUMMARY.md
@.planning/phases/02-settings-social-oauth/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite + curl smoke + write 02-VERIFICATION.md</name>
  <files>.planning/phases/02-settings-social-oauth/02-VERIFICATION.md</files>
  <read_first>
    - All Phase 2 plan SUMMARYs (02-01 through 02-06) — extract test counts and known issues
    - .planning/REQUIREMENTS.md (SETTINGS-01..10 — exact wording)
    - The "KNOWN ISSUE" section of 02-06-SUMMARY (if it flagged the redirect/manual issue)
  </read_first>
  <action>
**Step 1** — Run the full backend suite:

```
cd backend &amp;&amp; npm test
```

Capture: test count, pass count, fail count, total time. If any test fails, ABORT this task and return to the user with the failing test names — do not proceed to Step 2.

**Step 2** — Boot the backend:

```
cd backend &amp;&amp; npm run dev   # background process; capture logs to a temp file
```

Wait until the log line `[server] listening on :3001` appears. Verify:
- `[pg-boss] cleanup-stale-files job registered` is present (Phase 1 sanity)
- `[pg-boss] meta-token-refresh job registered (cron: 0 9 * * 1)` is present (SETTINGS-07)

If either job-registration log is missing, ABORT and report.

**Step 3** — curl smoke (with the dev server running). Use the user's real Supabase admin token (have them paste it) OR generate a JWT via the make-admin script's session. Record commands and outputs verbatim.

```
# Auth gate works
curl -i -s http://localhost:3001/api/settings | head -1
# Expect: HTTP/1.1 401 Unauthorized

# With token (paste the user's session.access_token):
TOKEN="..."
curl -s -H "Authorization: Bearer $TOKEN" http://localhost:3001/api/settings | jq
# Expect shape: { ai_provider, api_key_masked, default_niche, enabled_platforms, connected, timezone: "Asia/Karachi" }

# Save an API key
curl -s -X PATCH -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"api_key":"sk-test-roundtrip-1234"}' http://localhost:3001/api/settings | jq
# Expect: api_key_masked: "****1234"

# GET shows the mask
curl -s -H "Authorization: Bearer $TOKEN" http://localhost:3001/api/settings | jq .api_key_masked
# Expect: "****1234"

# Disconnect (no platform connected yet — should still 200 with no-op)
curl -s -X DELETE -H "Authorization: Bearer $TOKEN" http://localhost:3001/api/settings/connections/youtube | jq
# Expect: { ok: true }

# Stub-no-credentials guard for OAuth init: visiting /api/auth/google/connect should redirect (302)
curl -i -s -H "Authorization: Bearer $TOKEN" http://localhost:3001/api/auth/google/connect | head -5
# Expect: HTTP/1.1 302 Found; Location: https://accounts.google.com/o/oauth2/v2/auth?...
```

**Step 4** — Write `.planning/phases/02-settings-social-oauth/02-VERIFICATION.md`:

```markdown
# Phase 2 Verification Report

**Date:** &lt;today&gt;
**Plans completed:** 02-01 through 02-06
**Verified by:** Plan 02-07

## Automated test summary

- Backend Vitest: &lt;X passed / Y total&gt; in &lt;duration&gt;
  - encryption.test.ts: 9/9
  - oauth-state.test.ts: 5/5
  - settings.test.ts: 10/10
  - oauth-google.test.ts: 5/5
  - oauth-meta.test.ts: 9/9
  - meta-refresh.test.ts: 5/5
- Frontend `tsc --noEmit`: clean
- Frontend `npm run build`: success

## Startup verification

&lt;paste relevant log lines including both pg-boss job registrations&gt;

## Curl smoke matrix

| Endpoint | Expected | Actual | Status |
|----------|----------|--------|--------|
| GET /api/settings (no auth) | 401 | &lt;recorded&gt; | ✅/❌ |
| GET /api/settings (with token) | 200 + shape | &lt;recorded&gt; | ✅/❌ |
| PATCH /api/settings api_key | 200 + ****last4 | &lt;recorded&gt; | ✅/❌ |
| DELETE /api/settings/connections/youtube | 200 ok | &lt;recorded&gt; | ✅/❌ |
| GET /api/auth/google/connect | 302 to accounts.google.com | &lt;recorded&gt; | ✅/❌ |

## Requirements traceability

| ID | Requirement | Evidence | Status |
|----|-------------|----------|--------|
| SETTINGS-01 | Encrypted AI key, masked response | encryption.test.ts + curl PATCH/GET round-trip | ✅/❌ |
| SETTINGS-02 | Default niche | settings.test.ts test 4 + manual UI check | ✅/❌ |
| SETTINGS-03 | Platform toggles | settings.test.ts test 5 + manual UI check | ✅/❌ |
| SETTINGS-04 | YouTube OAuth (redirect) | oauth-google.test.ts + manual round-trip in Task 2 | ✅/❌ |
| SETTINGS-05 | Instagram OAuth (2025 scopes) | oauth-meta.test.ts tests 1-4 + manual round-trip in Task 2 | ✅/❌ |
| SETTINGS-06 | page_id + page_access_token | oauth-meta.test.ts tests 5-7 + manual round-trip in Task 2 | ✅/❌ |
| SETTINGS-07 | Weekly Meta refresh job | meta-refresh.test.ts + startup log line | ✅/❌ |
| SETTINGS-08 | TikTok greyed out | grep "Pending API approval" in SettingsPage.tsx + manual UI | ✅/❌ |
| SETTINGS-09 | Disconnect | settings.test.ts test 6 + manual UI Disconnect button | ✅/❌ |
| SETTINGS-10 | PKT timezone | settings.test.ts response shape + SettingsPage.tsx grep | ✅/❌ |

## Cross-user RLS spot check

- User A saved api_key X via PATCH. User B's GET /api/settings response does NOT contain X (manual check using two real Supabase test users).

## Known issues / deviations

&lt;copy any items flagged in 02-06-SUMMARY's "KNOWN ISSUE" section, e.g. redirect: 'manual' apiFetch tweak; record resolution path&gt;

## Outcome

- ✅ Phase 2 ready for `/gsd-verify-work 2`, OR
- ❌ Gaps identified — invoke `/gsd-plan-phase 2 --gaps`
```

Commit the report:

```
git add .planning/phases/02-settings-social-oauth/02-VERIFICATION.md
git commit -m "docs(02-07): phase 2 automated + curl verification report"
```
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm test &amp;&amp; cd ../frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend &amp;&amp; npm test` exits 0 with at least 43 tests passing across encryption, oauth-state, settings, oauth-google, oauth-meta, meta-refresh
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0
    - `cd frontend &amp;&amp; npm run build` exits 0
    - File `.planning/phases/02-settings-social-oauth/02-VERIFICATION.md` exists and is committed
    - The verification file contains rows for ALL ten SETTINGS-NN requirements
    - The verification file documents the cross-user RLS spot check outcome
    - Backend startup log evidence (`pg-boss meta-token-refresh job registered`) is captured in the file
  </acceptance_criteria>
  <done>Automated suite is green, smoke curls match expected outputs, the verification report is committed. The next task is the manual OAuth round-trip checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — real OAuth round-trip across YouTube, Instagram, Facebook</name>
  <what-built>
    The full Phase 2 stack:
    - Crypto + state map + env validation (02-01)
    - Settings GET/PATCH/disconnect with encryption and masking (02-02)
    - YouTube OAuth via googleapis (02-03)
    - Instagram + Facebook OAuth (02-04)
    - Weekly Meta refresh pg-boss job (02-05)
    - SettingsPage frontend with screen switcher and OAuth-redirect handling (02-06)
    - Automated suite + curl smoke + verification report (Task 1 above)
  </what-built>
  <how-to-verify>
    Claude has automated everything that can be automated. The remaining verifications require the user's real Google Cloud and Meta accounts — these cannot be mocked. The human runs through the four scenarios below and pastes results.

    **Setup**
    1. Backend running: `cd backend &amp;&amp; npm run dev`
    2. Frontend running: `cd frontend &amp;&amp; npm run dev`
    3. Browser open at `http://localhost:5173`
    4. Logged in as your test user (Phase 1 admin or another seeded user)
    5. Navigate: Generator → click "Settings" → SettingsPage renders

    **Scenario 1 — YouTube OAuth (SETTINGS-04)**
    1. In Settings → Connections, click "Connect" next to YouTube.
    2. Browser navigates full-page to `accounts.google.com`. Approve the consent screen including BOTH scopes (`youtube.upload` + `youtube.readonly`).
    3. Browser returns to `http://localhost:5173/?screen=settings&connected=youtube`. URL is stripped after the page loads. A green "YouTube connected." banner is visible.
    4. The YouTube row now shows "✓ Connected" with a "Disconnect" button.
    5. (DB check, optional) In Supabase SQL editor: `SELECT platform_config -&gt; 'youtube' FROM settings WHERE user_id = '&lt;your-uid&gt;';` — `access_token` and `refresh_token` are base64 strings (NOT recognisable plaintext).
    6. Click "Disconnect" — row reverts to "Connect" button. DB row's `platform_config -&gt; 'youtube'` is now `null`.

    **Scenario 2 — Instagram OAuth (SETTINGS-05)**
    1. Ensure your test Instagram account is set to Business or Creator (Pitfall 4 — Personal accounts are rejected).
    2. Click "Connect" next to Instagram.
    3. Browser navigates to `api.instagram.com/oauth/authorize`. Approve.
    4. Browser returns to `?screen=settings&connected=instagram`. Green banner. Row shows ✓ Connected.
    5. (DB check) `SELECT platform_config -&gt; 'instagram' FROM settings ...` — `access_token` is encrypted base64; `expiry` is roughly Date.now() + 60 days in ms.
    6. **Negative path:** disconnect, then try with a Personal Instagram account. Expected: red banner "Personal Instagram accounts cannot publish. Switch to Business or Creator and retry." No DB write.

    **Scenario 3 — Facebook OAuth (SETTINGS-06)**
    1. Ensure your Facebook user has at least one Facebook Page assigned (otherwise the no-Page path is exercised — see 3b).
    2. Click "Connect" next to Facebook.
    3. Approve at facebook.com (scope: pages_show_list, pages_manage_posts, pages_read_engagement).
    4. Browser returns to `?screen=settings&connected=facebook`. Green banner. Row shows ✓ Connected.
    5. (DB check) `platform_config -&gt; 'facebook'` contains `access_token` (encrypted page token), `page_id` (plain — safe), and `expiry`.

    **Scenario 3b — Facebook with no qualifying Page**
    1. Use a Facebook user with NO Page (or revoke Page admin permission).
    2. Click "Connect" → approve.
    3. Expected: yellow banner "Facebook connected, but no Page found. Create a Facebook Page to enable Reels uploads." DB has `platform_config -&gt; 'facebook' = { setup_required: true }`.

    **Scenario 4 — Cross-user RLS (AUTH-04 sanity)**
    1. Log out, log in as a different user (User B).
    2. GET /api/settings shows defaults — User A's saved API key and connections are NOT visible.
    3. Save a different API key as User B → confirm User B's mask appears.
    4. Log back in as User A → User A's original mask + connections are still visible.

    **Reporting**
    Paste:
    - For each scenario: ✅/❌ + any unexpected behaviour (URL contents, banner text, network panel error)
    - Screenshot of the SettingsPage with all three providers connected (or the issue you hit)
    - DB query output (from Supabase SQL editor) showing all three platform_config entries are non-null and the access_token strings are NOT plaintext

    If everything passes, type "phase 2 verified". Claude will update STATE.md and ROADMAP.md to mark Phase 2 complete and signal readiness for `/gsd-verify-work 2`.

    If anything fails (Meta app review pending, redirect URI mismatch, ENCRYPTION_KEY misconfigured), describe what you saw — Claude will scope a `/gsd-plan-phase 2 --gaps` follow-up.
  </how-to-verify>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md (updated only on success)</files>
  <action>This is a human-only verification step — Claude cannot execute real OAuth round-trips against the user's actual Google Cloud project and Meta app. The full four-scenario test plan is documented in <how-to-verify> above. Wait for the operator to complete and report results.</action>
  <verify>
    <automated>echo 'manual checkpoint — see how-to-verify'</automated>
  </verify>
  <done>The operator has typed "phase 2 verified" with attached evidence (banner screenshots + DB query output showing encrypted tokens), OR has typed "gap: ..." with a description of what failed. On success: STATE.md shows phase_complete; REQUIREMENTS.md SETTINGS-01..10 are ticked off.</done>
  <resume-signal>Type "phase 2 verified" when all four scenarios pass. If blocked, type "gap: &lt;short description&gt;" and Claude will plan the gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase test JWT → curl smoke | Real authentication; standard Bearer flow |
| Real OAuth provider → backend | The actual production trust boundary; this checkpoint is the only true coverage of T-02-02, T-02-05, T-02-06 in real conditions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-27 | Repudiation | Verification claims pass without real OAuth round-trip | mitigate | Task 2 is a blocking human checkpoint; Claude cannot self-attest scenarios 1-4 |
| T-02-28 | Information Disclosure | DB inspection step exposes encrypted tokens to the operator | accept | The operator already owns the database; encrypted blobs are useless without ENCRYPTION_KEY (still in their .env) |
</threat_model>

<verification>
- All 6 backend Vitest suites green
- Frontend builds clean
- Backend boots with both pg-boss job registrations logged
- All 5 curl smoke results match expected
- 02-VERIFICATION.md committed
- Human attests scenarios 1-4 pass (or files a gap report)
</verification>

<success_criteria>
- All 10 SETTINGS-NN requirements have automated AND manual evidence in 02-VERIFICATION.md
- Real OAuth round-trip succeeds for at least YouTube + Instagram + Facebook (or each is documented blocked-on-app-review with explicit user acknowledgement)
- Cross-user RLS spot check shows User B cannot see User A's settings
</success_criteria>

<output>
After human resume signal:
- If "phase 2 verified": update `.planning/STATE.md` (current_phase to "Phase 2 complete — ready for /gsd-verify-work 2", status to phase_complete, completed_phases++) and tick off SETTINGS-01..10 in REQUIREMENTS.md (`- [ ]` → `- [x]`). Commit with message `chore(state): phase 2 verified — ready for /gsd-verify-work`.
- If "gap: ...": write a brief `.planning/phases/02-settings-social-oauth/02-GAPS.md` capturing the failing scenario(s) and recommend `/gsd-plan-phase 2 --gaps`.
</output>
