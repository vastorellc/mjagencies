---
phase: 10-polish-resilience
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified: []
autonomous: false
requirements:
  - SC-01
  - SC-02
  - SC-03
  - SC-04
  - SC-05
  - SC-06
  - SC-07
  - SC-08
  - SC-09
  - SC-10

must_haves:
  truths:
    - "Full frontend test suite passes (all plans' tests green)"
    - "tsc clean on both frontend and backend"
    - "All SC-01 through SC-10 structural grep checks pass"
    - "Frontend build exits 0"
    - "Human confirms retry button behaviour, iOS safe-area, and error boundary recovery"
  artifacts:
    - path: "frontend/src/lib/ai.ts"
      provides: "parseProviderError exported and tested"
      contains: "export function parseProviderError"
    - path: "frontend/src/components/ErrorBoundary.tsx"
      provides: "Wraps generator, research, admin in App.tsx"
      contains: "getDerivedStateFromError"
    - path: "frontend/vite.config.ts"
      provides: "optimizeDeps.exclude present"
      contains: "optimizeDeps"
    - path: "frontend/src/App.tsx"
      provides: "safe-area on fixed nav + ErrorBoundary wraps"
      contains: "pb-[env(safe-area-inset-bottom)]"
  key_links:
    - from: "All Phase 10 plans"
      to: "ROADMAP success criteria SC-1 through SC-7"
      via: "Manual and automated verification in this checkpoint"
      pattern: "SC-"
---

<objective>
Phase 10 verification checkpoint — run all automated checks, confirm structural greps, human-verify UX behaviours.

Purpose: Gate the final phase of the platform against all seven ROADMAP success criteria. Plans 10-01 through 10-03 are automated; this plan verifies their combined output and captures any issues for gap closure.
Output: All SC-01 through SC-10 checks pass; human approves the session; VALIDATION.md updated to reflect final status; Phase 10 marked complete in ROADMAP and STATE.
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-resilience/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated checks — full test suite, tsc, build, structural greps</name>
  <read_first>
    - .planning/phases/10-polish-resilience/10-VALIDATION.md (confirm the full verification map before running)
  </read_first>
  <files></files>
  <action>
Run each check in sequence. Record pass/fail against the SC-ID. Stop if any check fails — fix before continuing.

**Frontend test suite (covers SC-01 through SC-07):**
```bash
cd /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend && npm run test:run 2>&1 | tail -20
```
Expected: all tests pass, zero failures.

**Frontend tsc:**
```bash
cd /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend && npx tsc --noEmit 2>&1
```
Expected: no output (exit 0).

**Backend tsc:**
```bash
cd /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend && npx tsc --noEmit 2>&1
```
Expected: no output (exit 0).

**Frontend build:**
```bash
cd /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend && npm run build 2>&1 | tail -10
```
Expected: exits 0.

**SC-08: h-[100dvh] on all 6 page components:**
```bash
grep -r "h-\[100dvh\]" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend/src/pages/ | wc -l
```
Expected: 6.

**SC-09: pb-[env(safe-area-inset-bottom)] on fixed nav in App.tsx:**
```bash
grep "pb-\[env(safe-area-inset-bottom)\]" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend/src/App.tsx
```
Expected: at least one match.

**SC-09: pb-[env(safe-area-inset-bottom)] on ScheduleModal inner card:**
```bash
grep "pb-\[env(safe-area-inset-bottom)\]" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend/src/components/ScheduleModal.tsx
```
Expected: at least one match.

**SC-10: optimizeDeps in vite.config.ts:**
```bash
grep "optimizeDeps" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend/vite.config.ts
```
Expected: at least one match.

**parseProviderError export verification:**
```bash
grep "export function parseProviderError" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend/src/lib/ai.ts
```
Expected: one match.

**ErrorBoundary in App.tsx (all three screens):**
```bash
grep "screenName=" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend/src/App.tsx
```
Expected: three matches (generator, research, admin).

**oauth_expired in upload workers:**
```bash
grep "oauth_expired" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-youtube.ts && grep "oauth_expired" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-instagram.ts && grep "oauth_expired" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/backend/src/lib/upload-facebook.ts
```
Expected: matches in all three files.

If any check fails, do NOT proceed to the checkpoint. Fix the failing check and re-run the full suite.
  </action>
  <acceptance_criteria>
    - cd frontend && npm run test:run exits 0 with zero test failures
    - cd frontend && npx tsc --noEmit exits 0
    - cd backend && npx tsc --noEmit exits 0
    - cd frontend && npm run build exits 0
    - grep -r "h-\[100dvh\]" frontend/src/pages/ | wc -l equals 6
    - grep "pb-\[env" frontend/src/App.tsx returns a match
    - grep "pb-\[env" frontend/src/components/ScheduleModal.tsx returns a match
    - grep "optimizeDeps" frontend/vite.config.ts returns a match
    - grep "export function parseProviderError" frontend/src/lib/ai.ts returns a match
    - grep "screenName=" frontend/src/App.tsx returns 3 matches
    - grep "oauth_expired" backend/src/lib/upload-youtube.ts returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/frontend && npm run test:run 2>&1 | tail -5</automated>
  </verify>
  <done>All automated checks pass. Full suite green. Both tsc clean. Build exits 0. All structural greps match.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 10 automated tasks complete:
- Plan 10-01: parseProviderError() added to ai.ts; GeneratorPage catch block uses it; model_busy added to RETRYABLE_ERRORS; upload workers emit oauth_expired:platform on token failure; Realtime handler detects the prefix; handleScheduleConfirm catch detects the prefix.
- Plan 10-02: ErrorBoundary class component created; Generator, Research, Admin screens wrapped in App.tsx; SC-05/SC-06 tests pass.
- Plan 10-03: pb-[env(safe-area-inset-bottom)] added to App.tsx fixed nav cluster and ScheduleModal inner card; vite.config.ts has optimizeDeps.exclude for ffmpeg packages.
  </what-built>
  <how-to-verify>
**1. Error retry button behaviour (SC-01/SC-02 — ROADMAP SC-2):**
- Open the app in browser: http://localhost:5173
- Go to Settings, enter an invalid API key, save.
- On the Generator screen, try to generate copy.
- Expected: "API key rejected" error message appears. NO retry button visible (invalid_key is not retryable).
- Now disconnect from the internet (disable Wi-Fi / toggle airplane mode briefly).
- Try to generate again.
- Expected: "Network error. Check your connection and retry." message + Retry button IS visible.

**2. Error boundary recovery (SC-05/SC-06 — ROADMAP SC-6):**
- In the browser DevTools console, run this to simulate a crash:
  ```javascript
  // Can't force a React render error from console, so verify via test run output instead
  ```
- Verify via the test suite output from Task 1 — SC-05 and SC-06 should show as PASS.
- Alternatively: temporarily break a component to trigger the boundary and confirm the "Reload" button appears, then revert.

**3. iOS safe-area (SC-09 — ROADMAP SC-5):**
- Open Chrome DevTools → Device emulation → select iPhone 14 Pro (or similar with home indicator).
- Check that the Research/Admin floating buttons in the bottom-right are NOT obscured by the home indicator bar.
- Check that the ScheduleModal Confirm button is not obscured when opened in iPhone emulation.

**4. OAuth expiry surfacing (ROADMAP SC-3):**
- Structural verification only (no live credentials needed): confirm grep passes from Task 1 output — oauth_expired in all three upload workers and Realtime handler in GeneratorPage.

**5. Platform completeness check:**
- Confirm the full test count from `npm run test:run` — should be higher than 206 (Phase 5's count) due to new Phase 10 tests.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 10 complete, or describe any issues found.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md, ROADMAP.md, and VALIDATION.md to mark Phase 10 complete</name>
  <read_first>
    - .planning/STATE.md (read full file — know exact current_phase and phase status table)
    - .planning/ROADMAP.md (Phase 10 section — Plans: TBD line)
    - .planning/phases/10-polish-resilience/10-VALIDATION.md (status column — all ⬜ pending)
  </read_first>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/10-polish-resilience/10-VALIDATION.md</files>
  <action>
This task runs ONLY after the human checkpoint is approved.

**Update STATE.md:**
- `current_phase` → "Phase 10 (Polish + Resilience) — complete"
- `status` → "complete"
- `stopped_at` → "Phase 10 complete — all SC checks passed, human checkpoint approved YYYY-MM-DD."
- `last_updated` → today's date
- `completed_phases` → 10
- `completed_plans` → 64
- `percent` → 100
- Phase 10 row in the phase table → "✅ Complete (4/4 plans, all SC-01–SC-10 passed, human checkpoint approved)"

**Update ROADMAP.md Phase 10 section:**
- `**Plans:** TBD` → `**Plans:** 4 plans`
- Add Plans list:
  ```
  Plans:
  - [x] 10-01-PLAN.md — parseProviderError + oauth_expired surfacing
  - [x] 10-02-PLAN.md — ErrorBoundary component + screen wrapping
  - [x] 10-03-PLAN.md — iOS safe-area fixes + Vite optimizeDeps
  - [x] 10-04-PLAN.md — Phase verification checkpoint
  ```
- `[ ] **Phase 10: Polish + Resilience**` → `[x] **Phase 10: Polish + Resilience**`

**Update VALIDATION.md:**
- All SC-01 through SC-10 status column → ✅ green
- `nyquist_compliant: true`
- `wave_0_complete: true`
  </action>
  <acceptance_criteria>
    - grep "current_phase" .planning/STATE.md shows "complete"
    - grep "percent" .planning/STATE.md shows 100
    - grep "Phase 10" .planning/ROADMAP.md shows "[x]" (checked off)
    - grep "4 plans" .planning/ROADMAP.md returns a match in Phase 10 section
  </acceptance_criteria>
  <verify>
    <automated>grep "current_phase" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/.planning/STATE.md</automated>
  </verify>
  <done>STATE.md, ROADMAP.md, and VALIDATION.md updated to reflect Phase 10 complete. Project at 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Verification-only plan — no new code written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | STATE.md / ROADMAP.md updates | accept | Documentation updates only; no production code; human checkpoint gates the update |
</threat_model>

<verification>
All prior plan checks from Task 1 plus human approval gate.

Final state verification:
```bash
grep "current_phase" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/.planning/STATE.md
grep "percent" /c/Users/jamshaid_pph/ClaudeMJ/viral-copy-generator/.planning/STATE.md
```
</verification>

<success_criteria>
- All automated checks from Task 1 pass (suite green, both tsc clean, build exits 0, all greps match)
- Human checkpoint approved
- STATE.md reflects Phase 10 complete, progress 100%
- ROADMAP.md Phase 10 checked off with 4 plans listed
- VALIDATION.md all SC statuses updated to green
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-resilience/10-04-SUMMARY.md`
</output>
