---
phase: 07-history-learning-loops
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - frontend/src/lib/types.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - HISTORY-01
  - HISTORY-02
  - HISTORY-03
  - HISTORY-04
  - HISTORY-05
  - HISTORY-06
  - LEARNING-06
  - LEARNING-07
  - LEARNING-09

must_haves:
  truths:
    - "Screen type includes 'history' and 'learning' — no TypeScript error when setting currentScreen"
    - "PostWithPlatforms type matches the response shape from GET /api/posts (Plan 01)"
    - "fetchPosts(), deletePost(), logActualViews() are exported from api.ts"
    - "fetchTopHooks() and fetchTopHashtags() are exported from api.ts — used by GeneratorPage injection"
    - "fetchLearningWeights() is exported from api.ts — used by LearningPage and ScorePanel badge"
    - "All api.ts functions use apiFetch() and throw on non-OK responses"
  artifacts:
    - path: "frontend/src/lib/types.ts"
      provides: "Phase 7 types: Screen extension, PostWithPlatforms, LearningData, etc."
      contains: "history.*learning"
    - path: "frontend/src/lib/api.ts"
      provides: "7 new API client functions for history and learning"
      contains: "fetchPosts"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "backend/src/routes/posts.ts"
      via: "apiFetch('/posts') in fetchPosts()"
      pattern: "apiFetch.*posts"
    - from: "frontend/src/lib/api.ts"
      to: "backend/src/routes/learning.ts"
      via: "apiFetch('/learning/hooks') in fetchTopHooks()"
      pattern: "apiFetch.*learning"
---

<objective>
Extend types.ts with all Phase 7 TypeScript types (Screen extension, post + platform_post
response shapes, learning data shapes) and add the API client wrappers to api.ts.

Purpose: Provides the typed contracts that HistoryPage, LearningPage, and the GeneratorPage
learning injection all compile against — ensuring no runtime shape mismatches.

Output: frontend/src/lib/types.ts (extended), frontend/src/lib/api.ts (extended).
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current Screen type in frontend/src/lib/types.ts (line 1) -->
```typescript
export type Screen = 'generator' | 'settings'
// Must extend to: 'generator' | 'settings' | 'history' | 'learning'
```

<!-- Current api.ts exports (full list) -->
```typescript
export async function apiFetch(path: string, init?: RequestInit): Promise<Response>
export async function proxyAIGenerate(body: AIProxyBody): Promise<{ text: string }>
export async function fetchSettings(): Promise<SettingsResponse>
export async function createPost(body: CreatePostBody): Promise<PostSaveResponse>
export async function fetchApiKey(): Promise<{ api_key: string | null }>
export async function uploadFile(file: File): Promise<UploadFileResponse>
export async function scheduleUpload(body: ScheduleUploadBody): Promise<ScheduleUploadResponse>
export async function fetchPeakTimes(platform: string): Promise<string[]>
```

<!-- Backend response shapes from Plan 01 + 02 -->
// GET /api/posts → { posts: PostWithPlatforms[] }
// PostWithPlatforms = post row + platforms: PlatformPostRow[]
// DELETE /api/posts/:id → { ok: true }
// POST /api/platform-posts/:id/views → { ok: true, accuracy: 'overperformed'|'matched'|'underperformed' }
// GET /api/learning/hooks → { hooks: { hook_text: string; max_views: number }[] }
// GET /api/learning/hashtags → { hashtags: { hashtag: string; avg_views: number }[] }
// GET /api/learning/posting-times → { times: { dow: number; hour: number; platform: string; avg_views: number; post_count: number }[] }
// GET /api/learning/niche-performance → { niches: { niche: string; avg_views: number; max_views: number; total_posts: number }[] }
// GET /api/learning/weights → { learned_weights: Record<string,number>|null; data_points: number; is_calibrated: boolean }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types.ts with all Phase 7 types</name>
  <files>frontend/src/lib/types.ts</files>
  <action>
Add the following Phase 7 type block at the end of frontend/src/lib/types.ts.
Do NOT modify any existing types — only append.

```typescript
// ============================================================================
// Phase 7: History + Learning Loops
// ============================================================================

// Screen extended to include history and learning screens
// App.tsx currentScreen state uses this type — extend here so both App.tsx and
// all page components compile against the same union.
export type Screen = 'generator' | 'settings' | 'history' | 'learning'

// PlatformPostRow — matches platform_posts DB row shape returned by GET /api/posts
export interface PlatformPostRow {
  id: string
  user_id: string
  post_id: string
  platform: string
  upload_status: string
  platform_post_id: string | null
  actual_views: number | null
  predicted_low: number | null
  predicted_high: number | null
  error_message: string | null
  posted_at: string | null    // ISO-8601 string (serialized from DB timestamp)
  created_at: string
}

// PostWithPlatforms — shape returned by GET /api/posts
export interface PostWithPlatforms {
  id: string
  user_id: string
  title: string
  niche: string
  virality_score: number
  engine_signals: Record<string, unknown>
  ai_output: Record<string, unknown>
  description: string | null
  created_at: string           // ISO-8601 string
  updated_at: string
  platforms: PlatformPostRow[]
}

// Accuracy label — returned by POST /api/platform-posts/:id/views and
// derived from PlatformPostRow.actual_views vs predicted_low/predicted_high
export type AccuracyLabel = 'overperformed' | 'matched' | 'underperformed'

// LogViewsResponse — from POST /api/platform-posts/:id/views
export interface LogViewsResponse {
  ok: boolean
  accuracy: AccuracyLabel
}

// TopHook — from GET /api/learning/hooks
export interface TopHook {
  hook_text: string
  max_views: number
}

// TopHashtag — from GET /api/learning/hashtags
export interface TopHashtag {
  hashtag: string
  avg_views: number
}

// PostingTimeSlot — from GET /api/learning/posting-times
export interface PostingTimeSlot {
  dow: number          // 0=Sunday … 6=Saturday
  hour: number         // 0–23, PKT (UTC+5)
  platform: string
  avg_views: number
  post_count: number
}

// NichePerformance — from GET /api/learning/niche-performance
export interface NichePerformance {
  niche: string
  avg_views: number
  max_views: number
  total_posts: number
}

// LearningWeightsResponse — from GET /api/learning/weights
export interface LearningWeightsResponse {
  learned_weights: Record<string, number> | null
  data_points: number
  is_calibrated: boolean
}

// LearningData — passed into buildPrompt() for LEARNING-06 injection
export interface LearningData {
  topHooks: TopHook[]
  topHashtags: TopHashtag[]
}

// PostFilters — used by HistoryPage to build GET /api/posts query string
export interface PostFilters {
  platform?: string
  niche?: string
  from?: string    // YYYY-MM-DD
  to?: string      // YYYY-MM-DD
}
```

NOTE: `Screen` is currently defined on line 1 of types.ts as:
  `export type Screen = 'generator' | 'settings'`

Replace that line with:
  `export type Screen = 'generator' | 'settings' | 'history' | 'learning'`

All other Phase 7 types are appended at the end.
  </action>
  <verify>
    <automated>cd "C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Screen type includes 'history' | 'learning' (grep: "history.*learning" in types.ts)
    - PostWithPlatforms interface exported
    - LearningData interface exported (consumed by GeneratorPage in Plan 04)
    - PostFilters interface exported (consumed by HistoryPage in Plan 04)
    - TypeScript compiles clean — no errors in types.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Add history and learning API client functions to api.ts</name>
  <files>frontend/src/lib/api.ts</files>
  <action>
Append the following Phase 7 section at the end of frontend/src/lib/api.ts.
Do NOT modify any existing functions — only append.

```typescript
// ============================================================================
// Phase 7: History + Learning Loops
// ============================================================================
import type {
  PostWithPlatforms,
  LogViewsResponse,
  TopHook,
  TopHashtag,
  PostingTimeSlot,
  NichePerformance,
  LearningWeightsResponse,
  PostFilters,
} from './types'

/**
 * GET /api/posts — returns authenticated user's posts, newest-first.
 * Filters are optional; platform filter uses EXISTS subquery server-side.
 */
export async function fetchPosts(filters: PostFilters = {}): Promise<PostWithPlatforms[]> {
  const params = new URLSearchParams()
  if (filters.platform) params.set('platform', filters.platform)
  if (filters.niche)    params.set('niche', filters.niche)
  if (filters.from)     params.set('from', filters.from)
  if (filters.to)       params.set('to', filters.to)

  const qs = params.size > 0 ? `?${params.toString()}` : ''
  const res = await apiFetch(`/posts${qs}`)
  if (!res.ok) throw new Error('posts_fetch_failed')
  const json = await res.json() as { posts: PostWithPlatforms[] }
  return json.posts
}

/**
 * DELETE /api/posts/:id — deletes post, cascades to platform_posts and learning_signals.
 */
export async function deletePost(postId: string): Promise<void> {
  const res = await apiFetch(`/posts/${encodeURIComponent(postId)}`, { method: 'DELETE' })
  if (!res.ok) throw new Error('post_delete_failed')
}

/**
 * POST /api/platform-posts/:platformPostId/views — logs actual views.
 * Returns accuracy label for inline display.
 * HISTORY-04, HISTORY-05, LEARNING-08: all 4 DB writes execute atomically server-side.
 */
export async function logActualViews(
  platformPostId: string,
  actualViews: number,
): Promise<LogViewsResponse> {
  const res = await apiFetch(`/platform-posts/${encodeURIComponent(platformPostId)}/views`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ actualViews }),
  })
  if (!res.ok) throw new Error('log_views_failed')
  return res.json() as Promise<LogViewsResponse>
}

/**
 * GET /api/learning/hooks?niche=...
 * Returns top 5 hooks by MAX(actual_views) — LEARNING-01.
 * Called fresh before every AI generation — no caching (LEARNING-06).
 */
export async function fetchTopHooks(niche?: string): Promise<TopHook[]> {
  const qs = niche ? `?niche=${encodeURIComponent(niche)}` : ''
  const res = await apiFetch(`/learning/hooks${qs}`)
  if (!res.ok) return []    // fail-open: learning data is non-blocking
  const json = await res.json() as { hooks: TopHook[] }
  return json.hooks ?? []
}

/**
 * GET /api/learning/hashtags?niche=...&platform=...
 * Returns top 10 hashtags by avg_views using unnest() aggregation — LEARNING-02.
 * Called fresh before every AI generation — no caching (LEARNING-06).
 */
export async function fetchTopHashtags(niche?: string, platform?: string): Promise<TopHashtag[]> {
  const params = new URLSearchParams()
  if (niche)    params.set('niche', niche)
  if (platform) params.set('platform', platform)
  const qs = params.size > 0 ? `?${params.toString()}` : ''
  const res = await apiFetch(`/learning/hashtags${qs}`)
  if (!res.ok) return []    // fail-open
  const json = await res.json() as { hashtags: TopHashtag[] }
  return json.hashtags ?? []
}

/**
 * GET /api/learning/posting-times?platform=...
 * Returns best PKT posting times — LEARNING-04.
 */
export async function fetchPostingTimes(platform?: string): Promise<PostingTimeSlot[]> {
  const qs = platform ? `?platform=${encodeURIComponent(platform)}` : ''
  const res = await apiFetch(`/learning/posting-times${qs}`)
  if (!res.ok) return []
  const json = await res.json() as { times: PostingTimeSlot[] }
  return json.times ?? []
}

/**
 * GET /api/learning/niche-performance
 * Returns niche breakdown — LEARNING-05.
 */
export async function fetchNichePerformance(): Promise<NichePerformance[]> {
  const res = await apiFetch('/learning/niche-performance')
  if (!res.ok) return []
  const json = await res.json() as { niches: NichePerformance[] }
  return json.niches ?? []
}

/**
 * GET /api/learning/weights
 * Returns learned_weights, data_points, is_calibrated — LEARNING-09.
 */
export async function fetchLearningWeights(): Promise<LearningWeightsResponse> {
  const res = await apiFetch('/learning/weights')
  if (!res.ok) return { learned_weights: null, data_points: 0, is_calibrated: false }
  return res.json() as Promise<LearningWeightsResponse>
}
```

IMPORTANT: The import block for Phase 7 types must be added at the top of the appended
section. The existing import at the top of api.ts imports from './types' — add the new
type names to that existing import statement instead of creating a second import from
the same module. Specifically, extend line 2 of api.ts:

```typescript
// Before:
import type { AIProxyBody, SettingsResponse, CreatePostBody, PostSaveResponse, UploadFileResponse, ScheduleUploadBody, ScheduleUploadResponse } from './types'

// After (add Phase 7 types):
import type {
  AIProxyBody, SettingsResponse, CreatePostBody, PostSaveResponse,
  UploadFileResponse, ScheduleUploadBody, ScheduleUploadResponse,
  PostWithPlatforms, LogViewsResponse, TopHook, TopHashtag,
  PostingTimeSlot, NichePerformance, LearningWeightsResponse, PostFilters,
} from './types'
```

Then the appended section has no local import statement — it uses the types already in scope.
  </action>
  <verify>
    <automated>cd "C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - fetchPosts() exported (grep: "export async function fetchPosts")
    - deletePost() exported
    - logActualViews() exported
    - fetchTopHooks() exported — used by GeneratorPage in Plan 04
    - fetchTopHashtags() exported — used by GeneratorPage in Plan 04
    - fetchPostingTimes() exported
    - fetchNichePerformance() exported
    - fetchLearningWeights() exported
    - All functions use apiFetch() internally
    - fetchTopHooks and fetchTopHashtags are fail-open (return [] on error, non-blocking)
    - TypeScript compiles clean — no errors in api.ts
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → backend API | All fetch calls go through apiFetch() which attaches Supabase JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Spoofing | apiFetch auth header | mitigate | apiFetch() calls supabase.auth.getSession() and injects Bearer token on every request; backend validates via authMiddleware |
| T-07-15 | Information Disclosure | fetchLearningWeights return shape | mitigate | Returns only is_calibrated + data_points + opaque weights object — no personal identifiers; weights are own-user data only |
| T-07-16 | Tampering | logActualViews actualViews param | mitigate | Backend validates as non-negative integer (Plan 01); client-side sends Number — no text injection possible |
| T-07-17 | Denial of Service | fetchTopHooks/fetchTopHashtags fail-open | accept | Return [] on error — non-blocking for AI generation; worst case: AI prompt has no learning injection, still functional |
</threat_model>

<verification>
```bash
# 1. TypeScript clean
cd C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend && npx tsc --noEmit

# 2. Screen type extended
grep -n "history.*learning" frontend/src/lib/types.ts

# 3. All 7 new api.ts functions exported
grep -n "export async function" frontend/src/lib/api.ts

# 4. fetchTopHooks is fail-open (returns [] on error)
grep -n "return \[\]" frontend/src/lib/api.ts

# 5. PostWithPlatforms interface exported
grep -n "export interface PostWithPlatforms" frontend/src/lib/types.ts

# 6. LearningData interface exported
grep -n "export interface LearningData" frontend/src/lib/types.ts
```
</verification>

<success_criteria>
- Screen type union includes 'history' | 'learning'
- PostWithPlatforms, PlatformPostRow, LearningData, PostFilters, and all learning response types exported from types.ts
- 7 new functions exported from api.ts: fetchPosts, deletePost, logActualViews, fetchTopHooks, fetchTopHashtags, fetchPostingTimes, fetchNichePerformance, fetchLearningWeights
- fetchTopHooks and fetchTopHashtags are fail-open (return [] on non-OK)
- All imports from './types' consolidated into a single import statement
- TypeScript clean — no errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-history-learning-loops/07-03-SUMMARY.md` using the
SUMMARY template at `@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md`.

Include: all new type names added, all new api.ts functions added, and the fail-open
decision for learning functions.
</output>
