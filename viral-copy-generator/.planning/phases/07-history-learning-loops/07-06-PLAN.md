---
phase: 07-history-learning-loops
plan: 06
type: execute
wave: 4
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
  - "07-04"
  - "07-05"
autonomous: false
requirements:
  - HISTORY-01
  - HISTORY-02
  - HISTORY-03
  - HISTORY-04
  - HISTORY-05
  - HISTORY-06
  - LEARNING-01
  - LEARNING-02
  - LEARNING-03
  - LEARNING-04
  - LEARNING-05
  - LEARNING-06
  - LEARNING-07
  - LEARNING-08
  - LEARNING-09

must_haves:
  truths:
    - "TypeScript compiles clean across both backend and frontend"
    - "All grep-based structural checks pass (EXISTS subquery, unnest, JSONB merge, inline style width)"
    - "History screen is reachable by clicking History nav button from GeneratorPage"
    - "Insights screen is reachable by clicking Insights nav button from GeneratorPage"
    - "Post history displays correctly — at least a loading state or empty state renders without JS error"
    - "View logging input renders per platform row"
    - "Bar charts in LearningPage use inline style width (confirmed by human visual check)"
    - "Generate Copy still works — learning injection does not break generation flow"
  artifacts:
    - path: "backend/src/routes/posts.ts"
      provides: "Full GET + DELETE — not stub"
      contains: "orderBy(desc(posts.created_at))"
    - path: "backend/src/routes/platformPosts.ts"
      provides: "Atomic view logging transaction"
      contains: "db.transaction"
    - path: "backend/src/routes/learning.ts"
      provides: "5 learning endpoints"
      contains: "unnest(hashtags)"
    - path: "frontend/src/pages/HistoryPage.tsx"
      provides: "History screen with filtering + view logging"
      contains: "logActualViews"
    - path: "frontend/src/pages/LearningPage.tsx"
      provides: "Insights screen with bar charts"
      contains: "style={{ width"
    - path: "frontend/src/lib/prompt.ts"
      provides: "buildPrompt with learning injection"
      contains: "LearningData"
    - path: "frontend/src/App.tsx"
      provides: "All 4 screens wired"
      contains: "HistoryPage.*LearningPage\|LearningPage.*HistoryPage"
  key_links:
    - from: "frontend HistoryPage"
      to: "backend GET /api/posts"
      via: "fetchPosts() in api.ts"
      pattern: "fetchPosts"
    - from: "frontend GeneratorPage"
      to: "backend GET /api/learning/hooks"
      via: "fetchTopHooks() called in handleGenerate()"
      pattern: "fetchTopHooks"
    - from: "frontend view logging"
      to: "backend POST /api/platform-posts/:id/views"
      via: "logActualViews() in api.ts"
      pattern: "logActualViews"
---

<objective>
Automated structural verification followed by a human visual checkpoint. Runs grep-based
checks to confirm all critical implementation rules are followed, then asks the developer
to verify History and Insights screens function correctly in the running app.

Purpose: Phase 7 is the learning loop — bugs here corrupt future AI generations and score
calibration permanently. Verification must be rigorous before marking complete.

Output: Confirmation that all automated checks pass and human sign-off on visual functionality.
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated structural verification — all Phase 7 rules</name>
  <files></files>
  <action>
Run all checks below. Fix any failures before proceeding. Report results.

```bash
# ── TypeScript ────────────────────────────────────────────────────────────────

# Backend TypeScript clean
cd C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend && npx tsc --noEmit
# Expected: no output (clean)

# Frontend TypeScript clean
cd C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend && npx tsc --noEmit
# Expected: no output (clean)

# ── Backend route structure ───────────────────────────────────────────────────

# 1. GET /api/posts uses EXISTS subquery for platform filter (not JOIN)
grep -n "EXISTS.*SELECT 1 FROM platform_posts" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\posts.ts
# Expected: 1 match

# 2. GET /api/posts uses orderBy(desc(posts.created_at))
grep -n "orderBy(desc(posts.created_at))" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\posts.ts
# Expected: 1 match

# 3. view logging uses db.transaction
grep -n "db.transaction" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\platformPosts.ts
# Expected: 1 match

# 4. JSONB merge operator present (not full column replacement)
grep -n "::jsonb" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\platformPosts.ts
# Expected: at least 1 match

# 5. EMA gate: dataPoints >= 10
grep -n "dataPoints >= 10" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\platformPosts.ts
# Expected: 1 match

# 6. unnest(hashtags) used in learning/hashtags route
grep -n "unnest(hashtags)" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\learning.ts
# Expected: 1 match

# 7. MAX(actual_views) used in hooks route (not AVG)
grep -n "MAX(actual_views)" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\learning.ts
# Expected: 1 match

# 8. PKT timezone in posting-times
grep -n "Asia/Karachi" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\learning.ts
# Expected: at least 2 matches (DOW and HOUR extracts)

# 9. HAVING COUNT >= 2 in posting-times
grep -n "HAVING COUNT" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\learning.ts
# Expected: 1 match

# 10. COALESCE for null niche
grep -n "COALESCE(niche" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\learning.ts
# Expected: 1 match

# 11. platformPostsRouter and learningRouter mounted in app.ts
grep -n "platformPostsRouter\|learningRouter" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\app.ts
# Expected: 2 matches (import + use)

# 12. userId never from req.body in posts or platformPosts routes
grep -rn "req.body.userId\|body.user_id\b" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\posts.ts C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\platformPosts.ts
# Expected: no matches

# ── Frontend structure ────────────────────────────────────────────────────────

# 13. Screen type includes history and learning
grep -n "history.*learning" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend\src\lib\types.ts
# Expected: 1 match

# 14. Bar charts use inline style (not dynamic Tailwind class)
grep -n "style={{ width" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend\src\pages\LearningPage.tsx
# Expected: at least 3 matches (hooks, hashtags, niches bar charts)

# 15. No dynamic Tailwind width classes in LearningPage
grep -n 'className.*w-\[' C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend\src\pages\LearningPage.tsx
# Expected: no matches

# 16. Learning injection in handleGenerate
grep -n "fetchTopHooks\|fetchTopHashtags" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend\src\pages\GeneratorPage.tsx
# Expected: at least 2 matches

# 17. App.tsx renders HistoryPage and LearningPage
grep -n "HistoryPage\|LearningPage" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend\src\App.tsx
# Expected: at least 4 matches (import + render for each)

# 18. buildPrompt accepts learningData param
grep -n "learningData" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend\src\lib\prompt.ts
# Expected: at least 2 matches (param + injection)

# 19. Frontend Vite build succeeds
cd C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend && npx vite build 2>&1 | tail -5
# Expected: "built in X.Xs" or similar success message

# 20. 5 learning endpoints exist
grep -n "learningRouter.get" C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend\src\routes\learning.ts | wc -l
# Expected: 5
```

If any check fails, diagnose and fix before the checkpoint task.
  </action>
  <verify>
    <automated>cd "C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend" && npx tsc --noEmit && cd "C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend" && npx tsc --noEmit && echo "BOTH CLEAN"</automated>
  </verify>
  <done>
    All 20 automated checks pass:
    - Backend TypeScript clean
    - Frontend TypeScript clean
    - EXISTS subquery confirmed for platform filter
    - db.transaction confirmed in platformPosts.ts
    - JSONB merge operator confirmed
    - EMA gate (>= 10) confirmed
    - unnest(hashtags) confirmed in learning routes
    - MAX(actual_views) (not AVG) confirmed for hooks
    - Asia/Karachi timezone confirmed in posting-times
    - HAVING COUNT >= 2 confirmed
    - COALESCE(niche, 'Other') confirmed
    - Both new routers mounted in app.ts
    - No userId from req.body
    - Inline style width used for bar charts (no dynamic Tailwind classes)
    - Vite build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 7 is complete: History screen lists posts with filters + inline view logging,
    Learning Insights shows top hooks/hashtags/posting times with bar charts, GeneratorPage
    injects learning data before every AI call, and App.tsx routes all 4 screens.
  </what-built>
  <how-to-verify>
    Start the app (if not already running):
    - Backend: `cd C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\backend && npm run dev`
    - Frontend: `cd C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator\frontend && npm run dev`
    - Visit http://localhost:5173 and log in.

    Verify these 8 behaviours:

    1. HISTORY SCREEN NAVIGATION
       - Click "History" button in GeneratorPage header
       - Expected: History screen loads with a list of posts (or empty state message)
       - Confirm: Platform icons, niche tag, virality score, date visible per post row

    2. HISTORY FILTERS
       - Select a platform from the dropdown → list reloads filtered
       - Select a niche → list reloads filtered
       - Set date range → list reloads filtered
       - Click "Clear filters" → full list returns

    3. VIEW LOGGING
       - Find any post with a platform row
       - Enter a number in the "Actual views" input → click "Log"
       - Expected: accuracy badge appears inline (Overperformed / Matched / Underperformed)
       - The badge color: green = Overperformed, neutral = Matched, red = Underperformed

    4. POST DELETE
       - Click Delete on any post → confirm dialog appears
       - Click OK → post disappears from list
       - Reload → post does not reappear (confirming server-side delete)

    5. LEARNING INSIGHTS SCREEN
       - Click "Insights" from GeneratorPage header OR from History screen
       - Expected: Insights screen loads
       - If no learning data yet: empty state message visible ("No learning data yet")
       - If data exists: hooks, hashtags, posting times, niche breakdown sections visible

    6. BAR CHARTS (if data visible)
       - Bar widths are proportional to view counts
       - Bars are not all the same width (confirms dynamic calculation)
       - Inspect element: width is set via `style` attribute (not class containing "w-[")

    7. CALIBRATED BADGE (if 10+ posts with views logged)
       - "Calibrated (N posts)" badge visible in Insights header
       - GeneratorPage header also shows it if ScorePanel receives live dataPoints

    8. GENERATE COPY (regression check)
       - Navigate back to GeneratorPage
       - Generate copy — confirm it still works end-to-end with no console errors
       - (Learning injection is silent — no visible change unless hooks are available)

    Report any screen that does not render, any JS console errors, or any behaviour that
    differs from the above.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 7 complete, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification task | Read-only grep + tsc; no write operations |
| human checkpoint | Developer validates their own running app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-28 | Tampering | Verification grep commands | accept | Commands are read-only pattern matches; no write operations; developer reviews output |
| T-07-29 | Information Disclosure | Dev server during verification | accept | Dev environment; no production data at risk during local verification |
</threat_model>

<verification>
```bash
# Summary check — run after Task 1 passes
cd C:\Users\jamshaid_pph\ClaudeMJ\viral-copy-generator

# Backend clean
cd backend && npx tsc --noEmit && echo "Backend: OK"

# Frontend clean
cd ..\frontend && npx tsc --noEmit && echo "Frontend: OK"

# Critical rule checks (quick summary)
grep -c "EXISTS.*SELECT 1 FROM platform_posts" backend\src\routes\posts.ts  # → 1
grep -c "db.transaction" backend\src\routes\platformPosts.ts                 # → 1
grep -c "unnest(hashtags)" backend\src\routes\learning.ts                   # → 1
grep -c "style={{ width" frontend\src\pages\LearningPage.tsx                # → 3+
grep -c 'className.*w-\[' frontend\src\pages\LearningPage.tsx               # → 0
```
</verification>

<success_criteria>
- All 20 automated checks pass without errors
- Human approves: History screen navigable, filters work, view logging produces accuracy badge, delete cascades, Insights screen renders, bar charts use inline style, Generate Copy regression passes
- Phase 7 STATE.md updated to: Phase 7 complete, Phase 8 (Admin Panel) ready to plan
</success_criteria>

<output>
After human approval, create `.planning/phases/07-history-learning-loops/07-06-SUMMARY.md` using the
SUMMARY template at `@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md`.

Update `.planning/STATE.md`:
- current_phase: Phase 8 (Admin Panel) — ready to plan
- Mark Phase 7 as complete in the Phase Status table
- Add Phase 7 to Completed Phases section with completion date

Include in summary: all checks that passed, human verification items confirmed, and
any issues found and fixed during verification.
</output>
