---
phase: 01-foundation-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - .nvmrc
  - .gitignore
  - .prettierrc
  - .editorconfig
  - CLAUDE.md
  - PROJECT.md
  - README.md
  - apps/web-main/package.json
  - apps/web-main/tsconfig.json
  - apps/web-main/next.config.mjs
  - apps/web-main/payload.config.ts
  - apps/web-main/postcss.config.mjs
  - apps/web-main/instrumentation.ts
  - apps/web-main/src/app/(frontend)/layout.tsx
  - apps/web-main/src/app/(frontend)/page.tsx
  - apps/web-main/src/app/(payload)/admin/[[...segments]]/page.tsx
  - apps/web-main/src/app/(payload)/admin/[[...segments]]/not-found.tsx
  - apps/web-main/src/app/(payload)/api/[...slug]/route.ts
  - apps/web-main/src/app/(payload)/api/graphql/route.ts
  - apps/web-main/src/app/(payload)/layout.tsx
  - apps/web-main/src/app/(payload)/custom.scss
  - apps/web-main/src/app/api/health/route.ts
  - apps/web-main/.size-limit.json
  - apps/web-ecommerce/**
  - apps/web-growth/**
  - apps/web-webdev/**
  - apps/web-ai/**
  - apps/web-branding/**
  - apps/web-strategy/**
  - apps/web-finance/**
  - apps/web-engineering/**
  - apps/web-product/**
  - apps/web-video/**
  - apps/web-graphic/**
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/src/index.ts
  - packages/ui/styles/theme.css
  - packages/ui/README.md
  - packages/db/package.json
  - packages/db/src/index.ts
  - packages/db/README.md
  - packages/config/package.json
  - packages/config/src/index.ts
  - packages/config/src/agency-constants.ts
  - packages/config/eslint/index.js
  - packages/config/tsconfig/base.json
  - packages/config/README.md
  - packages/auth/package.json
  - packages/auth/src/index.ts
  - packages/auth/README.md
  - packages/ai/package.json
  - packages/ai/README.md
  - packages/media/package.json
  - packages/media/README.md
  - packages/cms/package.json
  - packages/cms/README.md
  - packages/crm/package.json
  - packages/crm/README.md
  - packages/email/package.json
  - packages/email/README.md
  - packages/seo/package.json
  - packages/seo/README.md
  - packages/tools/package.json
  - packages/tools/README.md
  - packages/builder/package.json
  - packages/builder/README.md
  - packages/testing/package.json
  - packages/testing/vitest.config.ts
  - packages/testing/src/index.ts
  - packages/testing/src/fixtures/agency-fixture.ts
  - packages/testing/src/msw/handlers.ts
  - packages/testing/README.md
  - scripts/setup-hosts.sh
  - scripts/setup-hosts.ps1
  - docs/runbooks/local-dev.md
autonomous: true
requirements:
  - REQ-001
  - REQ-501
  - REQ-502
  - REQ-503
  - REQ-307
  - REQ-426
threat_refs:
  - T-01-001
  - T-01-002
  - T-01-003
  - T-01-005
user_setup: []

must_haves:
  truths:
    - "`pnpm install --frozen-lockfile` exits 0 from a clean clone"
    - "`pnpm dev --filter=@mjagency/web-main` starts without error and serves http://localhost:3000"
    - "Payload admin loads at http://localhost:3000/admin and shows the first-user wizard"
    - "All 13 apps (web-main + 12 agency apps) and all 13 packages exist with valid package.json + tsconfig.json"
    - "`pnpm list payload --depth=0` reports `payload 3.82.1` (exact, no `^`/`~`) in every app"
    - "`pnpm turbo run typecheck` exits 0 across the workspace"
    - "Subdomain hosts script appends 13 `.localhost` entries on a clean machine"
    - "Pino redact unit test passes — sensitive fields (password, email, token, phone, secret, apiKey, creditCard, ssn, refreshToken, accessToken, jti) emit `[REDACTED]`"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace enumeration"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Turborepo 2.x task graph (build/typecheck/lint/test/size-limit/dev)"
      contains: "\"tasks\""
    - path: ".nvmrc"
      provides: "Node 22 pin"
      contains: "22"
    - path: "tsconfig.base.json"
      provides: "Strict TS base config inherited by every app + package"
      contains: "\"strict\": true"
    - path: "apps/web-main/next.config.mjs"
      provides: "Next.js 15 + Payload via withPayload()"
      contains: "withPayload"
    - path: "apps/web-main/payload.config.ts"
      provides: "Payload 3.82.1 boot config (postgresAdapter + lexicalEditor)"
      contains: "buildConfig"
    - path: "apps/web-main/instrumentation.ts"
      provides: "Next.js 15 instrumentation hook (Edge-safe loader for Node SDK; full SDK lands in Plan 04)"
      contains: "NEXT_RUNTIME"
    - path: "packages/config/src/agency-constants.ts"
      provides: "12 agency slugs + Redis/BullMQ key prefix helpers (per CLAUDE.md §8)"
      contains: "agency:${agencyId}"
    - path: "packages/config/eslint/index.js"
      provides: "ESLint 9 flat config: next/core-web-vitals + ts-recommended + custom bans (jsonwebtoken, dangerouslyAllowSVG, NEXT_PUBLIC_*KEY)"
      contains: "no-restricted-imports"
    - path: "packages/testing/vitest.config.ts"
      provides: "Shared Vitest 2.x config consumed by every app/package"
      contains: "defineConfig"
    - path: "scripts/setup-hosts.sh"
      provides: "POSIX hosts installer for 13 *.localhost entries"
      contains: "ecommerce.localhost"
    - path: "scripts/setup-hosts.ps1"
      provides: "Windows hosts installer for 13 *.localhost entries"
      contains: "ecommerce.localhost"
    - path: "docs/runbooks/local-dev.md"
      provides: "Per-OS local dev runbook (macOS, Linux, Windows native, WSL2)"
      contains: "127.0.0.1 brand.localhost"
  key_links:
    - from: "apps/web-main/next.config.mjs"
      to: "@payloadcms/next/withPayload"
      via: "default export wrapped"
      pattern: "withPayload\\(nextConfig\\)"
    - from: "apps/web-*/package.json"
      to: "payload@3.82.1"
      via: "exact-version dependency"
      pattern: "\"payload\": \"3\\.82\\.1\""
    - from: "tsconfig.base.json"
      to: "every app/package tsconfig.json"
      via: "extends"
      pattern: "\"extends\": \"\\.\\.?/tsconfig\\.base\\.json\""
    - from: "packages/config/eslint/index.js"
      to: "every app/package eslint.config.js"
      via: "import"
      pattern: "from '@mjagency/config/eslint'"
---

<objective>
Stand up the Turborepo + pnpm workspace skeleton with Next.js 15 + Payload 3.82.1 inside `apps/web-main`, scaffold the remaining 11 agency apps and all 13 shared packages, and ship the dev-only quality-of-life pieces (hosts script, ESLint config, Pino logger, Vitest scaffold). Every other Phase 1 plan depends on this foundation.

Purpose: Plan 01-02..01-06 cannot run without a workspace, a working `web-main`, the locked tooling configs, and the Payload 3.82.1 pin. This is the substrate the rest of the phase scaffolds against.

Output: A clone-and-`pnpm install --frozen-lockfile` repository where `pnpm dev --filter=@mjagency/web-main` boots Next 15 + Payload admin without error, every package has TypeScript project references that compile clean, and the Pino logger's redact contract is verified by Vitest.
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-infra/01-CONTEXT.md
@.planning/phases/01-foundation-infra/01-RESEARCH.md
@.planning/phases/01-foundation-infra/01-VALIDATION.md
@mjagency/CLAUDE.md
@mjagency/PROJECT.md
@mjagency/specs/milestone-M001.md
@mjagency/specs/architecture.md
@mjagency/specs/cms.md
@mjagency/specs/gsd-config.md

<interfaces>
<!-- Contracts the executor must implement against. Sourced verbatim from RESEARCH.md §2.1, §2.2, §2.7, §2.13, §2.17 + CLAUDE.md §8. -->

Locked package scope:
```
@mjagency/web-main, @mjagency/web-ecommerce, @mjagency/web-growth, @mjagency/web-webdev,
@mjagency/web-ai, @mjagency/web-branding, @mjagency/web-strategy, @mjagency/web-finance,
@mjagency/web-engineering, @mjagency/web-product, @mjagency/web-video, @mjagency/web-graphic,
@mjagency/ui, @mjagency/db, @mjagency/config, @mjagency/auth, @mjagency/ai, @mjagency/media,
@mjagency/cms, @mjagency/crm, @mjagency/email, @mjagency/seo, @mjagency/tools, @mjagency/builder,
@mjagency/testing
```

Locked agency slug + port assignment (per RESEARCH §2.17 and §2.8):
```
brand        → port 3000 (web-main)
ecommerce    → port 3001
growth       → port 3002
webdev       → port 3003
ai           → port 3004
branding     → port 3005
strategy     → port 3006
finance      → port 3007
engineering  → port 3008
product      → port 3009
video        → port 3010
graphic      → port 3011
```

Locked Pino redact paths (D-12, RESEARCH §2.7):
```
'req.headers.authorization', 'req.headers.cookie', 'req.headers["x-api-key"]',
'req.headers["x-doppler-token"]', 'res.headers["set-cookie"]',
'*.password', '*.token', '*.secret', '*.apiKey', '*.api_key',
'*.email', '*.phone', '*.creditCard', '*.ssn',
'*.refreshToken', '*.accessToken', '*.jti',
'*.stripeKey', '*.stripeSecret', '*.r2AccessKey', '*.r2SecretKey',
'*.dopplerToken', '*.jwtSecret',
'*.payload.email', '*.payload.phone'
```

Agency-isolation key helpers (CLAUDE.md §8, RESEARCH pitfall 3.9):
```ts
export const REDIS_KEY = {
  cache:       (a: string, k: string) => `agency:${a}:cache:${k}`,
  session:     (a: string, u: string) => `agency:${a}:session:${u}`,
  bullPrefix:  (a: string)             => `agency:${a}:bull`,
  rateLimit:   (a: string, ip: string) => `agency:${a}:ratelimit:${ip}`,
} as const
```

Verified Turborepo 2.x `tasks` config skeleton (RESEARCH §2.1):
```json
{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local", "tsconfig.base.json"],
  "globalEnv": ["NODE_ENV", "AGENCY", "DOPPLER_TOKEN"],
  "tasks": {
    "build":       { "dependsOn": ["^build"], "outputs": [".next/**", "!.next/cache/**", "dist/**"], "env": ["DATABASE_URL", "PAYLOAD_SECRET", "JWT_SECRET"] },
    "typecheck":   { "dependsOn": ["^build"], "outputs": [] },
    "lint":        { "outputs": [] },
    "test":        { "dependsOn": ["^build"], "outputs": ["coverage/**"] },
    "size-limit":  { "dependsOn": ["build"], "outputs": [] },
    "dev":         { "cache": false, "persistent": true, "dependsOn": ["^build"] }
  }
}
```

Verified `next.config.mjs` skeleton (RESEARCH §2.2, code example §"Verified pattern: withPayload"):
```js
import { withPayload } from '@payloadcms/next/withPayload'
const nextConfig = {
  reactStrictMode: true,
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '*.cloudflare.com' },
      { protocol: 'https', hostname: 'imagedelivery.net' },
    ],
  },
  experimental: {
    serverComponentsExternalPackages: ['pino', 'pino-pretty', '@opentelemetry/sdk-node'],
  },
}
export default withPayload(nextConfig)
```

Verified `instrumentation.ts` Edge/Node split (RESEARCH §2.6, pitfall 3.6):
```ts
export async function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    await import('./instrumentation.node')
  }
}
```
(Plan 04 adds the Node SDK body in `instrumentation.node.ts`. Plan 01 ships the loader stub only.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Initialize Turborepo + pnpm workspaces + workspace tooling</name>
  <files>
    pnpm-workspace.yaml,
    turbo.json,
    package.json,
    tsconfig.base.json,
    .nvmrc,
    .gitignore,
    .prettierrc,
    .editorconfig,
    README.md,
    CLAUDE.md,
    PROJECT.md,
    packages/config/package.json,
    packages/config/tsconfig/base.json,
    packages/config/eslint/index.js,
    packages/config/src/index.ts,
    packages/config/src/agency-constants.ts,
    packages/config/src/logger.ts,
    packages/config/src/__tests__/logger.test.ts,
    packages/config/README.md,
    packages/testing/package.json,
    packages/testing/tsconfig.json,
    packages/testing/vitest.config.ts,
    packages/testing/src/index.ts,
    packages/testing/src/fixtures/agency-fixture.ts,
    packages/testing/src/msw/handlers.ts,
    packages/testing/README.md,
    packages/ui/package.json,
    packages/ui/tsconfig.json,
    packages/ui/src/index.ts,
    packages/ui/styles/theme.css,
    packages/ui/README.md,
    packages/db/package.json,
    packages/db/tsconfig.json,
    packages/db/src/index.ts,
    packages/db/README.md,
    packages/auth/package.json,
    packages/auth/tsconfig.json,
    packages/auth/src/index.ts,
    packages/auth/README.md,
    packages/ai/package.json,
    packages/ai/tsconfig.json,
    packages/ai/src/index.ts,
    packages/ai/README.md,
    packages/media/package.json,
    packages/media/tsconfig.json,
    packages/media/src/index.ts,
    packages/media/README.md,
    packages/cms/package.json,
    packages/cms/tsconfig.json,
    packages/cms/src/index.ts,
    packages/cms/README.md,
    packages/crm/package.json,
    packages/crm/tsconfig.json,
    packages/crm/src/index.ts,
    packages/crm/README.md,
    packages/email/package.json,
    packages/email/tsconfig.json,
    packages/email/src/index.ts,
    packages/email/README.md,
    packages/seo/package.json,
    packages/seo/tsconfig.json,
    packages/seo/src/index.ts,
    packages/seo/README.md,
    packages/tools/package.json,
    packages/tools/tsconfig.json,
    packages/tools/src/index.ts,
    packages/tools/README.md,
    packages/builder/package.json,
    packages/builder/tsconfig.json,
    packages/builder/src/index.ts,
    packages/builder/README.md,
    apps/.gitkeep
  </files>
  <read_first>
    - .gitignore (current root file — preserve all entries)
    - README.md (current — replace with project orientation)
    - mjagency/CLAUDE.md (full file — symlink target)
    - mjagency/PROJECT.md (full file — symlink target)
    - mjagency/specs/milestone-M001.md §SLICE 1 Task 1.1 (lines 19-25)
    - mjagency/specs/gsd-config.md (single CLAUDE.md / PROJECT.md mandate)
    - .planning/phases/01-foundation-infra/01-CONTEXT.md (D-14 ESLint preset, D-15 Node pin, D-17 packages list, D-18 npm scope)
    - .planning/phases/01-foundation-infra/01-RESEARCH.md §2.1 (turbo.json verbatim, lines 206-260), §2.7 (Pino redact verbatim, lines 628-712), §2.13 (ESLint flat config verbatim, lines 1018-1057), §2.17 (Repo Structure, lines 1174-1250)
    - .planning/phases/01-foundation-infra/01-VALIDATION.md §"Wave 0 Requirements" (the 16-item Wave-0 checklist)
  </read_first>
  <behavior>
    - `packages/config/src/__tests__/logger.test.ts` (TDD-FIRST): create the failing test BEFORE writing `logger.ts`. Test cases:
      - `createLogger({ service: 't', agencyId: 'brand' })` returns a Pino logger.
      - When the logger is called with `{ password: 'p' }`, the JSON output contains `"password":"[REDACTED]"`.
      - Same for `email`, `phone`, `token`, `secret`, `apiKey`, `creditCard`, `ssn`, `refreshToken`, `accessToken`, `jti`, `stripeKey`, `r2AccessKey`, `dopplerToken`, `jwtSecret`.
      - When called with `{ req: { headers: { authorization: 'Bearer x', cookie: 'a=b' } } }`, both header values redact to `[REDACTED]`.
      - The output line includes `"agency.id":"brand"` and `"service":"t"` from `base`.
      - `edgeLog('info', 'm', { foo: 'bar' })` emits a single JSON line on stdout (capture via `console.log` spy) with shape `{ level: 'info', msg: 'm', time: <iso>, foo: 'bar' }`.
    - All tests use Vitest 2.x. RED → GREEN → REFACTOR. Commit RED first, then GREEN.
  </behavior>
  <action>
    1. Generate root files exactly as specified.
       - `.nvmrc` content: `22\n` (no extras).
       - `pnpm-workspace.yaml`:
         ```yaml
         packages:
           - 'apps/*'
           - 'packages/*'
         ```
       - `turbo.json`: paste the verified Turborepo 2.x `tasks` block from `<interfaces>` above (RESEARCH §2.1) verbatim. Use `tasks` key (NOT `pipeline`).
       - `tsconfig.base.json`: TypeScript strict mode preset:
         ```json
         {
           "compilerOptions": {
             "target": "ES2022", "module": "ESNext", "moduleResolution": "Bundler",
             "strict": true, "noUncheckedIndexedAccess": true, "noImplicitOverride": true,
             "esModuleInterop": true, "skipLibCheck": true, "resolveJsonModule": true,
             "isolatedModules": true, "verbatimModuleSyntax": true,
             "jsx": "preserve", "incremental": true, "composite": false,
             "lib": ["ES2022", "DOM", "DOM.Iterable"]
           },
           "exclude": ["node_modules", "dist", ".next", "coverage"]
         }
         ```
       - `.prettierrc`: `{ "semi": false, "singleQuote": true, "trailingComma": "all", "printWidth": 100 }`
       - `.editorconfig`: standard 2-space indent / LF / utf-8 / trim-trailing-whitespace.
       - `.gitignore`: append (don't replace existing) — `node_modules`, `.turbo`, `.next`, `dist`, `coverage`, `.env*.local`, `.DS_Store`, `pnpm-debug.log`, `tsbuildinfo`, `.size-baseline.json` (committed manually).
       - Root `package.json` (private monorepo root):
         ```json
         {
           "name": "mjagency-monorepo",
           "private": true,
           "engines": { "node": ">=22.0.0", "pnpm": ">=10.0.0" },
           "packageManager": "pnpm@10.33.2",
           "scripts": {
             "build": "turbo run build",
             "dev": "turbo run dev",
             "test": "turbo run test",
             "lint": "turbo run lint",
             "typecheck": "turbo run typecheck",
             "size-limit": "turbo run size-limit",
             "format": "prettier --write .",
             "setup-hosts": "scripts/setup-hosts.sh"
           },
           "devDependencies": {
             "turbo": "2.9.6",
             "typescript": "5.6.3",
             "prettier": "3.3.3",
             "eslint": "9.13.0"
           }
         }
         ```
       - `README.md`: 1-paragraph project orientation (per content-complete rule — NO TODOs). Point at `mjagency/PROJECT.md`, `.planning/STATE.md`, `docs/runbooks/local-dev.md`.
       - `CLAUDE.md` and `PROJECT.md` at repo root: per `mjagency/specs/gsd-config.md`, the canonical copy lives in `mjagency/`. On POSIX systems use a symlink (`ln -s mjagency/CLAUDE.md CLAUDE.md`); on Windows / for portability **commit a copy** of each file content. Choose **copy** (not symlink) for cross-platform safety; document the parity expectation in `docs/runbooks/local-dev.md` and add a CI parity check in Plan 01-05.
    2. Scaffold all 13 packages under `packages/` with `package.json` + `tsconfig.json` + `src/index.ts` + `README.md`. Naming convention from `<interfaces>`. Each `package.json`:
       ```json
       {
         "name": "@mjagency/<name>",
         "version": "0.0.0",
         "private": true,
         "type": "module",
         "main": "./src/index.ts",
         "types": "./src/index.ts",
         "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
         "scripts": {
           "lint": "eslint . --max-warnings=0",
           "typecheck": "tsc --noEmit",
           "test": "vitest run --passWithNoTests"
         }
       }
       ```
       Each `tsconfig.json` extends `../../tsconfig.base.json`. `src/index.ts` exports the smallest real surface for that package (e.g. `packages/auth/src/index.ts` exports a single typed `auth` placeholder function with explicit `Promise<null>` return). README is a one-paragraph real description of what the package will hold and which milestone fills it (no "Coming soon" — per CLAUDE.md §5).
    3. **`packages/config`** is special — it is functional at M001:
       - `packages/config/src/agency-constants.ts`: export `AGENCIES` array (12 slugs in order: brand, ecommerce, growth, webdev, ai, branding, strategy, finance, engineering, product, video, graphic), `AGENCY_PORT_BASE = 3000`, and the `REDIS_KEY` helper from `<interfaces>` verbatim. Per CLAUDE.md §8 + pitfall 3.9.
       - `packages/config/src/logger.ts`: paste the `createLogger` + `edgeLog` + `REDACT_PATHS` block from RESEARCH §2.7 lines 632-678 verbatim (D-12 redact paths). Pino 10.3.1.
       - `packages/config/eslint/index.js`: paste RESEARCH §2.13 flat-config block verbatim (lines 1022-1053) — bans `jsonwebtoken` (REQ-502), `dangerouslyAllowSVG` (SEC-N4), `dangerouslySetInnerHTML` from non-sanitized sources, and `process.env.NEXT_PUBLIC_*KEY` access via `no-restricted-syntax` MemberExpression matcher. Per D-14.
       - `packages/config/tsconfig/base.json`: extends `../../../tsconfig.base.json`; this is the file every app/package indirectly inherits.
       - `packages/config/src/__tests__/logger.test.ts`: TDD-first test asserting the redact contract from `<behavior>`.
    4. **`packages/testing`** is functional at M001:
       - `packages/testing/vitest.config.ts`:
         ```ts
         import { defineConfig } from 'vitest/config'
         export default defineConfig({
           test: {
             environment: 'node',
             globals: true,
             coverage: { provider: 'v8', reporter: ['text', 'lcov'] },
             include: ['src/**/*.test.ts', '**/__tests__/**/*.test.ts'],
           },
         })
         ```
       - `packages/testing/src/fixtures/agency-fixture.ts`: export `TEST_AGENCIES` — array of 12 objects `{ slug, id (deterministic UUIDv4 per slug), port }`.
       - `packages/testing/src/msw/handlers.ts`: export `baseHandlers: HttpHandler[]` — empty array at M001 (Plan 03 adds Cloudflare Images handler, Plan 02 adds Stripe webhook handler at the route stub).
    5. Add `apps/.gitkeep` so the empty directory is committed; the agency apps come in Task 1.2 / 1.3.
    6. Run `pnpm install --frozen-lockfile=false` once to generate `pnpm-lock.yaml`. Commit the lockfile.
    7. Atomic commit per the M001 task ID convention: `feat(01-01): initialize turborepo + pnpm workspaces + 13 package scaffolds (Task 1.1)`.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm turbo run typecheck && pnpm turbo run test --filter=@mjagency/config && node -e "const w=require('js-yaml').load(require('fs').readFileSync('pnpm-workspace.yaml','utf8')); if(!w.packages.includes('apps/*')||!w.packages.includes('packages/*'))process.exit(1)" && grep -q '"tasks"' turbo.json && grep -q '\"strict\": true' tsconfig.base.json && [ "$(ls packages | wc -l)" -ge 13 ]</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` contains `apps/*` AND `packages/*`.
    - `turbo.json` contains `"tasks"` (NOT `"pipeline"`) and the 6 task names: `build`, `typecheck`, `lint`, `test`, `size-limit`, `dev`.
    - `.nvmrc` content is exactly `22` (with optional trailing newline).
    - `tsconfig.base.json` contains `"strict": true` AND `"noUncheckedIndexedAccess": true`.
    - `ls packages/ | wc -l` >= 13 — directories: `ui db config auth ai media cms crm email seo tools builder testing`.
    - For each `pkg` in the 13 packages: `packages/$pkg/package.json` contains `"@mjagency/$pkg"` and `"private": true`; `packages/$pkg/tsconfig.json` extends `../../tsconfig.base.json`; `packages/$pkg/src/index.ts` exists; `packages/$pkg/README.md` does NOT contain the strings `TODO`, `Coming soon`, `Lorem ipsum`, or `[insert]`.
    - `packages/config/src/agency-constants.ts` exports `AGENCIES` containing exactly: `brand,ecommerce,growth,webdev,ai,branding,strategy,finance,engineering,product,video,graphic`.
    - `packages/config/src/logger.ts` defines `REDACT_PATHS` array containing AT LEAST these strings: `*.password`, `*.token`, `*.secret`, `*.apiKey`, `*.email`, `*.phone`, `*.creditCard`, `*.ssn`, `*.refreshToken`, `*.accessToken`, `*.jti`, `*.stripeKey`, `*.r2AccessKey`, `*.dopplerToken`, `*.jwtSecret`, `req.headers.authorization`, `req.headers.cookie`.
    - `packages/config/eslint/index.js` references `no-restricted-imports` for `jsonwebtoken` AND a `no-restricted-syntax` selector containing the literal string `dangerouslyAllowSVG`.
    - `pnpm vitest run packages/config/src/__tests__/logger.test.ts` exits 0 with at least 14 passing assertions covering each redact path.
    - `pnpm install --frozen-lockfile` exits 0 from a clean clone (verified via `rm -rf node_modules && pnpm install --frozen-lockfile`).
    - `pnpm turbo run typecheck` exits 0 across all 13 packages.
    - `CLAUDE.md` and `PROJECT.md` exist at repo root and have content byte-for-byte equal to `mjagency/CLAUDE.md` and `mjagency/PROJECT.md` (CI parity check enforced in Plan 01-05).
    - No file in `packages/*/README.md`, `apps/`, or `scripts/` contains `TODO`, `Coming soon`, `Lorem ipsum`, or `[insert]` (REQ-200, CLAUDE.md §5).
  </acceptance_criteria>
  <done>Workspace boots from clean clone; 13 packages typecheck; logger redact contract green via Vitest; `pnpm-lock.yaml` committed; root config files match the M001 spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Base Next.js 15 + Payload 3.82.1 app (web-main)</name>
  <files>
    apps/web-main/package.json,
    apps/web-main/tsconfig.json,
    apps/web-main/next.config.mjs,
    apps/web-main/payload.config.ts,
    apps/web-main/postcss.config.mjs,
    apps/web-main/instrumentation.ts,
    apps/web-main/.size-limit.json,
    apps/web-main/eslint.config.js,
    apps/web-main/vitest.config.ts,
    apps/web-main/src/app/(frontend)/layout.tsx,
    apps/web-main/src/app/(frontend)/page.tsx,
    apps/web-main/src/app/(payload)/admin/[[...segments]]/page.tsx,
    apps/web-main/src/app/(payload)/admin/[[...segments]]/not-found.tsx,
    apps/web-main/src/app/(payload)/api/[...slug]/route.ts,
    apps/web-main/src/app/(payload)/api/graphql/route.ts,
    apps/web-main/src/app/(payload)/api/graphql-playground/route.ts,
    apps/web-main/src/app/(payload)/layout.tsx,
    apps/web-main/src/app/(payload)/custom.scss,
    apps/web-main/src/app/api/health/route.ts,
    apps/web-main/src/app/api/stripe/webhook/route.ts,
    apps/web-main/src/__tests__/health.test.ts,
    apps/web-main/public/robots.txt,
    pnpm-lock.yaml
  </files>
  <read_first>
    - apps/.gitkeep (confirm apps directory exists from Task 1.1)
    - turbo.json (confirm task contracts)
    - packages/config/src/index.ts (confirm exports available)
    - packages/config/src/logger.ts (used by health route)
    - mjagency/specs/milestone-M001.md §SLICE 1 Task 1.2 (lines 26-31)
    - mjagency/CLAUDE.md (full — Payload pin + jose-only + middleware matcher rules)
    - .planning/phases/01-foundation-infra/01-RESEARCH.md §2.2 (lines 261-350, complete `next.config.mjs` + `payload.config.ts` skeletons + boot verification gate), §2.6 (instrumentation.ts split), pitfall 3.1 (Payload 3.x routing fragility), pitfall 3.2 (middleware matcher), pitfall 3.4 (Pino in Edge), pitfall 3.6 (instrumentation Edge/Node split)
    - .planning/phases/01-foundation-infra/01-CONTEXT.md (D-13 trace_id injection deferral)
    - .planning/phases/01-foundation-infra/01-VALIDATION.md (Success Criterion #1, Wave-0 health.test.ts row)
  </read_first>
  <behavior>
    - `apps/web-main/src/__tests__/health.test.ts` (TDD-FIRST):
      - `GET /api/health` returns 200 with body `{ ok: true, service: 'mjagency-main', agency: 'main' }`.
      - The handler emits a Pino log line via `createLogger({ service, agencyId })` from `@mjagency/config` (assert via spy).
      - The handler runs on Node runtime (`export const runtime = 'nodejs'` is exported as a string equal to `'nodejs'`).
    - Test runs against the route handler imported directly (no full Next dev server needed); use `Request`/`Response` standard objects.
  </behavior>
  <action>
    1. Create `apps/web-main/package.json`:
       ```json
       {
         "name": "@mjagency/web-main",
         "version": "0.0.0",
         "private": true,
         "type": "module",
         "scripts": {
           "dev": "next dev -p 3000",
           "build": "next build",
           "start": "next start -p 3000",
           "lint": "next lint --max-warnings=0",
           "typecheck": "tsc --noEmit",
           "test": "vitest run",
           "size-limit": "size-limit"
         },
         "dependencies": {
           "next": "15.5.15",
           "react": "19.0.0",
           "react-dom": "19.0.0",
           "payload": "3.82.1",
           "@payloadcms/next": "3.82.1",
           "@payloadcms/db-postgres": "3.82.1",
           "@payloadcms/richtext-lexical": "3.82.1",
           "pino": "10.3.1",
           "@mjagency/config": "workspace:*",
           "@mjagency/ui": "workspace:*"
         },
         "devDependencies": {
           "@mjagency/testing": "workspace:*",
           "@types/node": "22.9.0",
           "@types/react": "19.0.0",
           "@types/react-dom": "19.0.0",
           "tailwindcss": "4.2.4",
           "@tailwindcss/postcss": "4.2.4",
           "typescript": "5.6.3",
           "size-limit": "11.1.6",
           "@size-limit/preset-app": "11.1.6",
           "vitest": "2.1.8",
           "eslint-config-next": "15.5.15"
         }
       }
       ```
       **CRITICAL:** `payload` MUST be `"3.82.1"` exactly — no `^`, no `~`. CI gate REQ-501 enforces. Same pin on `@payloadcms/next`, `@payloadcms/db-postgres`, `@payloadcms/richtext-lexical`.
    2. `apps/web-main/next.config.mjs`: paste the verified `withPayload` skeleton from `<interfaces>` above (RESEARCH §2.2 lines 293-314) verbatim. **Do NOT enable `dangerouslyAllowSVG`** anywhere (REQ-098, SEC-N4).
    3. `apps/web-main/payload.config.ts`: paste RESEARCH §2.2 lines 317-346 verbatim. Empty `collections: []` at M001 (Payload generates `users` automatically). `secret: process.env.PAYLOAD_SECRET || ''` — Doppler injects in Plan 01-06.
    4. `apps/web-main/tsconfig.json`:
       ```json
       {
         "extends": "../../tsconfig.base.json",
         "compilerOptions": {
           "plugins": [{ "name": "next" }],
           "paths": { "@/*": ["./src/*"] }
         },
         "include": ["next-env.d.ts", "src/**/*", "instrumentation.ts", "next.config.mjs", ".next/types/**/*"],
         "exclude": ["node_modules", ".next", "dist"]
       }
       ```
    5. `apps/web-main/instrumentation.ts`: paste the Edge-safe loader from `<interfaces>` above (RESEARCH §2.6, code-example block "Verified pattern: Next 15 instrumentation file split"). The full Node SDK body (`instrumentation.node.ts`) lands in Plan 01-04. Plan 01-02 must keep this file present so Next 15 detects the hook from day one.
    6. `apps/web-main/postcss.config.mjs`:
       ```js
       export default { plugins: { '@tailwindcss/postcss': {} } }
       ```
    7. App Router files:
       - `src/app/(frontend)/layout.tsx`: minimal HTML shell, imports `../../../packages/ui/styles/theme.css`. Real heading: "MJAgency Platform — Brand Hub" (no placeholder text).
       - `src/app/(frontend)/page.tsx`: real one-paragraph landing copy linking to `/admin`. NO `TODO`/`Coming soon`/`Lorem ipsum` (REQ-200, CLAUDE.md §5).
       - `src/app/(payload)/admin/[[...segments]]/page.tsx` and `not-found.tsx`: the canonical Payload-generated files. Generate by running `npx create-payload-app@3.82.1 --template blank --name _scratch_ --packageManager pnpm --skipInstall` in a temp dir, then copying the `(payload)` route group and `payload.config.ts` shape. Per pitfall 3.1 — DO NOT hand-edit.
       - `src/app/(payload)/api/[...slug]/route.ts` and `api/graphql/route.ts` and `api/graphql-playground/route.ts`: same — copied from create-payload-app output.
       - `src/app/(payload)/layout.tsx` and `custom.scss`: same.
    8. `src/app/api/health/route.ts`:
       ```ts
       import { NextResponse } from 'next/server'
       import { createLogger } from '@mjagency/config'

       export const runtime = 'nodejs'
       const log = createLogger({ service: 'mjagency-main', agencyId: process.env.AGENCY ?? 'main' })

       export async function GET() {
         log.info({ route: '/api/health' }, 'health check')
         return NextResponse.json({
           ok: true,
           service: `mjagency-${process.env.AGENCY ?? 'main'}`,
           agency: process.env.AGENCY ?? 'main',
         })
       }
       ```
    9. `src/app/api/stripe/webhook/route.ts`: stub at M001 — accepts POST, reads `req.text()` (raw body for signature, per CLAUDE.md §7), returns `501 Not Implemented` with body `{"error":"M002 will implement webhook handling"}`. Comment block cites pitfall 3.10 + REQ-303. Force `runtime = 'nodejs'`. Critical: do NOT call `req.json()` here — that would break Stripe signature verification when M002 wires it up.
    10. `apps/web-main/src/__tests__/health.test.ts`: TDD-first per `<behavior>`. Use `vi.spyOn(log, 'info')` pattern via the `@mjagency/config` createLogger.
    11. `apps/web-main/.size-limit.json`:
        ```json
        [
          { "name": "First Load JS (homepage)", "path": ".next/static/chunks/main-*.js", "limit": "150 KB", "gzip": true },
          { "name": "Admin chunk",              "path": ".next/static/chunks/app/(payload)/admin/**/*.js", "limit": "500 KB", "gzip": true }
        ]
        ```
    12. `public/robots.txt`: `User-agent: *` then `Disallow: /admin` (per RESEARCH §2.16 row "Payload admin enumeration").
    13. `apps/web-main/eslint.config.js`: re-export `@mjagency/config/eslint`.
    14. `apps/web-main/vitest.config.ts`: re-export shared config from `@mjagency/testing`.
    15. Run boot gate: `pnpm install`, then `pnpm dev --filter=@mjagency/web-main` in a separate terminal, `wait-on http://localhost:3000 -t 60000`, `curl -fsS http://localhost:3000/api/health`. Postgres is NOT yet running at this stage — Payload admin requires `DATABASE_URL`; document that the boot gate at Task 1.2 only verifies homepage + `/api/health`. The `/admin` first-user wizard gate runs at Plan 01-02 Task 2.1 once Postgres is up.
    16. Commit: `feat(01-01): web-main Next 15 + Payload 3.82.1 base app (Task 1.2)`.
  </action>
  <verify>
    <automated>cd apps/web-main && grep -E '"payload":\s*"3\.82\.1"' package.json && grep -E '"@payloadcms/next":\s*"3\.82\.1"' package.json && grep -E '"@payloadcms/db-postgres":\s*"3\.82\.1"' package.json && grep -q "withPayload" next.config.mjs && grep -q "buildConfig" payload.config.ts && grep -q "NEXT_RUNTIME" instrumentation.ts && [ -f "src/app/(payload)/admin/[[...segments]]/page.tsx" ] && [ -f "src/app/api/health/route.ts" ] && cd ../.. && pnpm install && pnpm vitest run apps/web-main/src/__tests__/health.test.ts && pnpm turbo run typecheck --filter=@mjagency/web-main && pnpm list payload --depth=0 | grep -q "payload 3.82.1"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web-main/package.json` contains the literal string `"payload": "3.82.1"` (NOT `"^3.82.1"` or `"~3.82.1"` — REQ-501, REQ-500, CLAUDE.md §1).
    - `apps/web-main/package.json` contains the same exact pin for `@payloadcms/next`, `@payloadcms/db-postgres`, `@payloadcms/richtext-lexical`.
    - `apps/web-main/next.config.mjs` contains `withPayload(nextConfig)` (default export).
    - `apps/web-main/next.config.mjs` does NOT contain the string `dangerouslyAllowSVG` (REQ-098, SEC-N4).
    - `apps/web-main/payload.config.ts` contains `buildConfig`, `postgresAdapter`, and `lexicalEditor`.
    - `apps/web-main/instrumentation.ts` contains `process.env.NEXT_RUNTIME === 'nodejs'`.
    - All four files exist: `src/app/(payload)/admin/[[...segments]]/page.tsx`, `src/app/(payload)/admin/[[...segments]]/not-found.tsx`, `src/app/(payload)/api/[...slug]/route.ts`, `src/app/(payload)/layout.tsx`.
    - `src/app/api/health/route.ts` exports `runtime = 'nodejs'` AND uses `createLogger` from `@mjagency/config`.
    - `src/app/api/stripe/webhook/route.ts` calls `req.text()` (NOT `req.json()`) and includes a comment referencing CLAUDE.md §7 / pitfall 3.10.
    - `apps/web-main/.size-limit.json` exists with at least 2 entries (homepage + admin chunk).
    - `pnpm vitest run apps/web-main/src/__tests__/health.test.ts` exits 0 with the expected JSON body assertion + Pino spy assertion green.
    - `pnpm turbo run typecheck --filter=@mjagency/web-main` exits 0.
    - `pnpm list payload --depth=0` reports `payload 3.82.1` (not 3.82.0, not 3.83.x).
    - No file under `apps/web-main/` contains `TODO`, `Coming soon`, `Lorem ipsum`, `[insert]`, or `jsonwebtoken` (REQ-200, REQ-502, CLAUDE.md §5+§2).
    - `apps/web-main/public/robots.txt` contains `Disallow: /admin`.
  </acceptance_criteria>
  <done>web-main has Next.js 15 + Payload 3.82.1 wired through `withPayload`; health route runs on Node with Pino redact; instrumentation.ts hook present (Node SDK body lands in Plan 04); homepage and Payload admin route group exist; size-limit baseline file present.</done>
</task>

<task type="auto">
  <name>Task 1.3: Scaffold the 11 remaining agency apps + hosts script + local-dev runbook</name>
  <files>
    apps/web-ecommerce/**,
    apps/web-growth/**,
    apps/web-webdev/**,
    apps/web-ai/**,
    apps/web-branding/**,
    apps/web-strategy/**,
    apps/web-finance/**,
    apps/web-engineering/**,
    apps/web-product/**,
    apps/web-video/**,
    apps/web-graphic/**,
    scripts/setup-hosts.sh,
    scripts/setup-hosts.ps1,
    scripts/scaffold-agency-app.ts,
    docs/runbooks/local-dev.md
  </files>
  <read_first>
    - apps/web-main/package.json (template to copy)
    - apps/web-main/next.config.mjs (template)
    - apps/web-main/payload.config.ts (template)
    - apps/web-main/src/** (entire tree — template)
    - packages/config/src/agency-constants.ts (the canonical agency list + ports)
    - mjagency/specs/milestone-M001.md §SLICE 1 Task 1.3 (lines 32-36)
    - .planning/phases/01-foundation-infra/01-CONTEXT.md (D-06 hosts decision, D-18 npm scope)
    - .planning/phases/01-foundation-infra/01-RESEARCH.md §2.12 (subdomain routing, lines 982-1016 — both bash + powershell scripts verbatim), §2.17 (agency port assignments, lines 1180-1193)
  </read_first>
  <action>
    1. Create `scripts/scaffold-agency-app.ts` — a **deterministic** TypeScript script that:
       - Reads `AGENCIES` from `packages/config/src/agency-constants.ts` (skip `brand` since web-main is the brand app).
       - For each of the 11 remaining slugs, copies `apps/web-main` to `apps/web-<slug>` with these substitutions:
         - `package.json`: rename to `@mjagency/web-<slug>`; bump dev/start port to `3000 + index`.
         - All Payload + Next pin versions stay identical to web-main.
         - `payload.config.ts`: identical (per `mjagency/specs/architecture.md` — agency apps share Payload config; agency selection happens via `process.env.AGENCY` in M002+).
         - `src/app/(frontend)/layout.tsx`: title swaps from "MJAgency Platform — Brand Hub" to e.g. "MJAgency E-commerce".
         - `src/app/api/health/route.ts`: pulls `service` and `agency` from `process.env.AGENCY` (already does — verify identical).
       - Idempotent: re-running on an existing `apps/web-<slug>` is a no-op (compare-and-skip).
    2. Run the script. Verify all 12 agency apps exist (web-main + 11 new).
    3. Create `scripts/setup-hosts.sh`: paste RESEARCH §2.12 lines 990-1002 verbatim (POSIX bash). Make executable: `chmod +x`.
    4. Create `scripts/setup-hosts.ps1`: paste RESEARCH §2.12 lines 1004-1014 verbatim (PowerShell).
    5. Create `docs/runbooks/local-dev.md`. Sections (real content per CLAUDE.md §5 — NO placeholders):
       - **Prerequisites**: Node 22 (via nvm), pnpm 10.x (via corepack), Docker 24+ with compose v2.20+, PgBouncer 1.21+, PM2 5.x, Doppler CLI, Stripe CLI.
       - **First-time setup** (the canonical 6-step sequence):
         1. `git clone && pnpm install --frozen-lockfile`
         2. `./scripts/setup-hosts.sh` (POSIX) or `Start-Process pwsh -Verb RunAs -ArgumentList scripts/setup-hosts.ps1` (Windows)
         3. `docker compose --profile dev up -d` (Plan 01-02)
         4. `pm2 start ecosystem.config.cjs` (Plan 01-02)
         5. `doppler login && doppler setup` (Plan 01-06)
         6. `pnpm dev --filter=@mjagency/web-main`
       - **Per-OS hosts notes** for macOS, Linux, Windows native (PowerShell elevated), WSL2 (must edit Windows hosts AND `/etc/hosts` inside WSL).
       - **Architecture spec divergence**: `mjagency/specs/architecture.md` line 364 says `*.mjagency.local` — CONTEXT D-06 wins (`*.localhost`). Note this and link to RESEARCH §2.12 final paragraph.
       - **CLAUDE.md / PROJECT.md parity**: copies live at repo root AND in `mjagency/`. Plan 01-05 CI parity check enforces byte-equality. To update, edit `mjagency/` first then `cp mjagency/CLAUDE.md ./CLAUDE.md && cp mjagency/PROJECT.md ./PROJECT.md`.
       - **Manual `/admin` first-user wizard gate** (Plan 01-02 verification — listed here for runbook readers): after Postgres is up, hit `http://localhost:3000/admin` and complete the wizard.
    6. Commit: `feat(01-01): scaffold 11 agency apps + hosts script + local-dev runbook (Task 1.3)`.
  </action>
  <verify>
    <automated>[ "$(ls -d apps/web-* | wc -l)" -eq 12 ] && for slug in ecommerce growth webdev ai branding strategy finance engineering product video graphic; do grep -E '"payload":\s*"3\.82\.1"' "apps/web-$slug/package.json" || exit 1; grep -q "@mjagency/web-$slug" "apps/web-$slug/package.json" || exit 1; [ -f "apps/web-$slug/next.config.mjs" ] || exit 1; [ -f "apps/web-$slug/instrumentation.ts" ] || exit 1; done && grep -q "ecommerce.localhost" scripts/setup-hosts.sh && grep -q "ecommerce.localhost" scripts/setup-hosts.ps1 && [ -x scripts/setup-hosts.sh ] && grep -q "127.0.0.1 brand.localhost" docs/runbooks/local-dev.md && pnpm turbo run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ls -d apps/web-* | wc -l` equals 12 (web-main + 11 agency apps).
    - For each agency slug in `ecommerce growth webdev ai branding strategy finance engineering product video graphic`:
      - `apps/web-$slug/package.json` contains `"payload": "3.82.1"` exact AND `"@mjagency/web-$slug"`.
      - `apps/web-$slug/next.config.mjs` exists and contains `withPayload`.
      - `apps/web-$slug/instrumentation.ts` exists and contains `NEXT_RUNTIME`.
      - `apps/web-$slug/payload.config.ts` exists.
      - `apps/web-$slug/src/app/(payload)/admin/[[...segments]]/page.tsx` exists.
      - `apps/web-$slug/src/app/api/health/route.ts` exists.
    - Each agency app's dev/start port follows the lock from `<interfaces>` (ecommerce=3001, growth=3002, ..., graphic=3011).
    - `scripts/setup-hosts.sh` exists, is executable (`-x`), and contains all 12 `*.localhost` entries on a single line: `brand.localhost ecommerce.localhost growth.localhost webdev.localhost ai.localhost branding.localhost strategy.localhost finance.localhost engineering.localhost product.localhost video.localhost graphic.localhost`.
    - `scripts/setup-hosts.ps1` exists and contains the same 12 entries.
    - `docs/runbooks/local-dev.md` contains:
      - the literal string `127.0.0.1 brand.localhost`
      - all six numbered first-time-setup steps
      - per-OS sections for `macOS`, `Linux`, `Windows`, `WSL2`
      - the architecture-spec divergence note referencing `*.mjagency.local` AND `*.localhost`
    - `pnpm turbo run typecheck` exits 0 across all 12 apps + 13 packages.
    - `pnpm list payload --depth=0 --json | jq -e 'all(.[]; .dependencies.payload.version == "3.82.1")'` exits 0 (every workspace pins 3.82.1 — REQ-501 dry run).
    - No agency app file contains `TODO`, `Coming soon`, `Lorem ipsum`, `[insert]`, or `jsonwebtoken`.
  </acceptance_criteria>
  <done>All 12 agency apps scaffolded from web-main template with port + scope + agency-name substitutions; hosts scripts ship for both OS families; local-dev runbook documents the canonical 6-step first-time-setup flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk → repo | Anything written to `apps/`, `packages/`, `scripts/` becomes shippable code; secrets must never land here |
| `mjagency/` (canonical) → repo root (`CLAUDE.md`/`PROJECT.md`) | Two copies of doctrine must stay byte-identical; drift = silent rule erosion |
| Workspace dep graph | Every app imports `@mjagency/config`; a bug here cascades across all 13 apps |

## STRIDE Threat Register (ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-001 | I (Information Disclosure) | Workspace ESLint config + future agency code | high | mitigate | `packages/config/eslint/index.js` includes `no-restricted-syntax` matcher for `process.env.NEXT_PUBLIC_*KEY` access (REQ-503). Plan 01-05 adds CI grep gate as fail-fast first job. |
| T-01-002 | D (Denial of Service via runtime crash) | Workspace dependency tree | high | mitigate | `packages/config/eslint/index.js` `no-restricted-imports` bans `jsonwebtoken` (REQ-502). Plan 01-05 adds CI grep gate at install time + dependency scan. |
| T-01-003 | T (Tampering) | Payload version drift | high | mitigate | Every app's `package.json` pins `"payload": "3.82.1"` exact. CI gate `pnpm list payload --depth=0 \| grep "payload 3.82.1"` lands in Plan 01-05. Plan 01-05 also ships `.github/dependabot.yml` ignoring `payload` and `@payloadcms/*`. |
| T-01-005 | I (Information Disclosure) | Pino logs across all apps | high | mitigate | `packages/config/src/logger.ts` REDACT_PATHS list locked at Task 1.1; Vitest unit test in `packages/config/src/__tests__/logger.test.ts` enforces redaction for `password`, `email`, `phone`, `token`, `secret`, `apiKey`, `creditCard`, `ssn`, `refreshToken`, `accessToken`, `jti`, `stripeKey`, `r2AccessKey`, `dopplerToken`, `jwtSecret`. Edge runtime uses `edgeLog()` fallback (no Pino in middleware — pitfall 3.4). |
| T-01-101 (plan-specific) | T (Tampering) | Payload route group hand-edits | medium | mitigate | Per pitfall 3.1, the `(payload)/` route group is generated from `create-payload-app@3.82.1` and never hand-edited. Acceptance criteria in Task 1.2 require all four canonical files to exist verbatim. |
| T-01-102 (plan-specific) | I (Information Disclosure) | Payload admin enumeration | medium | mitigate | `apps/web-main/public/robots.txt` + `apps/web-*/public/robots.txt` ship `Disallow: /admin`. Plan 01-04 will add `X-Robots-Tag: noindex` header at the route layer. |
| T-01-103 (plan-specific) | D (DoS / runtime crash) | Pino imported into Edge middleware | medium | mitigate | `packages/config/src/logger.ts` exports `edgeLog()` separately. Documented in `packages/config/README.md`. ESLint rule + future Plan 03 middleware will use `edgeLog` only. |
| T-01-104 (plan-specific) | T (Tampering) | `dangerouslyAllowSVG` re-introduced via `next.config.mjs` | high | mitigate | `apps/*/next.config.mjs` does NOT contain `dangerouslyAllowSVG`. ESLint rule in `packages/config/eslint/index.js` rejects `Property[key.name='dangerouslyAllowSVG']`. Plan 01-05 CI grep enforces. |

</threat_model>

<verification>
1. `pnpm install --frozen-lockfile` from a clean clone exits 0.
2. `pnpm turbo run typecheck` exits 0 across 12 apps + 13 packages.
3. `pnpm vitest run packages/config/src/__tests__/logger.test.ts` exits 0 with at least 14 redact-path assertions.
4. `pnpm vitest run apps/web-main/src/__tests__/health.test.ts` exits 0.
5. `pnpm dev --filter=@mjagency/web-main` boots Next.js + Payload without error; `curl -fsS http://localhost:3000/api/health` returns `{"ok":true,"service":"mjagency-main","agency":"main"}`. (Note: full Payload `/admin` first-user wizard requires Postgres from Plan 01-02.)
6. `pnpm list payload --depth=0` reports `payload 3.82.1` in every workspace.
7. `bash scripts/setup-hosts.sh` (or `.ps1`) appends 13 `*.localhost` entries idempotently.
8. No file under `apps/`, `packages/`, `scripts/` contains `TODO`, `Coming soon`, `Lorem ipsum`, `[insert]`, or `jsonwebtoken`.
9. Repo root `CLAUDE.md` and `PROJECT.md` are byte-identical to `mjagency/CLAUDE.md` and `mjagency/PROJECT.md`.
</verification>

<success_criteria>
- Workspace boots from clean clone (REQ-001).
- All 13 apps + 13 packages exist with strict TypeScript, exact Payload pin (REQ-501, REQ-500).
- Pino redact contract enforced by Vitest covering REQ-307 + REQ-426.
- ESLint flat config rejects `jsonwebtoken` import (REQ-502) and `NEXT_PUBLIC_*KEY` access (REQ-503).
- Hosts script ready for one-command per-OS dev setup (D-06).
- Subsequent plans (01-02..01-06) can read the workspace structure and add files without restructuring.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infra/01-foundation-infra-01-SUMMARY.md` summarizing:
- Workspace structure created (apps/13 + packages/13)
- Exact dependency versions installed (Payload 3.82.1, Next 15.5.15, Pino 10.3.1, etc.)
- Pino redact paths shipped + test coverage
- ESLint custom rules in effect
- Files Plan 01-02..01-06 will consume (turbo.json, instrumentation.ts loader, packages/config exports, agency port assignments)
</output>
