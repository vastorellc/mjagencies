---
phase: 12-launch-qa-seeds-runbooks-sla
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/incident-response.md
  - docs/runbooks/db-failover.md
  - docs/runbooks/redis-flush.md
  - docs/runbooks/cloudflare-waf-toggle.md
  - docs/runbooks/ccpa-erasure-manual.md
  - docs/runbooks/ga4-data-deletion.md
  - docs/runbooks/payload-backup-restore.md
  - docs/runbooks/dns-cutover.md
  - docs/runbooks/secrets-rotation.md
  - docs/runbooks/stripe-webhook-redeliver.md
  - docs/runbooks/bullmq-queue-drain.md
  - docs/runbooks/ssl-certificate-renewal.md
  - docs/runbooks/brand-setup-wizard.md
autonomous: true
requirements:
  - REQ-155

must_haves:
  truths:
    - "13 runbook files exist in docs/runbooks/"
    - "Every runbook has sections: Overview, Prerequisites, Procedure, Verification, Failure Diagnostics"
    - "Every runbook includes Audience, Last updated, Related metadata header"
    - "Runbooks reference real file paths and commands from the actual codebase"
    - "CCPA erasure manual runbook covers the 7-system fan-out (packages/compliance)"
    - "brand-setup-wizard.md runbook walks through all 5 wizard steps"
  artifacts:
    - path: "docs/runbooks/incident-response.md"
      provides: "P1-P4 incident response procedure"
      contains: "Incident Severity"
    - path: "docs/runbooks/brand-setup-wizard.md"
      provides: "Brand Setup Wizard walkthrough"
      contains: "Step 1"
    - path: "docs/runbooks/ccpa-erasure-manual.md"
      provides: "Manual CCPA 7-system erasure trigger"
      contains: "7-system"
  key_links:
    - from: "docs/runbooks/ccpa-erasure-manual.md"
      to: "packages/compliance/src/erasure/"
      via: "references 7-system worker"
      pattern: "packages/compliance"
    - from: "docs/runbooks/secrets-rotation.md"
      to: "Doppler CLI"
      via: "doppler secrets set command"
      pattern: "doppler secrets"
---

<objective>
Write all 13 runbooks (1 platform/ops + 12 per-procedure) covering every operational scenario for the MJAgency platform. Each runbook follows the mandatory 5-section structure with real commands, file paths, and env var references from the codebase.

Purpose: REQ-155 requires 13 runbooks pre-written before launch. Operators must be able to execute any procedure without codebase exploration.
Output: 13 .md files in docs/runbooks/ — each with Overview, Prerequisites, Procedure, Verification, Failure Diagnostics sections.
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/PROJECT.md
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/ROADMAP.md
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/phases/12-launch-qa-seeds-runbooks-sla/12-CONTEXT.md

<interfaces>
<!-- From docs/runbooks/backup-restore.md — runbook structure template -->
```markdown
# [Title] Runbook
**Audience:** On-call engineers and operators responsible for ...
**Last updated:** 2026-04-28 (Plan 12-05)
**Related:** `docs/runbooks/related.md`, `scripts/related.ts`

## Overview
Short description. **SLA targets:** RPO 1h, RTO 4h.

## Prerequisites
### Required role / access
Text + bash verification command.

## Procedure
### Step name
```bash
# numbered commands
```

## Verification
After procedure, confirm success via:
1. **Check 1:** Steps.

## Failure Diagnostics
**Symptom:** description.
**Check:** what to inspect.
**Fix:** remediation.
```

<!-- Existing runbooks in docs/runbooks/ (already present — do not recreate):
     auth-audit-events.md, backup-restore.md, clarity-project-setup.md,
     cloudflare-edge.md, cloudflare-waf-rollout.md, dr-drill.md, github-setup.md,
     local-dev.md, migrations.md, observability.md, pgbouncer-rls.md, seed.md,
     server-action-pattern.md, storybook-visual-regression.md, vault-audit.md, vps-provisioning.md
-->

<!-- 13 NEW runbooks to create (from D-05):
     incident-response.md, db-failover.md, redis-flush.md, cloudflare-waf-toggle.md,
     ccpa-erasure-manual.md, ga4-data-deletion.md, payload-backup-restore.md,
     dns-cutover.md, secrets-rotation.md, stripe-webhook-redeliver.md,
     bullmq-queue-drain.md, ssl-certificate-renewal.md, brand-setup-wizard.md
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Incident + infrastructure runbooks (7 files)</name>
  <files>
    docs/runbooks/incident-response.md,
    docs/runbooks/db-failover.md,
    docs/runbooks/redis-flush.md,
    docs/runbooks/cloudflare-waf-toggle.md,
    docs/runbooks/payload-backup-restore.md,
    docs/runbooks/dns-cutover.md,
    docs/runbooks/ssl-certificate-renewal.md
  </files>

  <read_first>
    - docs/runbooks/backup-restore.md (full file — structure template: all 5 sections, bash blocks, Symptom/Check/Fix format)
    - docs/runbooks/dr-drill.md (full file — DR procedure format)
    - docs/runbooks/cloudflare-waf-rollout.md (full file — WAF toggle steps to base waf-toggle runbook on)
    - docs/runbooks/migrations.md (full file — numbered procedure style)
    - 12-CONTEXT.md lines 39-42 (D-05 — list of 13 runbook topics)
  </read_first>

  <action>
    Create all 7 runbooks following the mandatory 5-section structure. Use REAL commands from the codebase.

    **1. docs/runbooks/incident-response.md**
    - Audience: On-call engineers, platform ops
    - Overview: P1-P4 incident severity matrix with response/resolution SLAs (per 12-UI-SPEC severity table: P1=5min response/1h resolution, P2=15min/4h, P3=1h/24h, P4=next-business-day/72h)
    - Prerequisites: PagerDuty access, Doppler super_admin, Cloudflare dashboard access
    - Procedure sections: "1. Detect and Classify" → check status.mjagency.com + OTel Grafana; "2. Declare incident severity (P1-P4)"; "3. Engage on-call rotation"; "4. Isolate fault domain" (WAF toggle, route traffic, drain queue); "5. Resolve and post-mortem"
    - Verification: all agencies respond 200 on /api/health
    - Failure Diagnostics: Symptom=high error rate / Check=OTel traces / Fix=WAF toggle or rollback

    **2. docs/runbooks/db-failover.md**
    - Audience: Database administrators, platform ops
    - Overview: Per-agency Postgres failover using pgBackRest + PgBouncer. RPO 1h, RTO 4h (PROJECT.md SLA).
    - Prerequisites: pgBackRest access, Doppler DB credentials, `scripts/pgbackrest-restore.sh` available
    - Procedure: "1. Confirm primary is down (psql check)"; "2. Promote replica if hot standby configured"; "3. Run `bash scripts/pgbackrest-restore.sh --agency={slug} --latest`"; "4. Update PgBouncer connection string in Doppler"; "5. Restart apps via PM2: `pm2 restart web-{slug}`"
    - Verification: `psql $DATABASE_URL_DIRECT -c "SELECT NOW()"` returns current time
    - Diagnostics: Symptom=connection refused / Check=pgBackRest status / Fix=restore from R2

    **3. docs/runbooks/redis-flush.md**
    - Audience: Platform ops
    - Overview: Flush Redis cache/queue for a single agency or all agencies. Use when cache corruption or stale data detected.
    - Prerequisites: Redis CLI access, `REDIS_URL` from Doppler
    - Procedure: "1. Single agency: `redis-cli -u $REDIS_URL DEL agency:{slug}:cache:*`"; "2. All agencies: iterate over 12 slugs"; "3. BullMQ: do NOT use FLUSHALL — use bullmq-queue-drain.md instead"; "4. Verify cache empty: `redis-cli -u $REDIS_URL KEYS agency:{slug}:cache:*`"
    - Verification: next request repopulates cache (5-min GA4 cache key `agency:<id>:dashboard:ga4:*` re-appears within 5 min)
    - Diagnostics: Symptom=stale analytics data / Check=Redis TTL / Fix=targeted DEL by key prefix

    **4. docs/runbooks/cloudflare-waf-toggle.md**
    - Audience: Security team, platform ops
    - Overview: Toggle Cloudflare WAF between log-only and enforcing mode. Companion to cloudflare-waf-rollout.md (which covers initial rollout). Use this for emergency disable/enable.
    - Prerequisites: Cloudflare API token with Zone:Firewall Rules:Edit, Terraform installed, `infra/cloudflare/` access
    - Procedure: "1. Set enable_enforcing=false: edit `infra/cloudflare/waf.tf` variable `enable_enforcing`"; "2. Run: `cd infra/cloudflare && terraform apply -var="enable_enforcing=false"`"; "3. To re-enable: `terraform apply -var="enable_enforcing=true"`"
    - WARNING note: "Do NOT toggle WAF to enforcing during active canary deploy window (canary-deploy.yml job annotation)"
    - Verification: WAF mode reflected in Cloudflare dashboard Security → WAF
    - Diagnostics: Symptom=blocking legitimate traffic / Check=WAF log for false positives / Fix=disable custom rule or add to allowlist

    **5. docs/runbooks/payload-backup-restore.md**
    - Audience: Platform ops, CMS administrators
    - Overview: Backup and restore Payload CMS data (SQLite media metadata + Postgres collections). Complement to pgBackRest DB backups.
    - Prerequisites: R2 access (CLOUDFLARE_R2_BUCKET from Doppler), Payload admin super_admin role
    - Procedure: "1. Export via Payload REST: `GET /api/{collection}?limit=0&depth=0`"; "2. Save JSON to R2: `scripts/pgbackrest-verify-r2.ts` pattern for upload"; "3. Restore: POST each collection record via seed-payload-collections.ts with idempotency check"; "4. For migration rollback: `npx tsx scripts/migrate-rollback.ts --agency={slug}`"
    - Verification: collection count matches pre-restore export
    - Diagnostics: Symptom=missing CMS records / Check=Payload API GET /api/{collection}?limit=0 / Fix=re-run seed-payload-collections.ts

    **6. docs/runbooks/dns-cutover.md**
    - Audience: Platform ops, agency account managers
    - Overview: DNS cutover checklist for moving a custom domain to the MJAgency deployment. Step-by-step for the admin user.
    - Prerequisites: Cloudflare zone access, agency's domain registrar credentials, Brand Setup Wizard completed (Step 5)
    - Procedure: "1. Add CNAME: `{agency-domain} CNAME {workers-subdomain}.workers.dev`"; "2. Wait for propagation (TTL-dependent, typically 5-15 min)"; "3. Confirm SSL issued: check Cloudflare dashboard SSL/TLS → Edge Certificates"; "4. Test via curl: `curl -I https://{domain}/`"; "5. Update CAL_SUBDOMAIN in Doppler if Cal.com is integrated"; "6. Verify email DKIM/SPF records via /admin/email-setup"
    - Verification: `curl -sI https://{domain}/` returns `HTTP/2 200` with `cf-ray:` header
    - Diagnostics: Symptom=ERR_SSL_PROTOCOL_ERROR / Check=Cloudflare SSL mode (must be Full Strict) / Fix=re-issue certificate

    **7. docs/runbooks/ssl-certificate-renewal.md**
    - Audience: Platform ops
    - Overview: SSL certificates are auto-renewed by Cloudflare. This runbook covers manual renewal if auto-renewal fails.
    - Prerequisites: Cloudflare API token with Zone:SSL/TLS:Edit
    - Procedure: "1. Check expiry: Cloudflare dashboard SSL/TLS → Edge Certificates → Expiration"; "2. Auto-renewal trigger: `POST /zones/{zone_id}/ssl/certificate_packs/order`"; "3. Verify renewal: poll every 60s until status=active"
    - Diagnostics: Symptom=cert expired warning / Check=Cloudflare SSL status via API / Fix=manual order via API
  </action>

  <verify>
    <automated>
      ls docs/runbooks/incident-response.md docs/runbooks/db-failover.md docs/runbooks/redis-flush.md docs/runbooks/cloudflare-waf-toggle.md docs/runbooks/payload-backup-restore.md docs/runbooks/dns-cutover.md docs/runbooks/ssl-certificate-renewal.md && echo "7 files present"; grep "## Overview\|## Prerequisites\|## Procedure\|## Verification\|## Failure Diagnostics" docs/runbooks/incident-response.md | wc -l && echo "sections count"
    </automated>
  </verify>

  <acceptance_criteria>
    - `ls docs/runbooks/incident-response.md docs/runbooks/db-failover.md docs/runbooks/redis-flush.md docs/runbooks/cloudflare-waf-toggle.md docs/runbooks/payload-backup-restore.md docs/runbooks/dns-cutover.md docs/runbooks/ssl-certificate-renewal.md` returns 7 files without error
    - `grep -c "## Overview\|## Prerequisites\|## Procedure\|## Verification\|## Failure Diagnostics" docs/runbooks/incident-response.md` returns 5
    - `grep "RPO\|RTO" docs/runbooks/db-failover.md` returns the SLA reference
    - `grep "enable_enforcing" docs/runbooks/cloudflare-waf-toggle.md` returns the Terraform var
    - `grep "Do NOT toggle.*WAF\|canary" docs/runbooks/cloudflare-waf-toggle.md` returns the canary warning
    - `grep "P1\|P2\|P3\|P4" docs/runbooks/incident-response.md` returns severity level references
    - Zero occurrences of "TODO", "Coming soon", "Lorem ipsum", "[insert]" in any of the 7 files:
      `grep -rl "TODO\|Coming soon\|Lorem ipsum\|\[insert\]" docs/runbooks/incident-response.md docs/runbooks/db-failover.md docs/runbooks/redis-flush.md` returns empty
  </acceptance_criteria>

  <done>7 infrastructure runbooks exist with all 5 mandatory sections; real commands from codebase; no placeholder text; WAF toggle includes canary warning.</done>
</task>

<task type="auto">
  <name>Task 2: Compliance + operational runbooks + brand-setup-wizard (6 files)</name>
  <files>
    docs/runbooks/ccpa-erasure-manual.md,
    docs/runbooks/ga4-data-deletion.md,
    docs/runbooks/secrets-rotation.md,
    docs/runbooks/stripe-webhook-redeliver.md,
    docs/runbooks/bullmq-queue-drain.md,
    docs/runbooks/brand-setup-wizard.md
  </files>

  <read_first>
    - docs/runbooks/clarity-project-setup.md (full file — per-agency env table + step-numbered procedure template)
    - docs/runbooks/vault-audit.md (full file — secrets and compliance procedure format)
    - docs/runbooks/auth-audit-events.md (full file — compliance audit procedure)
    - packages/compliance/src/erasure/ (ls — list 7-system modules for ccpa-erasure-manual.md)
    - 12-UI-SPEC.md lines 116-233 (Surface 1: Brand Setup Wizard steps 1-5 — exact copy for brand-setup-wizard.md)
    - 12-CONTEXT.md lines 44-47 (D-05 topics) and lines 70-73 (specific ideas about CCPA erasure)
  </read_first>

  <action>
    Create all 6 runbooks following the same 5-section structure.

    **1. docs/runbooks/ccpa-erasure-manual.md**
    - Audience: Privacy officer, compliance team, platform ops
    - Overview: Manual trigger for CCPA erasure 7-system fan-out (packages/compliance). Companion to automated flow at /privacy/erasure. Use when automated flow fails or for regulator-requested erasure.
    - Prerequisites: super_admin session, Redis access, BullMQ monitor, packages/compliance built
    - Procedure:
      "1. Identify the erasure request (email + requestId from ccpa_erasure_records table)"
      "2. Confirm identity verification: check JWT confirmation step was completed (status='confirmed' in DB)"
      "3. Manual queue enqueue:
         ```bash
         npx tsx -e "
         import { Queue } from 'bullmq'
         const q = new Queue('ccpa-erasure', { connection: { url: process.env.REDIS_URL } })
         await q.add('erasure', { email: 'user@example.com', requestId: 'REQ-123' })
         console.log('queued')
         await q.close()
         "
         ```"
      "4. Monitor fan-out: `redis-cli -u $REDIS_URL SUBSCRIBE bullmq:ccpa-erasure:completed`"
      "5. Verify 7 systems deleted: check ccpa_erasure_records.audit_rows count = 7"
      "6. Confirm receipt PDF uploaded to R2 erasure-receipts vault"
    - 7-system list: Postgres (delete-from-postgres.ts), Redis (delete-from-redis.ts), R2 (delete-from-r2.ts), GA4 (ga4-delete.ts), LiteLLM (litellm-delete.ts), Meta CAPI (via @mjagency/meta-capi), Clarity (via @mjagency/analytics)
    - Verification: `psql $DATABASE_URL_DIRECT -c "SELECT * FROM ccpa_erasure_records WHERE request_id = 'REQ-123'"` shows completed_at timestamp
    - Diagnostics: Symptom=fan-out partial / Check=worker logs / Fix=re-run failed system delete module directly

    **2. docs/runbooks/ga4-data-deletion.md**
    - Audience: Privacy officer, analytics team
    - Overview: Request user data deletion from GA4 following CCPA/GDPR erasure. Must be performed after automated erasure for any user who had GA4 tracking active.
    - Prerequisites: GA4 admin access, user's client_id (from consent_log or ga4 events)
    - Procedure: "1. Create User Deletion Request via GA4 Admin API"; "2. Use `curl -X POST 'https://www.googleapis.com/analytics/v3/userDeletion/userDeletionRequests:upsert?access_token={token}' -d '{...}'`"; "3. Wait 63 days for full deletion propagation"; "4. Document in ccpa_erasure_records"
    - Verification: GA4 Admin API GET user deletion request status returns 'deleted'
    - Diagnostics: Symptom=deletion request pending / Check=API status / Fix=re-submit request

    **3. docs/runbooks/secrets-rotation.md**
    - Audience: Security team, platform ops
    - Overview: Rotate secrets in Doppler for one or all agencies. Covers JWT_SECRET, DB passwords, API keys, webhook secrets.
    - Prerequisites: Doppler super_admin, `doppler login` completed
    - Procedure:
      "1. Generate new secret: `openssl rand -base64 32`"
      "2. Set in Doppler: `doppler secrets set JWT_SECRET=<new-value> --project mjagency-shared --config prd`"
      "3. Per-agency: `doppler secrets set GA4_MEASUREMENT_ID_<SLUG>=<value> --project mjagency-{slug} --config prd`"
      "4. Redeploy workers: `wrangler deploy` or restart PM2"
      "5. Revoke old token: add old JWT_SECRET to revocation list in Redis (if jose key rotation is needed)"
    - Per-agency env var table (per clarity-project-setup.md pattern):
      | Var | Scope | Purpose |
      | JWT_SECRET | Server | JWT signing key |
      | DATABASE_URL | Server | Postgres connection |
      | STRIPE_SECRET_KEY | Server | Stripe API |
      | STRIPE_WEBHOOK_SECRET | Server | Stripe webhook HMAC |
    - Verification: `doppler secrets get JWT_SECRET` returns new value
    - Diagnostics: Symptom=401 on all requests after rotation / Check=JWT_SECRET deployed / Fix=re-trigger deployment

    **4. docs/runbooks/stripe-webhook-redeliver.md**
    - Audience: Payments team, platform ops
    - Overview: Re-deliver failed Stripe webhook events. Use when a webhook endpoint was down during event delivery.
    - Prerequisites: Stripe Dashboard access with Developer permissions, STRIPE_WEBHOOK_SECRET in Doppler
    - Procedure: "1. Find failed events in Stripe Dashboard → Developers → Webhooks → endpoint → Recent events"; "2. Click 'Resend' on failed event (Stripe UI)"; "3. Or via CLI: `stripe events resend evt_123`"; "4. Check idempotency: Redis key `stripe:event:evt_123` prevents duplicate processing"
    - WARNING: "Stripe webhook handler uses `req.text()` for raw body (CLAUDE.md §7). Never send parsed JSON to `stripe.webhooks.constructEvent`."
    - Verification: BullMQ queue shows event processed; invoice/lead CRM record updated
    - Diagnostics: Symptom=duplicate invoice / Check=Redis idempotency key / Fix=manual Redis DEL to allow single re-process

    **5. docs/runbooks/bullmq-queue-drain.md**
    - Audience: Platform ops
    - Overview: Drain a BullMQ queue for a specific agency. Use for queue backlog, bad jobs, or pre-deploy queue flush.
    - Prerequisites: Redis access, BullMQ CLI or custom script
    - Procedure:
      "1. List queue counts: `npx tsx -e "import { Queue } from 'bullmq'; const q = new Queue('agency:{slug}:{name}', ...); console.log(await q.getJobCounts())"`"
      "2. Drain waiting jobs: `await q.drain()`"
      "3. Clean completed/failed: `await q.clean(0, 100, 'completed'); await q.clean(0, 100, 'failed')`"
      "4. For CCPA erasure queue: use ccpa-erasure-manual.md instead — do NOT drain"
    - WARNING: "Do NOT drain the `ccpa-erasure` queue — use ccpa-erasure-manual.md for controlled processing"
    - Verification: queue counts return `{ waiting: 0, active: 0, delayed: 0 }` for drained queue
    - Diagnostics: Symptom=queue stuck / Check=worker process running / Fix=restart BullMQ worker process

    **6. docs/runbooks/brand-setup-wizard.md**
    - Audience: Agency account managers, agency owners completing post-generate setup
    - Overview: Complete walkthrough of all 5 Brand Setup Wizard steps at /admin/brand-setup. Required once per agency before public launch. Covers logo upload, brand color, identity fields, API keys, and DNS + warmup checklist.
    - Prerequisites: super_admin session at /admin, agency domain and brand assets ready, GA4/Clarity/Meta Pixel IDs available
    - Procedure (mirror exact UI-SPEC step structure):
      "Step 1: Logo Upload — Upload SVG, PNG, or JPG via drag-drop. SVG files are sanitized via DOMPurify + SVGO automatically. Preview appears in 120×120 container. Click 'Next: Colors'."
      "Step 2: Brand Color — Select primary color via color picker. Wait for ΔE check result. If ΔE < 30 warning appears, adjust to a more distinct color. Click 'Next: Identity'."
      "Step 3: Identity Fields — Fill: Tagline (max 80 chars), About (max 500 chars), Phone (+1 format), Address. All required. Click 'Next: API Keys'."
      "Step 4: API Keys — Enter GA4 Measurement ID (G-XXXXXXXXXX), Microsoft Clarity Project ID, Meta Pixel ID. All optional. Keys stored in Doppler, never in NEXT_PUBLIC_ variables. Click 'Next: DNS + Warmup'."
      "Step 5: DNS + Warmup Checklist — Complete all 6 checklist items (see UI-SPEC). Click 'Save Brand Setup'."
      "On success: 'Brand setup saved. Your agency is ready for launch.' message appears."
    - Verification: Agency public site loads with new logo and brand color; /api/health returns 200
    - Diagnostics: Symptom=ΔE check always fails / Check=hero image assets in Payload / Fix=ensure Phase 12-02 seed run completed first
  </action>

  <verify>
    <automated>
      ls docs/runbooks/ccpa-erasure-manual.md docs/runbooks/ga4-data-deletion.md docs/runbooks/secrets-rotation.md docs/runbooks/stripe-webhook-redeliver.md docs/runbooks/bullmq-queue-drain.md docs/runbooks/brand-setup-wizard.md && echo "6 files present"; grep -c "## Overview\|## Prerequisites\|## Procedure\|## Verification\|## Failure Diagnostics" docs/runbooks/ccpa-erasure-manual.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `ls docs/runbooks/ccpa-erasure-manual.md docs/runbooks/ga4-data-deletion.md docs/runbooks/secrets-rotation.md docs/runbooks/stripe-webhook-redeliver.md docs/runbooks/bullmq-queue-drain.md docs/runbooks/brand-setup-wizard.md` returns 6 files
    - `grep -c "## Overview\|## Prerequisites\|## Procedure\|## Verification\|## Failure Diagnostics" docs/runbooks/ccpa-erasure-manual.md` returns 5
    - `grep "7-system\|7 systems\|packages/compliance" docs/runbooks/ccpa-erasure-manual.md` returns references to all 7 erasure systems
    - `grep "delete-from-postgres\|delete-from-redis\|delete-from-r2\|ga4-delete\|litellm-delete" docs/runbooks/ccpa-erasure-manual.md` returns file path references
    - `grep "Step 1\|Step 2\|Step 3\|Step 4\|Step 5" docs/runbooks/brand-setup-wizard.md` returns 5 matches
    - `grep "ΔE\|deltaE\|CIEDE2000" docs/runbooks/brand-setup-wizard.md` returns ΔE check reference
    - `grep "req.text()\|raw body" docs/runbooks/stripe-webhook-redeliver.md` returns the CLAUDE.md warning
    - `grep "do NOT drain.*ccpa\|ccpa-erasure.*drain" docs/runbooks/bullmq-queue-drain.md` returns the warning
    - `grep -rl "TODO\|Coming soon\|Lorem ipsum\|\[insert\]" docs/runbooks/ccpa-erasure-manual.md docs/runbooks/brand-setup-wizard.md` returns empty (no placeholder text)
    - Total: `ls docs/runbooks/ | grep -E "incident-response|db-failover|redis-flush|cloudflare-waf-toggle|ccpa-erasure-manual|ga4-data-deletion|payload-backup-restore|dns-cutover|secrets-rotation|stripe-webhook-redeliver|bullmq-queue-drain|ssl-certificate-renewal|brand-setup-wizard" | wc -l` returns 13
  </acceptance_criteria>

  <done>All 6 compliance/operational runbooks exist with 5 mandatory sections, real codebase file paths, no placeholder text; brand-setup-wizard.md mirrors all 5 UI-SPEC wizard steps; total runbook count reaches 13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runbook commands → production systems | Runbook bash commands execute against live systems; wrong target = data loss |
| Secrets rotation procedure → Doppler | Procedure instructs doppler secrets set; token must have write scope |
| CCPA erasure manual trigger → BullMQ | Manual enqueue bypasses the verified identity check; must document this risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | ccpa-erasure-manual.md | mitigate | Procedure explicitly requires confirming identity verification step was completed before manual enqueue; documented as step 2 |
| T-12-05-02 | Information Disclosure | secrets-rotation.md | mitigate | Runbook instructs to use Doppler (never grep secrets from .env files); old JWT_SECRET added to Redis revocation list |
| T-12-05-03 | Denial of Service | bullmq-queue-drain.md | mitigate | Warning: do NOT drain ccpa-erasure queue; other queues can be drained by platform ops only |
| T-12-05-04 | Repudiation | ccpa-erasure-manual.md | mitigate | Manual trigger must be logged: procedure includes step to document in ccpa_erasure_records |
</threat_model>

<verification>
1. `ls docs/runbooks/ | grep -E "incident-response|db-failover|redis-flush|cloudflare-waf-toggle|ccpa-erasure-manual|ga4-data-deletion|payload-backup-restore|dns-cutover|secrets-rotation|stripe-webhook-redeliver|bullmq-queue-drain|ssl-certificate-renewal|brand-setup-wizard" | wc -l` returns 13
2. `grep -c "## Overview\|## Prerequisites\|## Procedure\|## Verification\|## Failure Diagnostics" docs/runbooks/incident-response.md` returns 5
3. `grep "packages/compliance" docs/runbooks/ccpa-erasure-manual.md` returns file path reference
4. `grep -rl "TODO\|Coming soon\|Lorem ipsum" docs/runbooks/*.md` returns empty (no new placeholders)
</verification>

<success_criteria>
- All 13 new runbooks exist in docs/runbooks/
- Every runbook has exactly 5 mandatory sections (Overview, Prerequisites, Procedure, Verification, Failure Diagnostics)
- CCPA erasure runbook references all 7 erasure system modules by file name
- brand-setup-wizard.md mirrors all 5 wizard steps from UI-SPEC
- Zero placeholder text in any runbook
</success_criteria>

<output>
After completion, create `.planning/phases/12-launch-qa-seeds-runbooks-sla/12-05-SUMMARY.md`
</output>
