---
phase: 12-launch-qa-seeds-runbooks-sla
plan: 02
type: execute
wave: 2
depends_on: [12-07]
files_modified:
  - scripts/seed-all-agencies.mjs
  - packages/db/src/seeds/agency-seed-manifest.ts
  - packages/db/src/seeds/seed-payload-collections.ts
  - packages/db/src/seeds/seed-crm-preseeds.ts
autonomous: true
requirements:
  - REQ-151
  - REQ-152

must_haves:
  truths:
    - "A single seed-all-agencies.mjs script runs all 12 agencies top-to-bottom and exits 0 on success"
    - "Seeds cover: pages, services, tools, team, testimonials, FAQs, blog posts, case studies, CRM leads/deals/contacts, niche benchmarks"
    - "All long-form seeded content (blog, case studies, tool pages) meets MIN_WORD_COUNT=2200"
    - "Script is resumable — skips agencies already seeded (idempotent)"
    - "No placeholder text in any seeded content (CONTENT-COMPLETE RULE)"
    - "Seed validates content completeness before writing to Payload REST API"
  artifacts:
    - path: "scripts/seed-all-agencies.mjs"
      provides: "Master seed orchestrator script"
      contains: "seed-all-agencies"
    - path: "packages/db/src/seeds/agency-seed-manifest.ts"
      provides: "Per-agency seed data map (all 12 agencies, all collections)"
      exports: ["AGENCY_SEED_MANIFEST"]
    - path: "packages/db/src/seeds/seed-payload-collections.ts"
      provides: "Payload REST API seed writer"
      exports: ["seedPayloadCollection"]
    - path: "packages/db/src/seeds/seed-crm-preseeds.ts"
      provides: "CRM pre-seed writer (leads, deals, contacts, pipelines)"
      exports: ["seedCrmData"]
  key_links:
    - from: "scripts/seed-all-agencies.mjs"
      to: "packages/db/src/seeds/agency-seed-manifest.ts"
      via: "import AGENCY_SEED_MANIFEST"
      pattern: "AGENCY_SEED_MANIFEST"
    - from: "packages/db/src/seeds/seed-payload-collections.ts"
      to: "Payload REST API /api/{collection}"
      via: "fetch with Authorization: JWT"
      pattern: "fetch.*api.*Authorization"
---

<objective>
Build and execute the complete seed run for all 12 agencies across all Payload CMS collections and CRM pre-seed data. Seeds must be real, complete, and word-count validated — no placeholders.

Purpose: REQ-151 requires 100% content coverage with real assets and validator passes. This is the data layer that makes the platform content-complete at launch.
Output: seed-all-agencies.mjs script, agency-seed-manifest.ts (all content), seed-payload-collections.ts + seed-crm-preseeds.ts helpers, idempotent run.
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/PROJECT.md
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/ROADMAP.md
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/phases/12-launch-qa-seeds-runbooks-sla/12-CONTEXT.md

<interfaces>
<!-- From scripts/seed-runner.ts — CLI entry pattern -->
```typescript
#!/usr/bin/env tsx
/**
 * Usage: pnpm tsx scripts/seed-runner.ts [flags]
 * Exit codes: 0 = success, 1 = failure
 */
async function main(): Promise<void> {
  const args = parseArgs(process.argv)
  if (args.help) { console.log(HELP); process.exit(0) }
  // ...
  process.exit(0)
}
main().catch((err) => {
  console.error('seed-runner: unexpected error:', err)
  process.exit(1)
})
```

<!-- gsd-headless.mjs uses .mjs (not .ts) — same pattern applies here -->
<!-- seed-all-agencies.mjs: #!/usr/bin/env node, import { execSync } from 'node:child_process' -->

<!-- CONTENT-COMPLETE RULE (CLAUDE.md §5): NEVER generate placeholder text -->
<!-- MIN_WORD_COUNT=2200 on long-form (blog, case studies, tool pages) -->
<!-- Content must be real agency-niche content written for: -->
<!-- web-ai=AI/Marketing, web-branding=Branding, web-construction=Construction, -->
<!-- web-dental=Dental, web-ecommerce=Ecommerce, web-financial=Financial Advisory, -->
<!-- web-fitness=Fitness, web-homeservices=Home Services, web-legal=Legal, -->
<!-- web-realestate=Real Estate, web-restaurant=Restaurant, web-spa=Spa/Wellness -->

<!-- Payload REST API seed pattern: POST /api/{collection} with Authorization: Bearer {jwt} -->
<!-- Idempotency: check if slug exists with GET /api/{collection}?where[slug][equals]={slug} before POST -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agency seed manifest — all 12 agencies, all collections</name>
  <files>
    packages/db/src/seeds/agency-seed-manifest.ts
  </files>

  <read_first>
    - packages/db/src/seeds/ (list existing files to understand current seed structure)
    - scripts/seed-runner.ts (full file — idempotency pattern and how agencies are enumerated)
    - scripts/content-sprint/ (check for existing seeded content to reuse/extend)
    - packages/cms/src/collections/ (collection slugs to target: pages, services, tools, team, testimonials, faqs, blog-posts, case-studies)
    - 12-CONTEXT.md lines 30-35 (D-02 seed completeness spec)
  </read_first>

  <behavior>
    - AGENCY_SEED_MANIFEST is a Record<AgencySlug, AgencySeedData> where AgencySlug is the 12 slugs
    - AgencySeedData contains:
      - `pages`: array of page records (home, about, services, contact — minimum 4 per agency)
      - `services`: array of service records (minimum 5 per agency, 1500+ words each)
      - `tools`: array of tool records (3 per agency — reuse Phase 10 tool content)
      - `team`: array of team member records (minimum 3 per agency)
      - `testimonials`: array of testimonial records (minimum 5 per agency)
      - `faqs`: array of FAQ records (minimum 8 per agency)
      - `blogPosts`: array of blog post records (minimum 3 per agency, 1500+ words each)
      - `caseStudies`: array of case study records (minimum 2 per agency, 2200+ words each)
    - All content MUST be real, agency-niche-specific, no "Lorem ipsum", no "Coming soon", no "[insert]"
    - All testimonial content: attributed to real-sounding names (first + last), no fake statistics without source
    - Blog posts and case studies: 2200+ word entries with real niche-specific content
    - Each record has `slug` field (URL-safe, agency-prefixed e.g. 'ai-home', 'branding-about')
    - Validation test: `validateWordCount(text: string, min: number): boolean` — throws if below min
  </behavior>

  <action>
    Create `packages/db/src/seeds/agency-seed-manifest.ts`:

    1. Define `AgencySlug` type as union of all 12 slugs: 'web-ai' | 'web-branding' | 'web-construction' | 'web-dental' | 'web-ecommerce' | 'web-financial' | 'web-fitness' | 'web-homeservices' | 'web-legal' | 'web-realestate' | 'web-restaurant' | 'web-spa'

    2. Define interfaces: `SeedPage`, `SeedService`, `SeedTool`, `SeedTeamMember`, `SeedTestimonial`, `SeedFaq`, `SeedBlogPost`, `SeedCaseStudy`, `AgencySeedData`

    3. Export `validateWordCount(text: string, minWords: number): void` — counts words via `text.trim().split(/\s+/).length`, throws `Error('Word count ${count} below minimum ${minWords}')` if below.

    4. Export `AGENCY_SEED_MANIFEST: Record<AgencySlug, AgencySeedData>` with REAL content for all 12 agencies. Each niche gets:
       - web-ai: AI marketing agency content (AI automation, machine learning campaigns, predictive analytics)
       - web-branding: Brand identity agency (brand strategy, logo systems, visual identity, brand voice)
       - web-construction: Construction company marketing (commercial construction, project management, safety records)
       - web-dental: Dental practice marketing (cosmetic dentistry, patient acquisition, appointment booking)
       - web-ecommerce: Ecommerce growth agency (conversion optimization, paid ads, retention marketing)
       - web-financial: Financial advisory marketing (wealth management, retirement planning, investment strategies)
       - web-fitness: Fitness studio marketing (personal training, group classes, nutrition coaching)
       - web-homeservices: Home services marketing (HVAC, plumbing, electrical, landscaping lead generation)
       - web-legal: Legal services marketing (personal injury, family law, business law, local SEO)
       - web-realestate: Real estate marketing (buyer/seller leads, listing photography, CMA reports)
       - web-restaurant: Restaurant marketing (online ordering, loyalty programs, local SEO, social media)
       - web-spa: Spa and wellness marketing (booking systems, membership programs, treatment packages)

    5. All blog posts and case studies must be ≥ 2200 words of real content. For each: write 4-5 paragraph introduction, 3-4 substantive sections with H2 headings, a conclusion, and a CTA section.

    IMPORTANT: Do not write placeholder content. Every word must be real and usable as actual marketing copy for that niche.
  </action>

  <verify>
    <automated>
      npx tsx -e "import { AGENCY_SEED_MANIFEST, validateWordCount } from './packages/db/src/seeds/agency-seed-manifest.js'; const slugs = Object.keys(AGENCY_SEED_MANIFEST); console.log('agencies:', slugs.length); const first = Object.values(AGENCY_SEED_MANIFEST)[0]; console.log('blog posts:', first.blogPosts.length); console.log('manifest ok');"
    </automated>
  </verify>

  <acceptance_criteria>
    - `Object.keys(AGENCY_SEED_MANIFEST).length` equals 12
    - Every agency has `.blogPosts.length >= 3` and `.caseStudies.length >= 2`
    - `grep -c "Lorem ipsum" packages/db/src/seeds/agency-seed-manifest.ts` returns 0
    - `grep -c "TODO\|Coming soon\|\[insert\]\|placeholder" packages/db/src/seeds/agency-seed-manifest.ts` returns 0
    - `grep "validateWordCount" packages/db/src/seeds/agency-seed-manifest.ts` finds the exported function
    - `pnpm tsc --noEmit` passes with no errors from this file
  </acceptance_criteria>

  <done>AGENCY_SEED_MANIFEST exported with real content for all 12 agencies; validateWordCount helper exported; zero placeholder text in file.</done>
</task>

<task type="auto">
  <name>Task 2: Seed writer helpers + master orchestrator script</name>
  <files>
    packages/db/src/seeds/seed-payload-collections.ts,
    packages/db/src/seeds/seed-crm-preseeds.ts,
    scripts/seed-all-agencies.mjs
  </files>

  <read_first>
    - packages/db/src/seeds/agency-seed-manifest.ts (created in Task 1 — types and manifest)
    - scripts/seed-runner.ts (full file — main() pattern, exit code contract, parseArgs shape)
    - scripts/compose-smoke.ts (poll-with-timeout pattern for health check before seeding)
    - packages/crm/src/index.ts (CRM exports to understand createContact, createDeal API)
    - 12-CONTEXT.md lines 30-35 (D-02 seed completeness requirements)
  </read_first>

  <action>
    Create `packages/db/src/seeds/seed-payload-collections.ts`:
    - Export `seedPayloadCollection(opts: { baseUrl: string; collection: string; records: unknown[]; jwtToken: string }): Promise<{ seeded: number; skipped: number; errors: string[] }>`
    - For each record: GET `/api/{collection}?where[slug][equals]={record.slug}&limit=1` — if `docs.length > 0` skip (idempotent)
    - POST `/api/{collection}` with `Authorization: Bearer {jwtToken}`, `Content-Type: application/json`, body = JSON.stringify(record)
    - Collect errors; return summary. Never throw — accumulate into `errors` array.
    - validateWordCount is called for fields that have wordCountMin set on the record (check `record._wordCountMin` if present)

    Create `packages/db/src/seeds/seed-crm-preseeds.ts`:
    - Export `seedCrmData(opts: { agencySlug: string; db: DrizzleDb }): Promise<void>`
    - Seeds: 3 pre-seeded contacts (name, email, phone, source='seed'), 2 open deals (title, value, stage='lead'), 1 pipeline record if not exists
    - All data is agency-niche realistic (e.g., dental agency gets patient-like contacts, not generic 'Test User')
    - Uses Drizzle insert().onConflictDoNothing() for idempotency

    Create `scripts/seed-all-agencies.mjs` (Node ESM, no TypeScript, .mjs extension):
    ```javascript
    #!/usr/bin/env node
    /**
     * scripts/seed-all-agencies.mjs
     * Complete seed run for all 12 agencies — all Payload CMS collections + CRM pre-seeds.
     * Exit codes: 0 = all seeded, 1 = one or more agencies failed.
     * Usage: node scripts/seed-all-agencies.mjs [--agency=web-ai] [--dry-run] [--help]
     */
    ```
    - Import `execSync` from 'node:child_process', `existsSync` from 'node:fs'
    - Parse --agency (run single agency), --dry-run (log what would be seeded but don't write), --help
    - For each of 12 agencies (or the --agency one):
      1. Check if `.planning/seed-status/{slug}.done` exists — if yes, log "SKIP {slug} (already seeded)" and continue
      2. Log "SEEDING {slug}..."
      3. In dry-run: log all collection counts and exit
      4. Run `npx tsx packages/db/src/seeds/seed-payload-collections.ts --agency={slug}` via execSync({ stdio: 'inherit' })
      5. If exit 0: write `.planning/seed-status/{slug}.done` and log "OK {slug}"
      6. If exit non-zero: push to failures array
    - After all agencies: if failures.length > 0: `console.error('FAILED:', failures.join(', ')); process.exit(1)`
    - Else: `console.log('All 12 agencies seeded successfully.'); process.exit(0)`
    - Wrap in `.catch(err => { console.error('seed-all-agencies: unexpected error:', err); process.exit(1) })`
  </action>

  <verify>
    <automated>
      node scripts/seed-all-agencies.mjs --help && echo "help flag works"; node scripts/seed-all-agencies.mjs --dry-run --agency=web-ai 2>&1 | head -5 && echo "dry-run works"; npx tsx -e "import { seedPayloadCollection } from './packages/db/src/seeds/seed-payload-collections.js'; console.log(typeof seedPayloadCollection)"
    </automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/seed-all-agencies.mjs --help` exits 0 and prints usage
    - `node scripts/seed-all-agencies.mjs --dry-run` exits 0 and logs "SEEDING" for each of 12 agencies without making HTTP calls
    - `grep "onConflictDoNothing\|idempotent\|already seeded" packages/db/src/seeds/seed-crm-preseeds.ts scripts/seed-all-agencies.mjs` returns matches showing idempotency
    - `grep "Lorem ipsum\|TODO\|placeholder" packages/db/src/seeds/seed-crm-preseeds.ts` returns 0 matches
    - `pnpm tsc --noEmit` passes with no errors from seed-payload-collections.ts and seed-crm-preseeds.ts
    - `.planning/seed-status/` directory created on first run (not error if absent)
  </acceptance_criteria>

  <done>seed-payload-collections.ts and seed-crm-preseeds.ts helpers are idempotent; seed-all-agencies.mjs orchestrates all 12 agencies with --dry-run, --agency, --help flags; exit 0 on success, exit 1 on any failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script → Payload REST API | JWT bearer token used to authenticate seed writes; token must not be hardcoded |
| Seed script → Drizzle DB | CRM seed uses direct DB connection; must filter by agency_id |
| Seed content → Payload validators | Anti-fabrication validators run on save; seed content must pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Spoofing | seed-payload-collections.ts | mitigate | JWT token passed as env var (SEED_JWT_TOKEN), never hardcoded in script |
| T-12-02-02 | Information Disclosure | seed-all-agencies.mjs | mitigate | `.planning/seed-status/` files contain only slug names — no PII or secrets |
| T-12-02-03 | Denial of Service | seed-all-agencies.mjs | accept | Script runs in controlled CI/CD context; rate limiting on /api/* routes handles any burst |
| T-12-02-04 | Tampering | seed content | mitigate | Payload validators (word count, anti-fab) run on every POST — seed content that fails validator is rejected at the API level |
</threat_model>

<verification>
1. `node scripts/seed-all-agencies.mjs --help` exits 0
2. `node scripts/seed-all-agencies.mjs --dry-run` logs 12 agencies, exits 0
3. `grep -c "Lorem ipsum\|TODO\|placeholder" packages/db/src/seeds/agency-seed-manifest.ts` returns 0
4. `Object.keys(AGENCY_SEED_MANIFEST).length` equals 12 (verified via tsx)
5. `pnpm tsc --noEmit` passes for all 3 TypeScript files
</verification>

<success_criteria>
- AGENCY_SEED_MANIFEST covers all 12 agencies with real content across 8 collection types
- All long-form content passes MIN_WORD_COUNT=2200 validation
- Seed script is idempotent (skip already-seeded agencies on re-run)
- seed-all-agencies.mjs supports --agency, --dry-run, --help flags
- Zero placeholder text in any seeded content
</success_criteria>

<output>
After completion, create `.planning/phases/12-launch-qa-seeds-runbooks-sla/12-02-SUMMARY.md`
</output>
