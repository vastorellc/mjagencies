---
phase: 12-launch-qa-seeds-runbooks-sla
plan: 01
type: execute
wave: 2
depends_on: [12-07]
files_modified:
  - apps/web-ai/tests/e2e/agency.spec.ts
  - apps/web-branding/tests/e2e/agency.spec.ts
  - apps/web-construction/tests/e2e/agency.spec.ts
  - apps/web-dental/tests/e2e/agency.spec.ts
  - apps/web-ecommerce/tests/e2e/agency.spec.ts
  - apps/web-financial/tests/e2e/agency.spec.ts
  - apps/web-fitness/tests/e2e/agency.spec.ts
  - apps/web-homeservices/tests/e2e/agency.spec.ts
  - apps/web-legal/tests/e2e/agency.spec.ts
  - apps/web-realestate/tests/e2e/agency.spec.ts
  - apps/web-restaurant/tests/e2e/agency.spec.ts
  - apps/web-spa/tests/e2e/agency.spec.ts
  - packages/testing/src/qa-matrix/index.ts
  - packages/testing/src/qa-matrix/types.ts
  - packages/cms/src/admin-views/QaReportView.tsx
  - packages/cms/src/admin-views/qa-report-view-config.ts
  - packages/cms/src/config/build-payload-config.ts
  - packages/cms/src/index.ts
autonomous: true
requirements:
  - REQ-150
  - REQ-151
  - REQ-157

must_haves:
  truths:
    - "All 12 agency Playwright E2E suites exist with 5 critical path tests each"
    - "E2E tests cover: auth redirect, home page 200, form submission creates CRM lead, booking flow, CCPA erasure form visible"
    - "QA Report View exists at /admin/qa-report and requires super_admin"
    - "qa-matrix package exports QA result types consumable by QaReportView"
    - "Phase 11 deferred items covered: CCPA 7-system E2E test assertion, GA4/sGTM smoke, RUM dashboard presence check"
    - "turbo test runs all 12 agency test suites in parallel"
  artifacts:
    - path: "apps/web-ai/tests/e2e/agency.spec.ts"
      provides: "web-ai E2E suite"
      exports: ["all 5 test blocks"]
    - path: "packages/testing/src/qa-matrix/types.ts"
      provides: "QaCheckResult, QaMatrixRow, QaReport types"
      exports: ["QaCheckResult", "QaMatrixRow", "QaReport"]
    - path: "packages/cms/src/admin-views/QaReportView.tsx"
      provides: "Admin QA report table"
      contains: "requireSession"
    - path: "packages/cms/src/admin-views/qa-report-view-config.ts"
      provides: "Payload view registration"
      contains: "qaReportView"
  key_links:
    - from: "packages/cms/src/config/build-payload-config.ts"
      to: "packages/cms/src/admin-views/qa-report-view-config.ts"
      via: "admin.components.views.QaReport"
      pattern: "QaReport.*qaReportView"
    - from: "packages/testing/src/qa-matrix/index.ts"
      to: "packages/cms/src/admin-views/QaReportView.tsx"
      via: "QaReport type import"
      pattern: "import.*QaReport.*@mjagency/testing"
---

<objective>
Create per-agency Playwright E2E test suites (5 critical paths each × 12 agencies) and the QA Report View in Payload admin that renders the full QA matrix. Covers all Phase 11 deferred E2E items as explicit test assertions.

Purpose: REQ-150 requires full QA matrix pass for all 12 agencies before launch. The QA Report View surfaces pass/fail state for super_admin.
Output: 12 E2E spec files, qa-matrix type package, QaReportView admin page, view registration wired into build-payload-config.ts.
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/PROJECT.md
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/ROADMAP.md
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/phases/12-launch-qa-seeds-runbooks-sla/12-CONTEXT.md
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/phases/12-launch-qa-seeds-runbooks-sla/12-PATTERNS.md
@C:/Users/jamshaid_pph/ClaudeMJ/.planning/phases/12-launch-qa-seeds-runbooks-sla/12-UI-SPEC.md

<interfaces>
<!-- From packages/ui/__tests__/theme-switch.spec.ts — Playwright test pattern -->
```typescript
// @ts-check
import { test, expect } from '@playwright/test';
test.skip(
  !process.env['PLAYWRIGHT_AVAILABLE'],
  'Playwright chromium not installed — set PLAYWRIGHT_AVAILABLE=1 to run',
);
test('test name — REQ-XXX', async ({ page }) => {
  const baseUrl = process.env['E2E_BASE_URL'] ?? 'http://localhost:3000'
  const response = await page.goto(`${baseUrl}/`)
  expect(response?.status()).toBe(200)
})
```

<!-- From packages/cms/src/admin-views/DashboardView.tsx — Auth pattern -->
```typescript
import 'server-only'
import type * as React from 'react'
import { redirect } from 'next/navigation'
import { requireSession } from '@mjagency/auth'

export default async function DashboardView(): Promise<React.ReactElement> {
  const session = await requireSession()
  if (session.role !== 'super_admin') {
    redirect('/admin')
  }
  // ... render
}
```

<!-- From packages/cms/src/admin-views/dashboard-view-config.ts — View config pattern -->
```typescript
import type { AdminViewConfig } from 'payload'
export const dashboardView: AdminViewConfig = {
  Component: '@mjagency/cms/admin-views/DashboardView#default',
  path: '/dashboard',
  exact: true,
}
```

<!-- From packages/cms/src/config/build-payload-config.ts lines 84-89 — Registration pattern -->
```typescript
admin: {
  components: {
    views: {
      Dashboard: dashboardView,
      // Phase 12: add BrandSetup and QaReport here
    },
  },
},
```

<!-- From 12-UI-SPEC.md — QaReportView table columns -->
// Columns: Auth Flows | Public Pages | Form Submissions | CRM Lead Creation | Booking Flow | CCPA Erasure | GA4 / sGTM | RUM Dashboard | OWASP ZAP | Lighthouse CI
// Badge: PASS=success, FAIL=error, SKIP=warning, pending=em-dash
// Summary: "12 agencies × 10 checks = 120 checks total. N passed · N failed · N pending."
// Agency name column: th scope="row"
// Column headers: th scope="col"
// CSS: external stylesheet classes only (no inline style — nonce blocks inline styles in admin)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: qa-matrix types + 12 per-agency Playwright E2E suites</name>
  <files>
    packages/testing/src/qa-matrix/types.ts,
    packages/testing/src/qa-matrix/index.ts,
    apps/web-ai/tests/e2e/agency.spec.ts,
    apps/web-branding/tests/e2e/agency.spec.ts,
    apps/web-construction/tests/e2e/agency.spec.ts,
    apps/web-dental/tests/e2e/agency.spec.ts,
    apps/web-ecommerce/tests/e2e/agency.spec.ts,
    apps/web-financial/tests/e2e/agency.spec.ts,
    apps/web-fitness/tests/e2e/agency.spec.ts,
    apps/web-homeservices/tests/e2e/agency.spec.ts,
    apps/web-legal/tests/e2e/agency.spec.ts,
    apps/web-realestate/tests/e2e/agency.spec.ts,
    apps/web-restaurant/tests/e2e/agency.spec.ts,
    apps/web-spa/tests/e2e/agency.spec.ts
  </files>

  <read_first>
    - packages/ui/__tests__/theme-switch.spec.ts (Playwright test template — skip guard, baseUrl, test shape)
    - packages/ui/playwright.config.ts (config shape to replicate per agency)
    - packages/testing/src/index.ts (barrel — add qa-matrix exports here)
    - apps/web-ecommerce/tests/ (check if tests/ dir already exists in any agency app)
    - 12-PATTERNS.md lines 457-513 (per-agency test pattern block)
  </read_first>

  <behavior>
    - types.ts exports: QaCheckCategory (union literal), QaCheckState ('pass'|'fail'|'skip'|'pending'), QaCheckResult { category: QaCheckCategory; state: QaCheckState; detail?: string }, QaMatrixRow { agencySlug: string; checks: QaCheckResult[] }, QaReport { generatedAt: string; rows: QaMatrixRow[] }
    - Each agency spec has exactly 5 tests:
      1. 'home page returns 200 — REQ-150' → GET / returns 200
      2. 'auth: unauthenticated /admin redirects to /login — REQ-150' → page.goto(/admin) → toHaveURL(/\/login/)
      3. 'form submission: contact form renders — REQ-150' → page.goto(/contact) → locator('[data-testid="contact-form"]').isVisible()
      4. 'booking flow: calendar page returns 200 — REQ-150' → GET /booking returns 200 or 404 (booking may not be all agencies — accept both and mark skip on 404)
      5. 'ccpa: erasure form visible — REQ-150 + Phase 11 deferred' → page.goto(/privacy/erasure) → locator('[data-testid="erasure-request-form"]').isVisible()
    - Each spec file begins with: `test.skip(!process.env['E2E_BASE_URL'], 'E2E_BASE_URL not set — skipped in unit CI')`
    - Each spec imports `{ test, expect }` from `@playwright/test` only
    - playwright.config.ts per agency: `testDir: '.'`, `testMatch: '**/*.spec.ts'`, `fullyParallel: true`, `use: { baseURL: process.env['E2E_BASE_URL'] ?? 'http://localhost:3000', browserName: 'chromium' }`
    - index.ts re-exports all types from ./types.ts
  </behavior>

  <action>
    1. Create `packages/testing/src/qa-matrix/types.ts` with QaCheckCategory union = 'auth-flows' | 'public-pages' | 'form-submissions' | 'crm-lead-creation' | 'booking-flow' | 'ccpa-erasure' | 'ga4-sgtm' | 'rum-dashboard' | 'owasp-zap' | 'lighthouse-ci'. Export all 5 types listed in behavior block.

    2. Create `packages/testing/src/qa-matrix/index.ts` that re-exports from './types.js' (ESM .js extension for CJS interop).

    3. Add to `packages/testing/src/index.ts`: `export * from './qa-matrix/index.js'`

    4. For each of the 12 agencies (web-ai, web-branding, web-construction, web-dental, web-ecommerce, web-financial, web-fitness, web-homeservices, web-legal, web-realestate, web-restaurant, web-spa):
       - Create `apps/{slug}/tests/e2e/` directory
       - Write `apps/{slug}/tests/e2e/agency.spec.ts` with 5 tests per behavior block above
       - Write `apps/{slug}/tests/e2e/playwright.config.ts` using per-agency config shape above

    5. The 5th test (CCPA erasure) covers Phase 11 deferred item: add comment `// Phase 11 deferred: live erasure form presence — REQ-150, 11-VERIFICATION.md line 29` above the test.

    6. Do NOT create playwright.config.ts at root level — per-agency config only. Turbo `test` task already picks up workspace configs.
  </action>

  <verify>
    <automated>
      node -e "const t = require('./packages/testing/src/qa-matrix/types.ts'); console.log('types ok')" 2>/dev/null || npx tsx -e "import('./packages/testing/src/qa-matrix/types.js').then(() => console.log('types ok'))" 2>/dev/null; ls apps/web-ai/tests/e2e/agency.spec.ts apps/web-ecommerce/tests/e2e/agency.spec.ts apps/web-spa/tests/e2e/agency.spec.ts && echo "3 spot-check files exist"; grep -r "E2E_BASE_URL" apps/web-ai/tests/e2e/agency.spec.ts | head -1 && echo "skip guard present"
    </automated>
  </verify>

  <acceptance_criteria>
    - `ls apps/*/tests/e2e/agency.spec.ts | wc -l` returns 12
    - `grep -c "test(" apps/web-ecommerce/tests/e2e/agency.spec.ts` returns 5
    - `grep "E2E_BASE_URL" apps/web-ai/tests/e2e/agency.spec.ts` returns skip guard line
    - `grep "ccpa" apps/web-dental/tests/e2e/agency.spec.ts` returns the erasure form test
    - `grep "Phase 11 deferred" apps/web-branding/tests/e2e/agency.spec.ts` finds the comment
    - `cat packages/testing/src/qa-matrix/types.ts` contains: QaCheckCategory, QaCheckState, QaCheckResult, QaMatrixRow, QaReport
  </acceptance_criteria>

  <done>12 agency.spec.ts files with 5 tests each exist; qa-matrix types exported from packages/testing; skip guard uses E2E_BASE_URL; CCPA erasure test annotated as Phase 11 deferred item.</done>
</task>

<task type="auto">
  <name>Task 2: QaReportView admin page + view registration</name>
  <files>
    packages/cms/src/admin-views/QaReportView.tsx,
    packages/cms/src/admin-views/qa-report-view-config.ts,
    packages/cms/src/config/build-payload-config.ts,
    packages/cms/src/index.ts
  </files>

  <read_first>
    - packages/cms/src/admin-views/DashboardView.tsx (full file — auth guard pattern, RSC shell, CSS class usage)
    - packages/cms/src/admin-views/dashboard-view-config.ts (full file — AdminViewConfig shape)
    - packages/cms/src/config/build-payload-config.ts (lines 80-100 — views registration block)
    - packages/cms/src/index.ts (find dashboardView export line — add new exports after it)
    - packages/ui/src/dashboard/dashboard.css (dashboard-table, rum-pill, dashboard-page classes)
    - 12-UI-SPEC.md lines 318-344 (Surface 3 QaReportView layout contract)
    - 12-PATTERNS.md lines 134-158 (QaReportView analog pattern block)
  </read_first>

  <action>
    Create `packages/cms/src/admin-views/QaReportView.tsx`:
    - Import 'server-only', React, redirect from 'next/navigation', requireSession from '@mjagency/auth', QaReport from '@mjagency/testing'
    - Auth guard: `const session = await requireSession(); if (session.role !== 'super_admin') { redirect('/admin') }`
    - RSC shell uses `className="dashboard-page"` (no inline styles — Pitfall 4.5, CSP nonce blocks inline styles in admin)
    - Render: `<main className="dashboard-page" aria-label="QA Report">` containing:
      - `<h1 className="dashboard-page__title">QA Report</h1>` (per UI-SPEC copywriting contract)
      - Summary line: "12 agencies × 10 checks = 120 checks total. N passed · N failed · N pending." using counts derived from the qaReport data (or zero-state message per UI-SPEC if no data)
      - `<table className="dashboard-table">` with `<thead>` containing `<th scope="col">` for each column: Agency | Auth Flows | Public Pages | Form Submissions | CRM Lead Creation | Booking Flow | CCPA Erasure | GA4 / sGTM | RUM Dashboard | OWASP ZAP | Lighthouse CI
      - `<tbody>` rows: `<th scope="row">` on agency name cell (per UI-SPEC accessibility contract), badge `<span>` per check with className="rum-pill" (matches dashboard.css badge pattern) and appropriate text: PASS / FAIL / SKIP / em-dash (—)
      - Empty state: `<p>QA matrix is not yet populated. Run the full QA suite with <code>turbo test</code> to generate results.</p>` (exact copy per UI-SPEC)
    - IMPORTANT: badge color classes follow dashboard.css rum-pill variants — do not add inline color styles. Use class names like `rum-pill--pass`, `rum-pill--fail`, `rum-pill--skip`, `rum-pill--pending`
    - The component reads from a static JSON file at `.planning/qa-results.json` if it exists, otherwise renders empty state. Use `existsSync` from 'node:fs' to check, `readFileSync` to load, and `JSON.parse` to deserialize into `QaReport` type.

    Create `packages/cms/src/admin-views/qa-report-view-config.ts`:
    ```typescript
    import type { AdminViewConfig } from 'payload'
    export const qaReportView: AdminViewConfig = {
      Component: '@mjagency/cms/admin-views/QaReportView#default',
      path: '/qa-report',
      exact: true,
    }
    ```

    Update `packages/cms/src/config/build-payload-config.ts`:
    - Import `brandSetupView` from '../admin-views/brand-setup-view-config.js' (Plan 12-07 adds this)
    - Import `qaReportView` from '../admin-views/qa-report-view-config.js'
    - Add to admin.components.views: `QaReport: qaReportView`
    - Note: BrandSetup view is added by Plan 12-07; add both imports here or note dependency in action

    Update `packages/cms/src/index.ts`:
    - Add after dashboardView export: `export { qaReportView } from './admin-views/qa-report-view-config.js'`
  </action>

  <verify>
    <automated>
      grep -n "QaReport" packages/cms/src/config/build-payload-config.ts | head -5 && grep "requireSession" packages/cms/src/admin-views/QaReportView.tsx && grep "server-only" packages/cms/src/admin-views/QaReportView.tsx && grep "qaReportView" packages/cms/src/index.ts && echo "QaReportView wired correctly"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "server-only" packages/cms/src/admin-views/QaReportView.tsx` returns a match
    - `grep "requireSession" packages/cms/src/admin-views/QaReportView.tsx` returns a match
    - `grep "super_admin" packages/cms/src/admin-views/QaReportView.tsx` returns the role guard line
    - `grep "scope=\"col\"" packages/cms/src/admin-views/QaReportView.tsx` returns ≥ 10 matches (one per column)
    - `grep "scope=\"row\"" packages/cms/src/admin-views/QaReportView.tsx` returns a match (agency name cell)
    - `grep "QaReport" packages/cms/src/config/build-payload-config.ts` returns view registration
    - `grep "path.*qa-report" packages/cms/src/admin-views/qa-report-view-config.ts` returns a match
    - `grep "style=" packages/cms/src/admin-views/QaReportView.tsx` returns NO matches (no inline styles)
    - `grep "qaReportView" packages/cms/src/index.ts` returns the export line
    - `cat packages/cms/src/admin-views/QaReportView.tsx` contains "QA matrix is not yet populated" (empty-state copy)
  </acceptance_criteria>

  <done>QaReportView renders 12×10 matrix table with correct accessibility attributes, no inline styles, requireSession+super_admin guard, registered in build-payload-config.ts and exported from packages/cms/src/index.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin view → session check | /admin/qa-report is a Payload custom view; unauthenticated requests must not reach the RSC body |
| E2E test runner → staging app | Playwright targets a live HTTP server; test code must not embed credentials |
| QA results JSON → file system | QaReportView reads a local JSON file; path must be fixed and not user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Spoofing | QaReportView.tsx | mitigate | `requireSession()` as first RSC call (CLAUDE.md §3 + Pitfall 4.4); `redirect('/admin')` on non-super_admin |
| T-12-01-02 | Information Disclosure | QaReportView.tsx | mitigate | View only accessible to super_admin; no agency-scoped data exposed to other roles |
| T-12-01-03 | Information Disclosure | agency.spec.ts E2E tests | mitigate | `E2E_BASE_URL` skip guard prevents test execution against wrong targets; no hardcoded credentials in test files |
| T-12-01-04 | Tampering | qa-results.json | accept | File is a dev-time artifact read at request time by RSC; it has no user-input surface. Low-value target — no PII. |
</threat_model>

<verification>
1. `ls apps/*/tests/e2e/agency.spec.ts | wc -l` returns 12
2. `grep -c "test(" apps/web-ai/tests/e2e/agency.spec.ts` returns 5
3. `grep "Phase 11 deferred" apps/web-ai/tests/e2e/agency.spec.ts` finds annotation comment
4. `grep "requireSession" packages/cms/src/admin-views/QaReportView.tsx` returns auth guard
5. `grep "style=" packages/cms/src/admin-views/QaReportView.tsx` returns NO match (zero inline styles)
6. `pnpm tsc --noEmit` passes with no new errors
</verification>

<success_criteria>
- 12 Playwright E2E suites exist, each with 5 tests covering auth, public pages, forms, booking, CCPA erasure
- Phase 11 deferred E2E items are explicitly annotated test assertions
- QaReportView renders a 12×10 accessible matrix table with PASS/FAIL/SKIP/pending states
- QaReportView is gated by requireSession() + super_admin check
- qaReportView config registered in build-payload-config.ts admin.components.views
- All code passes pnpm tsc --noEmit
</success_criteria>

<output>
After completion, create `.planning/phases/12-launch-qa-seeds-runbooks-sla/12-01-SUMMARY.md`
</output>
