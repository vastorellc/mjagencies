---
phase: 07-ai-assistant
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/ai/src/cost-cap.ts
  - packages/ai/src/__tests__/cost-cap.test.ts
  - packages/ai/src/model-routing.ts
  - packages/ai/src/__tests__/model-routing.test.ts
  - packages/ai/src/generate-content.ts
  - packages/ai/src/__tests__/generate-content.test.ts
  - packages/ai/src/index.ts
  - packages/ai/package.json
  - apps/web-main/src/jobs/cost-reset.ts
  - apps/web-main/instrumentation.node.ts
autonomous: true
requirements: [REQ-080]

must_haves:
  truths:
    - "generateContent() accepts agencyId and resolves per-agency LiteLLM API key"
    - "Monthly per-agency budget enforced via Redis counter; exceeding cap throws AiBudgetExceededError"
    - "Successful LiteLLM responses increment Redis spend counter (estimated cents)"
    - "Model routing returns the correct model for each tier (tier1-bulk default for editor features)"
    - "BullMQ cron at '0 0 1 * *' resets all agency monthly-spend counters"
  artifacts:
    - path: "packages/ai/src/cost-cap.ts"
      provides: "checkAgencyCostCap, recordAgencySpend, getAgencyLiteLLMKey, AiBudgetExceededError"
      exports: ["checkAgencyCostCap", "recordAgencySpend", "getAgencyLiteLLMKey", "resetMonthlySpend", "AiBudgetExceededError"]
    - path: "packages/ai/src/model-routing.ts"
      provides: "getModelForTier and tier->model map"
      exports: ["getModelForTier", "ModelTier", "MODEL_ROUTING"]
    - path: "packages/ai/src/generate-content.ts"
      provides: "Extended generateContent() with agencyId, tier, cost cap enforcement"
      contains: "checkAgencyCostCap"
    - path: "packages/ai/src/__tests__/cost-cap.test.ts"
      provides: "Vitest unit tests for cost cap + key lookup (TDD red->green)"
    - path: "packages/ai/src/__tests__/model-routing.test.ts"
      provides: "Vitest unit tests for tier->model mapping"
    - path: "apps/web-main/src/jobs/cost-reset.ts"
      provides: "BullMQ repeatable cron job that calls resetMonthlySpend()"
      exports: ["registerCostReset"]
  key_links:
    - from: "packages/ai/src/generate-content.ts"
      to: "packages/ai/src/cost-cap.ts"
      via: "checkAgencyCostCap(agencyId) called BEFORE fetch; recordAgencySpend(agencyId, cents) AFTER success"
      pattern: "checkAgencyCostCap|recordAgencySpend"
    - from: "apps/web-main/instrumentation.node.ts"
      to: "apps/web-main/src/jobs/cost-reset.ts"
      via: "await registerCostReset()"
      pattern: "registerCostReset"
---

<objective>
Upgrade `packages/ai/generateContent()` to support per-agency LiteLLM budget enforcement (REQ-080):
1. Resolve a per-agency LiteLLM API key from `LITELLM_API_KEY_<AGENCY_ID_UPPER>` (fallback to global `LITELLM_API_KEY`)
2. Enforce a monthly cap from `LITELLM_BUDGET_<AGENCY_ID_UPPER>` (cents) via a Redis counter at `agency:<id>:ai:monthly-spend`
3. Provide a model-routing table (tier1-bulk, tier2-writing, tier2-research, tier3-max). Default tier for editor features = tier1-bulk
4. Register a BullMQ monthly cron at `'0 0 1 * *'` that resets all agency spend counters

Purpose: Cost containment + tiered model selection are prerequisites for Plan 07-02 (20 editor actions), 07-05 (PII redactor) and 07-06 (prompt guard). Wave 1 work — no other Phase 7 plans block on each other for code, but they ALL import this plan's exports.

Output: `cost-cap.ts`, `model-routing.ts`, extended `generate-content.ts`, BullMQ cron worker `cost-reset.ts`, plus full Vitest coverage (TDD).
</objective>

<execution_context>
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamshaid_pph/ClaudeMJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-ai-assistant/07-PATTERNS.md
@CLAUDE.md
@packages/ai/src/generate-content.ts
@packages/ai/src/index.ts
@packages/ai/package.json
@packages/seo/src/config-cache.ts
@apps/web-main/src/jobs/self-learning.ts
@apps/web-main/instrumentation.node.ts

<interfaces>
<!-- Existing exports the executor MUST NOT break (used by Phase 5 + Phase 6 self-learning worker). -->

From packages/ai/src/generate-content.ts (current):
```typescript
export interface GenerateContentParams {
  prompt: string
  agencySlug: string
  pageType: 'home' | 'about' | 'services' | 'blog' | 'contact' | 'tool' | 'landing' | 'legal'
  maxTokens?: number
}
export interface GenerateContentResult {
  text: string
  aiContentRatio: number
  isAiGenerated: true
  model: string
}
export async function generateContent(params: GenerateContentParams): Promise<GenerateContentResult>
```

Phase 7 EXTENDS GenerateContentParams with optional fields (do not break callers):
```typescript
export interface GenerateContentParams {
  prompt: string
  agencySlug: string
  pageType: 'home' | 'about' | 'services' | 'blog' | 'contact' | 'tool' | 'landing' | 'legal'
  maxTokens?: number
  // Phase 7 additions (all optional — callers without agencyId fall back to global key, no cap):
  agencyId?: string
  tier?: ModelTier  // default: 'tier1-bulk'
  systemPrompt?: string  // optional override; default keeps anti-fab system prompt
}
```

From packages/seo/src/config-cache.ts (lines 14-19) — copy this Redis client pattern verbatim:
```typescript
function createRedisClient(): Redis {
  return new Redis({
    host: process.env['REDIS_HOST'] ?? 'localhost',
    port: parseInt(process.env['REDIS_PORT'] ?? '6379', 10),
  })
}
// Always wrap in try { ... } finally { await redis.quit() }
```

From packages/queue/src/index.ts:
```typescript
export { createEncryptedQueue, createEncryptedWorker } from './encrypted-queue.js'
```

From apps/web-main/src/jobs/self-learning.ts (lines 26-86) — copy register-cron pattern verbatim, using getRepeatableJobs() Pitfall 7 dedup.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — cost-cap.ts (RED): write failing tests for getAgencyLiteLLMKey + checkAgencyCostCap + recordAgencySpend</name>
  <files>packages/ai/src/__tests__/cost-cap.test.ts, packages/ai/package.json</files>
  <read_first>
    - packages/ai/src/generate-content.ts (current shape of generateContent params)
    - packages/seo/src/config-cache.ts (Redis lifecycle pattern — try/finally redis.quit())
    - packages/cms/src/__tests__/content-validators.test.ts (Vitest test layout pattern)
  </read_first>
  <behavior>
    Test cases (all should FAIL initially because cost-cap.ts does not exist yet):

    A. getAgencyLiteLLMKey(agencyId):
      - Returns `process.env.LITELLM_API_KEY_ECOMMERCE` when set (uppercase agencyId 'ecommerce')
      - Falls back to `process.env.LITELLM_API_KEY` when per-agency key not set
      - Returns empty string when neither is set
      - Uppercase normalization: agencyId 'eCommerce' → looks up LITELLM_API_KEY_ECOMMERCE

    B. checkAgencyCostCap(agencyId):
      - When LITELLM_BUDGET_<ID> is unset → resolves (no cap, no throw)
      - When monthly-spend counter < cap → resolves
      - When monthly-spend counter >= cap → throws AiBudgetExceededError with message 'Agency <id> exceeded monthly LiteLLM budget'
      - Reads Redis key `agency:<id>:ai:monthly-spend` (lowercase agencyId in key)

    C. recordAgencySpend(agencyId, cents):
      - INCRBY on `agency:<id>:ai:monthly-spend` by `cents`
      - Sets EXPIRE to 35 * 86400 if key was newly created (so unset keys eventually drop)
      - Does not throw on Redis error (logs and swallows — cost tracking must never break user requests)

    D. resetMonthlySpend():
      - Deletes ALL keys matching `agency:*:ai:monthly-spend` via SCAN + DEL pipeline
      - Returns count of keys deleted

    E. AiBudgetExceededError extends Error and has `name === 'AiBudgetExceededError'`

    Use ioredis-mock (add to packages/ai/package.json devDependencies: "ioredis-mock": "8.9.0", "ioredis": "5.4.1") and inject via `vi.mock('ioredis', () => ({ Redis: RedisMock }))` at top of test file. Add "ioredis": "5.4.1" to dependencies (NOT devDependencies) since it ships in the runtime.
  </behavior>
  <action>
    1. Update packages/ai/package.json:
       - Add to "dependencies": `"ioredis": "5.4.1"`
       - Add to "devDependencies": `"ioredis-mock": "8.9.0"`
    2. Create packages/ai/src/__tests__/cost-cap.test.ts with describe blocks for each behavior above. Use `import RedisMock from 'ioredis-mock'` and `vi.mock('ioredis', () => ({ Redis: RedisMock }))` at top of file (BEFORE any cost-cap import). Reset env between tests with `beforeEach(() => { process.env = { ...ORIGINAL_ENV } })`.
    3. Run `pnpm --filter @mjagency/ai test` — verify ALL tests fail with "Cannot find module './cost-cap.js'" or similar.
    4. COMMIT: `test(07-01): add failing tests for cost-cap (RED)`
  </action>
  <verify>
    <automated>cd packages/ai && pnpm test 2>&1 | grep -E "FAIL|Cannot find module|cost-cap"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/ai/src/__tests__/cost-cap.test.ts` exists
    - `grep -c "describe\\|it(" packages/ai/src/__tests__/cost-cap.test.ts` returns >= 12 (4 behaviors x 3+ assertions)
    - `pnpm --filter @mjagency/ai test` shows test failures (RED state) — exit code 1
    - `grep -q "ioredis-mock" packages/ai/package.json` succeeds
    - `grep -q "\\"ioredis\\": \\"5.4.1\\"" packages/ai/package.json` succeeds
    - Commit message starts with `test(07-01):`
  </acceptance_criteria>
  <done>Failing tests committed (RED state). Test file imports './cost-cap.js' (or './cost-cap.ts'), failing because file doesn't exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — cost-cap.ts (GREEN) + model-routing.ts + extend generate-content.ts</name>
  <files>packages/ai/src/cost-cap.ts, packages/ai/src/model-routing.ts, packages/ai/src/__tests__/model-routing.test.ts, packages/ai/src/generate-content.ts, packages/ai/src/__tests__/generate-content.test.ts, packages/ai/src/index.ts</files>
  <read_first>
    - packages/ai/src/__tests__/cost-cap.test.ts (just-written failing tests — implementation must satisfy them)
    - packages/ai/src/generate-content.ts (existing implementation — extend without breaking Phase 5/6 callers)
    - packages/seo/src/config-cache.ts lines 14-86 (Redis client pattern — copy verbatim)
  </read_first>
  <behavior>
    A. cost-cap.ts implementation must make ALL Task 1 tests pass.

    B. model-routing.ts behavior:
      - Type `ModelTier = 'tier1-bulk' | 'tier2-writing' | 'tier2-research' | 'tier3-max'`
      - `MODEL_ROUTING` const map:
        * tier1-bulk → 'gemini-2.5-flash-lite' (primary), 'gpt-4.1-nano' (fallback array entry [1])
        * tier2-writing → 'claude-sonnet-4-6'
        * tier2-research → 'gemini-2.5-pro'
        * tier3-max → 'claude-opus-4-6'
      - `getModelForTier(tier)` returns the primary model string (first entry).
      - Default tier when undefined → 'tier1-bulk'

    C. generate-content.ts EXTENSION (preserve all Phase 5 behavior — stub fallback when LITELLM_API_URL absent must STILL WORK):
      - Add optional fields: `agencyId?`, `tier?`, `systemPrompt?` to GenerateContentParams
      - When LITELLM_API_URL is absent → stub fallback unchanged (Phase 5 behavior). Still returns GenerateContentResult.
      - When LITELLM_API_URL is present:
        1. If params.agencyId is provided: call `await checkAgencyCostCap(params.agencyId)` BEFORE fetch (throws AiBudgetExceededError if over cap)
        2. Resolve API key: `getAgencyLiteLLMKey(params.agencyId ?? '')` (falls back to global LITELLM_API_KEY)
        3. Resolve model: `getModelForTier(params.tier)`
        4. Use `params.systemPrompt` if provided; otherwise the existing anti-fabrication system prompt
        5. After successful fetch, estimate cost in cents (rough heuristic: tokens used * 0.000002 USD * 100 = cents; use response.usage.total_tokens if present, else estimate prompt+maxTokens). Call `await recordAgencySpend(params.agencyId, cents)` if agencyId provided. Errors swallowed (cost tracking must never break user request).
        6. Pass `metadata: { tags: ['agency:' + agencyId] }` in LiteLLM body when agencyId provided (LiteLLM budget tagging)
      - Existing Phase 5/6 callers (no agencyId, no tier) MUST keep working unchanged: same prompt, same flash-lite model, same return shape.

    D. generate-content.test.ts:
      - Test stub fallback path (no LITELLM_API_URL) — unchanged Phase 5 behavior
      - Test cost cap throws when over limit (mock checkAgencyCostCap to throw)
      - Test per-agency key used when agencyId provided
      - Test model routing: tier='tier2-writing' uses 'claude-sonnet-4-6'
      - Test recordAgencySpend called after success (use vi.fn() mock)
      - Test backward compat: call without agencyId → no Redis access, no errors
  </behavior>
  <action>
    1. Create `packages/ai/src/model-routing.ts`:
       ```typescript
       export type ModelTier = 'tier1-bulk' | 'tier2-writing' | 'tier2-research' | 'tier3-max'
       export const MODEL_ROUTING: Record<ModelTier, readonly string[]> = {
         'tier1-bulk': ['gemini-2.5-flash-lite', 'gpt-4.1-nano'],
         'tier2-writing': ['claude-sonnet-4-6'],
         'tier2-research': ['gemini-2.5-pro'],
         'tier3-max': ['claude-opus-4-6'],
       } as const
       export function getModelForTier(tier?: ModelTier): string {
         const t = tier ?? 'tier1-bulk'
         return MODEL_ROUTING[t][0] ?? 'gemini-2.5-flash-lite'
       }
       ```

    2. Create `packages/ai/src/cost-cap.ts` (copy createRedisClient from config-cache.ts):
       - `export class AiBudgetExceededError extends Error { constructor(agencyId: string) { super(`Agency ${agencyId} exceeded monthly LiteLLM budget`); this.name = 'AiBudgetExceededError' } }`
       - `export function getAgencyLiteLLMKey(agencyId: string): string` — reads `process.env['LITELLM_API_KEY_' + agencyId.toUpperCase()] ?? process.env['LITELLM_API_KEY'] ?? ''`
       - `export async function checkAgencyCostCap(agencyId: string): Promise<void>`:
         * Read `process.env['LITELLM_BUDGET_' + agencyId.toUpperCase()]` — if absent, return.
         * Otherwise create Redis client, GET `agency:${agencyId}:ai:monthly-spend` (parseInt or 0), compare to cap (parseInt of env var, in cents). Throw AiBudgetExceededError if >=. Always finally `await redis.quit()`.
       - `export async function recordAgencySpend(agencyId: string, cents: number): Promise<void>`:
         * Try: createRedisClient, INCRBY on key by Math.ceil(cents). If new key (TTL is -1), set EXPIRE to 35 * 86400. finally redis.quit().
         * Catch: console.warn(`[ai-cost-cap] failed to record spend for ${agencyId}: ...`); swallow.
       - `export async function resetMonthlySpend(): Promise<number>`:
         * SCAN MATCH `agency:*:ai:monthly-spend` (ioredis stream-style scanning). Collect keys. UNLINK or DEL them in pipeline. Return count. finally redis.quit().

    3. Create `packages/ai/src/__tests__/model-routing.test.ts` — 4 tests, one per tier; one for default (undefined → tier1-bulk).

    4. Edit `packages/ai/src/generate-content.ts`:
       - Add new optional fields to GenerateContentParams (agencyId, tier, systemPrompt) — see <behavior> C
       - Import `checkAgencyCostCap, recordAgencySpend, getAgencyLiteLLMKey` from './cost-cap.js'
       - Import `getModelForTier` from './model-routing.js'
       - Inside generateContent(): preserve LITELLM_API_URL absent stub path. In the present path:
         a. `if (params.agencyId) await checkAgencyCostCap(params.agencyId)`
         b. `const litellmKey = params.agencyId ? getAgencyLiteLLMKey(params.agencyId) : (process.env['LITELLM_API_KEY'] ?? '')`
         c. `const model = getModelForTier(params.tier)`
         d. `const sysPrompt = params.systemPrompt ?? <existing anti-fab system prompt>`
         e. body now uses `model` instead of hardcoded 'flash-lite'; add `metadata: { tags: params.agencyId ? ['agency:' + params.agencyId] : [] }`
         f. After successful parse: `if (params.agencyId) { const tokens = (json as any).usage?.total_tokens ?? (params.maxTokens ?? 2000); const cents = Math.ceil(tokens * 0.0002); await recordAgencySpend(params.agencyId, cents) }`

    5. Create `packages/ai/src/__tests__/generate-content.test.ts`:
       - Mock fetch globally with `vi.spyOn(globalThis, 'fetch').mockResolvedValue(...)` returning OK response with choices and usage.
       - Mock cost-cap module with `vi.mock('../cost-cap.js', () => ({ checkAgencyCostCap: vi.fn(), recordAgencySpend: vi.fn(), getAgencyLiteLLMKey: vi.fn().mockReturnValue('test-key'), AiBudgetExceededError: class extends Error {} }))`.
       - Tests: stub fallback (no LITELLM_API_URL), cost cap throws when mocked to throw, per-agency key used, tier='tier2-writing' uses 'claude-sonnet-4-6', recordAgencySpend called, backward compat.

    6. Update `packages/ai/src/index.ts` to add:
       ```typescript
       export { checkAgencyCostCap, recordAgencySpend, getAgencyLiteLLMKey, resetMonthlySpend, AiBudgetExceededError } from './cost-cap.js'
       export { getModelForTier, MODEL_ROUTING, type ModelTier } from './model-routing.js'
       ```

    7. Run `pnpm --filter @mjagency/ai test` — ALL tests must pass (GREEN).
    8. Run `pnpm --filter @mjagency/ai typecheck` — must pass.
    9. COMMIT: `feat(07-01): cost cap + model routing + agencyId-aware generateContent (GREEN)`
  </action>
  <verify>
    <automated>cd packages/ai && pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/ai/src/cost-cap.ts` exists with exports: getAgencyLiteLLMKey, checkAgencyCostCap, recordAgencySpend, resetMonthlySpend, AiBudgetExceededError (verify with `grep -c "^export" packages/ai/src/cost-cap.ts` >= 5)
    - File `packages/ai/src/model-routing.ts` exists with `getModelForTier` and `MODEL_ROUTING`
    - `grep -q "claude-sonnet-4-6" packages/ai/src/model-routing.ts` succeeds
    - `grep -q "gemini-2.5-flash-lite" packages/ai/src/model-routing.ts` succeeds
    - `grep -q "checkAgencyCostCap" packages/ai/src/generate-content.ts` succeeds
    - `grep -q "recordAgencySpend" packages/ai/src/generate-content.ts` succeeds
    - `grep -q "getModelForTier" packages/ai/src/generate-content.ts` succeeds
    - `grep -q "metadata.*tags.*agency:" packages/ai/src/generate-content.ts` succeeds (LiteLLM budget tagging)
    - `pnpm --filter @mjagency/ai test` exit code 0 and shows ALL tests pass
    - `pnpm --filter @mjagency/ai typecheck` exit code 0
    - `grep -q "checkAgencyCostCap" packages/ai/src/index.ts` succeeds (re-exported)
  </acceptance_criteria>
  <done>cost-cap, model-routing, and extended generate-content all green. Phase 5/6 callers (no agencyId) still work unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: BullMQ monthly cron — cost-reset job</name>
  <files>apps/web-main/src/jobs/cost-reset.ts, apps/web-main/instrumentation.node.ts</files>
  <read_first>
    - apps/web-main/src/jobs/self-learning.ts (canonical cron registration pattern with Pitfall 7 dedup — copy verbatim, swap names + cron)
    - apps/web-main/instrumentation.node.ts (existing job registration order)
    - packages/ai/src/cost-cap.ts (just-written resetMonthlySpend export)
  </read_first>
  <action>
    1. Create `apps/web-main/src/jobs/cost-reset.ts` modeled exactly on self-learning.ts (lines 1-86):
       - Cron: `'0 0 1 * *'` (midnight on the 1st of each month)
       - Job name: `'reset'`
       - Queue name: `'ai-cost-reset'`
       - Worker body: `const { resetMonthlySpend } = await import('@mjagency/ai'); const count = await resetMonthlySpend(); console.info('[ai-cost-reset] cleared ' + count + ' counters')`
       - Pitfall 7 dedup: getRepeatableJobs() check before add (same shape as self-learning.ts lines 28-49)
       - Export: `export async function registerCostReset(): Promise<void>`

    2. Edit `apps/web-main/instrumentation.node.ts` — add at the bottom (after registerAlgoWatcher):
       ```typescript
       // Phase 7: AI cost-cap monthly reset (REQ-080)
       import { registerCostReset } from './src/jobs/cost-reset.js'
       await registerCostReset()
       ```

    3. JSDoc comment must NOT contain `*/` sequences inside cron expressions (STATE.md decision: TypeScript misparses as closing JSDoc). Use `'0 0 1 * *'` (no slashes) — safe.

    4. Run `pnpm --filter @mjagency/web-main typecheck`.
    5. COMMIT: `feat(07-01): register monthly cost-reset cron job`
  </action>
  <verify>
    <automated>grep -q "registerCostReset" apps/web-main/instrumentation.node.ts && grep -F "'0 0 1 * *'" apps/web-main/src/jobs/cost-reset.ts && cd apps/web-main && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web-main/src/jobs/cost-reset.ts` exists
    - `grep -q "registerCostReset" apps/web-main/src/jobs/cost-reset.ts` succeeds
    - `grep -F "'0 0 1 * *'" apps/web-main/src/jobs/cost-reset.ts` succeeds
    - `grep -q "getRepeatableJobs" apps/web-main/src/jobs/cost-reset.ts` succeeds (Pitfall 7 dedup)
    - `grep -q "resetMonthlySpend" apps/web-main/src/jobs/cost-reset.ts` succeeds
    - `grep -q "import { registerCostReset }" apps/web-main/instrumentation.node.ts` succeeds
    - `grep -q "await registerCostReset()" apps/web-main/instrumentation.node.ts` succeeds
    - `pnpm --filter @mjagency/web-main typecheck` exit code 0
  </acceptance_criteria>
  <done>Monthly cron registered. Spend counters reset on the 1st of each month at midnight UTC.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @mjagency/ai test` → all green
2. `pnpm --filter @mjagency/ai typecheck` → exit 0
3. `pnpm --filter @mjagency/web-main typecheck` → exit 0
4. `grep -q "AiBudgetExceededError" packages/ai/src/index.ts` (re-exported)
5. Phase 5 caller (`generateTldr` in apps/web-main/src/actions/seo-score.ts) compiles unchanged
6. Phase 6 caller (worker.ts at packages/seo/src/self-learning/worker.ts:161) compiles unchanged
</verification>

<success_criteria>
- generateContent({agencyId, tier, ...}) enforces per-agency monthly budget
- AiBudgetExceededError thrown when over cap (callers must catch)
- Model routing returns correct primary model for each tier
- BullMQ cron at '0 0 1 * *' registered and idempotent (Pitfall 7)
- All existing Phase 5/6 callers continue to work without modification
- TDD: RED commit precedes GREEN commit
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-assistant/07-01-SUMMARY.md` with: files added, files modified, exports added (cost-cap, model-routing), test counts, commit hashes (RED + GREEN + cron).
</output>
